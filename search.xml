<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Unity如何快速创建URPShader模板</title>
      <link href="/undefined/84a14b1a.html"/>
      <url>/undefined/84a14b1a.html</url>
      
        <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><h2 id="emsp-在Unity中创建模板Shader，我们都是通过右键Create-Shader，然后输入自己的Shader名字创建，这样可以快速创建一个模板Shader，省去很多麻烦。但是在UnityURP中，我们却没有可以创建URPShader模板的功能，除非自己实现。"><a href="#emsp-在Unity中创建模板Shader，我们都是通过右键Create-Shader，然后输入自己的Shader名字创建，这样可以快速创建一个模板Shader，省去很多麻烦。但是在UnityURP中，我们却没有可以创建URPShader模板的功能，除非自己实现。" class="headerlink" title="&emsp;在Unity中创建模板Shader，我们都是通过右键Create-&gt;Shader，然后输入自己的Shader名字创建，这样可以快速创建一个模板Shader，省去很多麻烦。但是在UnityURP中，我们却没有可以创建URPShader模板的功能，除非自己实现。"></a>&emsp;在Unity中创建模板Shader，我们都是通过右键Create-&gt;Shader，然后输入自己的Shader名字创建，这样可以快速创建一个模板Shader，省去很多麻烦。但是在UnityURP中，我们却没有可以创建URPShader模板的功能，除非自己实现。</h2><h1 id="如何创建模板URPShader"><a href="#如何创建模板URPShader" class="headerlink" title="如何创建模板URPShader"></a>如何创建模板URPShader</h1><h2 id="方法一："><a href="#方法一：" class="headerlink" title="方法一："></a>方法一：</h2><p>&emsp;自定义编辑器代码，扩展右键功能或者自定义窗口，这里我附上以前做的URP创建模板编辑器插件，这个插件包含了创建hlsl和模板URPShader的编辑器代码，适合学习编辑器的一些运用，或者是不想对引擎内部做修改的，可以使用它。<br>提取码: 8hmb: &lt;<a href="https://pan.baidu.com/s/1X_ANVLDkK9Nf5XoLt-8CVg?pwd=8hmb">https://pan.baidu.com/s/1X_ANVLDkK9Nf5XoLt-8CVg?pwd=8hmb</a> &gt;</p><h2 id="方法二："><a href="#方法二：" class="headerlink" title="方法二："></a>方法二：</h2><p>&emsp;在编辑器引擎内部进行创建，具体地址如下：编辑器版本根文件夹-Editor-Data-Resources-ScriptTemplates<br><img src="/images/post/UnityURPTemplate/1.png"><br>&emsp;文件的命名格式未：数字-创建类型_创建时候的名字-成功创建重命名时的初始名字.文件后缀名.txt<br>&emsp;例如：<code>86-Shader__URP Shader-NewURPShader.shader.txt</code></p><ul><li>数字：数字代表在功能块中的排序，比如创建了一个86序号的，则它从小到大在对应的Create功能中排序，在下图中我们就可以看到URP Shader在_Image Effect Shader之后。<br><img src="/images/post/UnityURPTemplate/2.png"></li><li>创建类型：指代在Create目录下的是哪一个，Shader模板就写Shader就可以，如果你想创建一个脚本模板，那么就可以写C# Script。</li><li>创建时候的名字：指代在Create目录下的名字，如上图是URP Shader。</li><li>成功创建重命名时的初始名字：指代的是当我们点击后就会在Assets下创建模板资源的时候，会有一个重命名操作，重命名的时候不是有个初始名字么，这就是那个名字，如下图：<br><img src="/images/post/UnityURPTemplate/3.png"></li><li>文件后缀名指代的就是你的文件是什么格式的。</li></ul>]]></content>
      
      
      <categories>
          
          <category> Tool </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Tool </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>UnityURP-LitShader详解</title>
      <link href="/undefined/7fc566d1.html"/>
      <url>/undefined/7fc566d1.html</url>
      
        <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>&emsp;Unity URP Lit Shader是基于物理渲染的Shader，但Unity对其做了一些修改，不完全按照BRDF公式计算，很多都做了拟合曲线，这样就导致UnityURP的LitShader与实际的物理效果材质有所偏差。Unity这样做的目的是对其进行优化，以便能够在移动端高性能运行。</p><hr><h1 id="属性"><a href="#属性" class="headerlink" title="属性"></a>属性</h1><pre><code class="language-hlsl">Properties&#123;    // Specular vs Metallic workflow    _WorkflowMode(&quot;WorkflowMode&quot;, Float) = 1.0    [MainTexture] _BaseMap(&quot;Albedo&quot;, 2D) = &quot;white&quot; &#123;&#125;    [MainColor] _BaseColor(&quot;Color&quot;, Color) = (1,1,1,1)    _Cutoff(&quot;Alpha Cutoff&quot;, Range(0.0, 1.0)) = 0.5    _Smoothness(&quot;Smoothness&quot;, Range(0.0, 1.0)) = 0.5    _SmoothnessTextureChannel(&quot;Smoothness texture channel&quot;, Float) = 0    _Metallic(&quot;Metallic&quot;, Range(0.0, 1.0)) = 0.0    _MetallicGlossMap(&quot;Metallic&quot;, 2D) = &quot;white&quot; &#123;&#125;    _SpecColor(&quot;Specular&quot;, Color) = (0.2, 0.2, 0.2)    _SpecGlossMap(&quot;Specular&quot;, 2D) = &quot;white&quot; &#123;&#125;    [ToggleOff] _SpecularHighlights(&quot;Specular Highlights&quot;, Float) = 1.0    [ToggleOff] _EnvironmentReflections(&quot;Environment Reflections&quot;, Float) = 1.0    _BumpScale(&quot;Scale&quot;, Float) = 1.0    _BumpMap(&quot;Normal Map&quot;, 2D) = &quot;bump&quot; &#123;&#125;    _Parallax(&quot;Scale&quot;, Range(0.005, 0.08)) = 0.005    _ParallaxMap(&quot;Height Map&quot;, 2D) = &quot;black&quot; &#123;&#125;    _OcclusionStrength(&quot;Strength&quot;, Range(0.0, 1.0)) = 1.0    _OcclusionMap(&quot;Occlusion&quot;, 2D) = &quot;white&quot; &#123;&#125;    [HDR] _EmissionColor(&quot;Color&quot;, Color) = (0,0,0)    _EmissionMap(&quot;Emission&quot;, 2D) = &quot;white&quot; &#123;&#125;    _DetailMask(&quot;Detail Mask&quot;, 2D) = &quot;white&quot; &#123;&#125;    _DetailAlbedoMapScale(&quot;Scale&quot;, Range(0.0, 2.0)) = 1.0    _DetailAlbedoMap(&quot;Detail Albedo x2&quot;, 2D) = &quot;linearGrey&quot; &#123;&#125;    _DetailNormalMapScale(&quot;Scale&quot;, Range(0.0, 2.0)) = 1.0    [Normal] _DetailNormalMap(&quot;Normal Map&quot;, 2D) = &quot;bump&quot; &#123;&#125;    // SRP batching compatibility for Clear Coat (Not used in Lit)    [HideInInspector] _ClearCoatMask(&quot;_ClearCoatMask&quot;, Float) = 0.0    [HideInInspector] _ClearCoatSmoothness(&quot;_ClearCoatSmoothness&quot;, Float) = 0.0    // Blending state    _Surface(&quot;__surface&quot;, Float) = 0.0    _Blend(&quot;__blend&quot;, Float) = 0.0    _Cull(&quot;__cull&quot;, Float) = 2.0    [ToggleUI] _AlphaClip(&quot;__clip&quot;, Float) = 0.0    [HideInInspector] _SrcBlend(&quot;__src&quot;, Float) = 1.0    [HideInInspector] _DstBlend(&quot;__dst&quot;, Float) = 0.0    [HideInInspector] _SrcBlendAlpha(&quot;__srcA&quot;, Float) = 1.0    [HideInInspector] _DstBlendAlpha(&quot;__dstA&quot;, Float) = 0.0    [HideInInspector] _ZWrite(&quot;__zw&quot;, Float) = 1.0    [HideInInspector] _BlendModePreserveSpecular(&quot;_BlendModePreserveSpecular&quot;, Float) = 1.0    [HideInInspector] _AlphaToMask(&quot;__alphaToMask&quot;, Float) = 0.0    [ToggleUI] _ReceiveShadows(&quot;Receive Shadows&quot;, Float) = 1.0    // Editmode props    _QueueOffset(&quot;Queue offset&quot;, Float) = 0.0    // ObsoleteProperties    [HideInInspector] _MainTex(&quot;BaseMap&quot;, 2D) = &quot;white&quot; &#123;&#125;    [HideInInspector] _Color(&quot;Base Color&quot;, Color) = (1, 1, 1, 1)    [HideInInspector] _GlossMapScale(&quot;Smoothness&quot;, Float) = 0.0    [HideInInspector] _Glossiness(&quot;Smoothness&quot;, Float) = 0.0    [HideInInspector] _GlossyReflections(&quot;EnvironmentReflections&quot;, Float) = 0.0    [HideInInspector][NoScaleOffset]unity_Lightmaps(&quot;unity_Lightmaps&quot;, 2DArray) = &quot;&quot; &#123;&#125;    [HideInInspector][NoScaleOffset]unity_LightmapsInd(&quot;unity_LightmapsInd&quot;, 2DArray) = &quot;&quot; &#123;&#125;    [HideInInspector][NoScaleOffset]unity_ShadowMasks(&quot;unity_ShadowMasks&quot;, 2DArray) = &quot;&quot; &#123;&#125;&#125;</code></pre><ul><li><p><strong>_WorkflowMode</strong>：工作流模式，关联关键字 <strong>_SPECULAR_SETUP</strong>。</p><ul><li>类型范围：float类型，数值为0 或 1。</li><li>数值含义：<ul><li>0：使用高光工作流，脚本开启关键字 <strong>_SPECULAR_SETUP</strong>。</li><li>1：使用金属工作流，脚本关闭关键字 <strong>_SPECULAR_SETUP</strong>。</li></ul></li></ul></li><li><p><strong>_BaseMap</strong>：主帖图。</p><ul><li>类型：2D，默认白色贴图。</li><li>数值含义：是我们经常使用的albedo贴图，代码物体的反照率颜色，通俗讲就是物体本身的基础色。</li></ul></li><li><p><strong>_BaseColor</strong>：主颜色。</p><ul><li>类型：Color，默认白色。</li><li>数值含义：用于调节反照率，一般与采样 <strong>_BaseMap</strong> 后的颜色进行相乘获取最终的反照率。</li></ul></li><li><p><strong>_Cutoff</strong>：用于透明度裁剪。</p><ul><li>数值范围：float 类型，范围0~1。</li><li>数值含义：在需要AlphaTest中可以作为参数使用，因为不透明物体想要做出镂空或者溶解这种效果的话，只能使用透明裁剪。</li></ul></li><li><p><strong>_Smoothness</strong>：光滑度调节值。</p><ul><li>类型范围：float类型，单位0~1。</li><li>数值含义：控制光滑度的强度。</li></ul></li><li><p><strong>_SmoothnessTextureChannel</strong>：光滑度数值存放在哪张贴图的通道中，关联的Shader关键字 <strong>_SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A</strong>。</p><ul><li><p>类型范围：float类型，数值为0 或 1。</p></li><li><p>数值含义：</p><ul><li>0：贴图的光滑度存在于金属度贴图或者高光贴图的Alpha通道中，脚本关闭关键字 <strong>_SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A</strong>。</li><li>1：贴图的光滑度存在于 <strong>_BaseMap</strong> 贴图的Alpha通道中，脚本开启关键字 <strong>_SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A</strong>。</li></ul></li><li><details><summary> <font color="#007ACC">🐍 _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A关键字开启与关闭的具体代码 </font></summary><pre><code class="language-c#">if (material.HasProperty(&quot;_SmoothnessTextureChannel&quot;))&#123;    var opaque = IsOpaque(material);    CoreUtils.SetKeyword(material, &quot;_SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A&quot;,        GetSmoothnessMapChannel(material) == SmoothnessMapChannel.AlbedoAlpha &amp;&amp; opaque);&#125;</code></pre><pre><code class="language-c#">/// &lt;summary&gt;/// Retrieves the alpha channel used for smoothness./// &lt;/summary&gt;/// &lt;param name=&quot;material&quot;&gt;&lt;/param&gt;/// &lt;returns&gt;The Alpha channel used for Smoothness.&lt;/returns&gt;public static SmoothnessMapChannel GetSmoothnessMapChannel(Material material)&#123;    int ch = (int)material.GetFloat(&quot;_SmoothnessTextureChannel&quot;);    if (ch == (int)SmoothnessMapChannel.AlbedoAlpha)        return SmoothnessMapChannel.AlbedoAlpha;    return SmoothnessMapChannel.SpecularMetallicAlpha;&#125;</code></pre><pre><code class="language-c#">/// &lt;summary&gt;/// Options to select the texture channel where the smoothness value is stored./// &lt;/summary&gt;public enum SmoothnessMapChannel&#123;    /// &lt;summary&gt;    /// Use this when smoothness is stored in the alpha channel of the Specular/Metallic Map.    /// &lt;/summary&gt;    SpecularMetallicAlpha,    /// &lt;summary&gt;    /// Use this when smoothness is stored in the alpha channel of the Albedo Map.    /// &lt;/summary&gt;    AlbedoAlpha,&#125;</code></pre></details></li><li><details><summary> <font color="#007ACC">🐍 _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A关键字在Shader中的具体运用 </font></summary><pre><code class="language-hlsl">half4 SampleMetallicSpecGloss(float2 uv, half albedoAlpha)&#123;    half4 specGloss;#ifdef _METALLICSPECGLOSSMAP    specGloss = half4(SAMPLE_METALLICSPECULAR(uv));    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a *= _Smoothness;    #endif#else // _METALLICSPECGLOSSMAP    #if _SPECULAR_SETUP        specGloss.rgb = _SpecColor.rgb;    #else        specGloss.rgb = _Metallic.rrr;    #endif    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a = _Smoothness;    #endif#endif    return specGloss;&#125;</code></pre><blockquote><p>可以看到 <strong>specGloss.a</strong> 存放的就是光滑度的值，要么采样Albedo贴图的Alpha通道，要么就是采样金属或者高光贴图的Alpha通道，这只是Unity URP LitShader存放光滑度的一种方式而已，如果是自定义代码，也不是必须按照这样方式存储。</p></blockquote></details></li></ul></li><li><p><strong>_Metallic</strong>：金属度调节值。</p><ul><li>类型范围：float类型，单位0~1。</li><li>数值含义：控制金属度的强度。</li></ul></li><li><p><strong>_MetallicGlossMap</strong>：金属度调节值。</p><ul><li><p>类型：2D，默认白色贴图。</p></li><li><p>数值含义：金属度光泽贴图，只有存在贴图并且启用金属工作流的时候才会存在颜色。</p></li><li><details><summary> <font color="#007ACC">🐍 相关Shader功能代码 </font></summary><pre><code class="language-hlsl">#ifdef _SPECULAR_SETUP    #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_SpecGlossMap, sampler_SpecGlossMap, uv)#else    #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_MetallicGlossMap, sampler_MetallicGlossMap, uv)#endifhalf4 SampleMetallicSpecGloss(float2 uv, half albedoAlpha)&#123;    half4 specGloss;#ifdef _METALLICSPECGLOSSMAP    specGloss = half4(SAMPLE_METALLICSPECULAR(uv));    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a *= _Smoothness;    #endif#else // _METALLICSPECGLOSSMAP    #if _SPECULAR_SETUP        specGloss.rgb = _SpecColor.rgb;    #else        specGloss.rgb = _Metallic.rrr;    #endif    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a = _Smoothness;    #endif#endif    return specGloss;&#125;</code></pre><blockquote><p>可以看到只有是金属工作流的时候，<strong>SAMPLE_METALLICSPECULAR</strong> 函数是采样的 <strong>_MetallicGlossMap</strong> 贴图，同时要使用贴图颜色，要开启关键字 <strong>_METALLICSPECGLOSSMAP</strong> ，这个关键字就是在编辑器代码中开启的。</p></blockquote></details></li><li><details><summary> <font color="#007ACC">🐍 _METALLICSPECGLOSSMAP关键字开启与关闭代码 </font></summary><pre><code class="language-c#">var specularGlossMap = isSpecularWorkFlow ? &quot;_SpecGlossMap&quot; : &quot;_MetallicGlossMap&quot;;var hasGlossMap = material.GetTexture(specularGlossMap) != null;CoreUtils.SetKeyword(material, &quot;_METALLICSPECGLOSSMAP&quot;, hasGlossMap);</code></pre></details></li></ul></li><li><p><strong>_SpecColor</strong>：高光颜色。</p><ul><li>类型：Color，默认白色。</li><li>数值含义：决定高光反射的基础颜色。</li></ul></li><li><p><strong>_SpecGlossMap</strong>：高光颜色。</p><ul><li>类型：2D，默认白色贴图。</li><li>数值含义：决定高光反射的基础颜色。</li><li><details><summary> <font color="#007ACC">🐍 _SpecColor,_SpecGlossMap,_Metallic,_MetallicGlossMap的一些关系计算 </font></summary><pre><code class="language-hlsl">#ifdef _SPECULAR_SETUP    #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_SpecGlossMap, sampler_SpecGlossMap, uv)#else    #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_MetallicGlossMap, sampler_MetallicGlossMap, uv)#endifhalf4 SampleMetallicSpecGloss(float2 uv, half albedoAlpha)&#123;    half4 specGloss;#ifdef _METALLICSPECGLOSSMAP    specGloss = half4(SAMPLE_METALLICSPECULAR(uv));    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a *= _Smoothness;    #endif#else // _METALLICSPECGLOSSMAP    #if _SPECULAR_SETUP        specGloss.rgb = _SpecColor.rgb;    #else        specGloss.rgb = _Metallic.rrr;    #endif    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a = _Smoothness;    #endif#endif    return specGloss;&#125;</code></pre><blockquote><p>可以看到实际上这四个参数被分为了两类， <strong>_SpecColor</strong> 与 <strong>_SpecGlossMap</strong> 为一组，<strong>_Metallic</strong> 与 <strong>_MetallicGlossMap</strong> 为一组，这两组分别对应高光工作流与金属工作流。其中这两种工作流又分为有贴图和没有贴图时的数值为多少，有贴图的时候就采样贴图，没贴图的时候就设置对应的颜色，其中需要注意的是金属工作流没有贴图时高光颜色给的是 <strong>_Metallic</strong> 的灰度颜色。</p></blockquote></details></li></ul></li><li><p><strong>_SpecularHighlights</strong>: 高光开关，关联关键字 <strong>_SPECULARHIGHLIGHTS_OFF</strong>。</p><ul><li>类型范围：float，拥有[ToggleOff]标签，是个开关，数值为 0 或 1。</li><li>数值含义：决定高光是否关闭。<ul><li>0：关闭高光，脚本开启关键字 <strong>_SPECULARHIGHLIGHTS_OFF</strong>。</li><li>1：开启高光，脚本关闭关键字 <strong>_SPECULARHIGHLIGHTS_OFF</strong>。</li></ul></li><li><details><summary> <font color="#007ACC">🐍 编辑器脚本开启与关闭_SPECULARHIGHLIGHTS_OFF关键字代码 </font></summary><pre><code class="language-c#">if (material.HasProperty(&quot;_SpecularHighlights&quot;))CoreUtils.SetKeyword(material, &quot;_SPECULARHIGHLIGHTS_OFF&quot;,    material.GetFloat(&quot;_SpecularHighlights&quot;) == 0.0f);</code></pre></details></li><li><details><summary> <font color="#007ACC">🐍 Shader中有关_SpecularHighlights的代码 </font></summary>在 <strong>UniversalFragmentPBR</strong> 一开始就进行了 <strong>specularHighlightsOff</strong> 变量的声明：<br><img src="/images/post/URPLit/1.png"><br>定位可以看到这个变量用在了 <strong>LightingPhysicallyBased</strong> 函数中。<pre><code class="language-hlsl">half3 LightingPhysicallyBased(BRDFData brdfData, BRDFData brdfDataClearCoat,half3 lightColor, half3 lightDirectionWS, half lightAttenuation,half3 normalWS, half3 viewDirectionWS,half clearCoatMask, bool specularHighlightsOff)&#123;    half NdotL = saturate(dot(normalWS, lightDirectionWS));    half3 radiance = lightColor * (lightAttenuation * NdotL);    half3 brdf = brdfData.diffuse;#ifndef _SPECULARHIGHLIGHTS_OFF    [branch] if (!specularHighlightsOff)    &#123;        brdf += brdfData.specular * DirectBRDFSpecular(brdfData, normalWS, lightDirectionWS, viewDirectionWS);#if defined(_CLEARCOAT) || defined(_CLEARCOATMAP)        // Clear coat evaluates the specular a second timw and has some common terms with the base specular.        // We rely on the compiler to merge these and compute them only once.        half brdfCoat = kDielectricSpec.r * DirectBRDFSpecular(brdfDataClearCoat, normalWS, lightDirectionWS, viewDirectionWS);            // Mix clear coat and base layer using khronos glTF recommended formula            // https://github.com/KhronosGroup/glTF/blob/master/extensions/2.0/Khronos/KHR_materials_clearcoat/README.md            // Use NoV for direct too instead of LoH as an optimization (NoV is light invariant).            half NoV = saturate(dot(normalWS, viewDirectionWS));            // Use slightly simpler fresnelTerm (Pow4 vs Pow5) as a small optimization.            // It is matching fresnel used in the GI/Env, so should produce a consistent clear coat blend (env vs. direct)            half coatFresnel = kDielectricSpec.x + kDielectricSpec.a * Pow4(1.0 - NoV);        brdf = brdf * (1.0 - clearCoatMask * coatFresnel) + brdfCoat * clearCoatMask;#endif // _CLEARCOAT    &#125;#endif // _SPECULARHIGHLIGHTS_OFF    return brdf * radiance;&#125;</code></pre><blockquote><p>可以看到 <strong>LightingPhysicallyBased</strong> 中又判断了关键字 <strong>_SPECULARHIGHLIGHTS_OFF</strong> 的定义，然后还使用了布尔变量 <strong>specularHighlightsOff</strong>，简直是脱裤子放屁多此一举，不知道Unity为什么要这么做。</p></blockquote></details></li></ul></li><li><p><strong>_EnvironmentReflections</strong>：环境反射，关联关键字 <strong>_ENVIRONMENTREFLECTIONS_OFF</strong>。</p><ul><li><p>类型范围：float，拥有[ToggleOff]标签，是个开关，数值为 0 或 1。</p></li><li><p>数值含义：决定环境反射是否关闭。</p><ul><li>0：关闭环境反射，脚本开启关键字 <strong>_ENVIRONMENTREFLECTIONS_OFF</strong>。</li><li>1：开启环境反射，脚本关闭关键字 <strong>_ENVIRONMENTREFLECTIONS_OFF</strong>。</li></ul></li><li><details><summary> <font color="#007ACC">🐍 编辑器脚本开启与关闭_ENVIRONMENTREFLECTIONS_OFF关键字代码 </font></summary><pre><code class="language-c#">if (material.HasProperty(&quot;_EnvironmentReflections&quot;))    CoreUtils.SetKeyword(material, &quot;_ENVIRONMENTREFLECTIONS_OFF&quot;,        material.GetFloat(&quot;_EnvironmentReflections&quot;) == 0.0f);</code></pre></details>   </li><li><details><summary> <font color="#007ACC">🐍 Shader中有关_EnvironmentReflections的代码 </font></summary><pre><code class="language-hlsl">half3 GlossyEnvironmentReflection(half3 reflectVector, float3 positionWS, half perceptualRoughness, half occlusion, float2 normalizedScreenSpaceUV)&#123;#if !defined(_ENVIRONMENTREFLECTIONS_OFF)    half3 irradiance;#if defined(_REFLECTION_PROBE_BLENDING) || USE_FORWARD_PLUS    irradiance = CalculateIrradianceFromReflectionProbes(reflectVector, positionWS, perceptualRoughness, normalizedScreenSpaceUV);#else#ifdef _REFLECTION_PROBE_BOX_PROJECTION    reflectVector = BoxProjectedCubemapDirection(reflectVector, positionWS, unity_SpecCube0_ProbePosition, unity_SpecCube0_BoxMin, unity_SpecCube0_BoxMax);#endif // _REFLECTION_PROBE_BOX_PROJECTION    half mip = PerceptualRoughnessToMipmapLevel(perceptualRoughness);    half4 encodedIrradiance = half4(SAMPLE_TEXTURECUBE_LOD(unity_SpecCube0, samplerunity_SpecCube0, reflectVector, mip));    irradiance = DecodeHDREnvironment(encodedIrradiance, unity_SpecCube0_HDR);#endif // _REFLECTION_PROBE_BLENDING    return irradiance * occlusion;#else    return _GlossyEnvironmentColor.rgb * occlusion;#endif // _ENVIRONMENTREFLECTIONS_OFF&#125;#if !USE_FORWARD_PLUShalf3 GlossyEnvironmentReflection(half3 reflectVector, float3 positionWS, half perceptualRoughness, half occlusion)&#123;    return GlossyEnvironmentReflection(reflectVector, positionWS, perceptualRoughness, occlusion, float2(0.0f, 0.0f));&#125;#endifhalf3 GlossyEnvironmentReflection(half3 reflectVector, half perceptualRoughness, half occlusion)&#123;#if !defined(_ENVIRONMENTREFLECTIONS_OFF)    half3 irradiance;    half mip = PerceptualRoughnessToMipmapLevel(perceptualRoughness);    half4 encodedIrradiance = half4(SAMPLE_TEXTURECUBE_LOD(unity_SpecCube0, samplerunity_SpecCube0, reflectVector, mip));    irradiance = DecodeHDREnvironment(encodedIrradiance, unity_SpecCube0_HDR);    return irradiance * occlusion;#else    return _GlossyEnvironmentColor.rgb * occlusion;#endif // _ENVIRONMENTREFLECTIONS_OFF&#125;</code></pre><p>两个方法都很明显，是对环境高光反射的计算，而且它们在没有开启环境反射的情况下，都返回内置的环境高光颜色 <strong>_GlossyEnvironmentColor</strong>。而开启环境高光，他们的区别就在于第一个函数多了反射探针的计算。</p><ul><li><strong>USE_FORWARD_PLUS</strong> 是前向渲染预定义，它的定义代码如下：<pre><code class="language-hlsl">#if defined(_FORWARD_PLUS)#define _ADDITIONAL_LIGHTS 1#undef _ADDITIONAL_LIGHTS_VERTEX#define USE_FORWARD_PLUS 1#else#define USE_FORWARD_PLUS 0#endif</code></pre></li><li><strong>_REFLECTION_PROBE_BLENDING</strong>：反射探针混合关键字，作用是多个反射探针叠加。<ul><li><strong>CalculateIrradianceFromReflectionProbes</strong>：计算混合后的反射探针辐照度。</li></ul></li><li><strong>_REFLECTION_PROBE_BOX_PROJECTION</strong>：反射探针盒型投影关键字，作用是但反射探针采样。<ul><li><strong>BoxProjectedCubemapDirection</strong>：修正反射方向向量，让反射探针的采样结果贴合场景中的 “盒子” 范围，避免反射探针在盒子外采样错误，提升反射的空间准确性。</li><li><strong>unity_SpecCube0_ProbePosition</strong>：第一个反射探针的世界空间位置。</li><li><strong>unity_SpecCube0_BoxMin&#x2F;BoxMax</strong>：反射探针盒式投影的最小 &#x2F; 最大边界。</li></ul></li><li><strong>PerceptualRoughnessToMipmapLevel</strong>：将感知粗糙度转换为立方体贴图的 Mip 层级，粗糙度越高，Mip 层级越大，采样越模糊的层级，符合物理规律。</li><li><strong>SAMPLE_TEXTURECUBE_LOD</strong>： 采样立方体贴图（反射探针的纹理 <strong>unity_SpecCube0</strong> ），指定 Mip 层级 <strong>mip</strong>，得到编码后的 HDR 反射颜色。</li><li><strong>DecodeHDREnvironment</strong>：解码 HDR 格式的环境色（Unity 反射探针默认用 RGBM 等格式压缩 HDR 数据），<strong>unity_SpecCube0_HDR</strong> 是解码所需的 HDR 参数。</li></ul></details></li></ul></li><li><p><strong>_BumpScale</strong>：法线缩放。</p><ul><li>类型范围：float类型，默认1。</li><li>数值含义：控制法线的强度。</li></ul></li><li><p><strong>_BumpMap</strong>：法线。</p><ul><li>类型：2D，默认法线贴图。</li><li>数值含义：给予法线。</li></ul></li><li><p><strong>_Parallax</strong>：视差缩放。</p><ul><li>类型：float类型，范围0.005~0.08，默认0.005。</li><li>数值含义：控制视差强度。</li></ul></li><li><p><strong>_ParallaxMap</strong>：视差图。</p><ul><li>类型：2D，默认黑色贴图。</li><li>数值含义：给予视差高度数据。</li></ul></li><li><p><strong>_OcclusionStrength</strong>：遮蔽强度。</p><ul><li>类型：float类型，范围0~1，默认1。</li><li>数值含义：控制遮蔽强度。</li></ul></li><li><p><strong>_OcclusionMap</strong>：遮蔽遮罩图。</p><ul><li>类型：2D，默认白色贴图。</li><li>数值含义：给予哪些地方有遮蔽数据。</li></ul></li><li><p><strong>_EmissionColor</strong>：自发光颜色。</p><ul><li>类型：Color，HDR颜色，默认白色。</li><li>数值含义：控制自发光的颜色。</li></ul></li><li><p><strong>_EmissionMap</strong>：自发光遮罩图。</p><ul><li>类型：2D，默认白色贴图。</li><li>数值含义：给予哪些地方有自发光数据。</li></ul></li><li><p><strong>_DetailMask</strong>：细节遮罩。</p><ul><li>类型：2D，默认白色贴图。</li><li>数值含义：给予哪些地方有细节颜色数据，遮罩图。</li></ul></li><li><p><strong>_DetailAlbedoMapScale</strong>：细节反照率贴图强度。</p><ul><li>类型：float类型，范围0~2，默认1。</li><li>数值含义：控制细节贴图反照率颜色的强度，0的时候就是不会显示细节贴图。</li></ul></li><li><p><strong>_DetailAlbedoMap</strong>：细节基础颜色图。</p><ul><li>类型：2D，默认线性灰贴图。</li><li>数值含义：给予细节贴图的反照率颜色数据。</li></ul></li><li><p><strong>_DetailNormalMapScale</strong>：细节贴图法线强度。</p><ul><li>类型：float类型，范围0~2，默认1。</li><li>数值含义：控制细节贴图法线的强度。</li></ul></li><li><p><strong>_DetailNormalMap</strong>：细节法线图。</p><ul><li>类型：2D，默认法线贴图。</li><li>数值含义：给予细节的法线数据。</li></ul></li><li><p>因为清漆在Lit中并未使用，所以 <strong>_ClearCoatMask</strong> 和 <strong>_ClearCoatSmoothness</strong> 不作解释，后续关于清晰的Shader代码也会被忽略不进行解释。</p></li><li><p><strong>_Surface</strong>: Shader表面类型。</p><ul><li>类型范围：float类型，值为 0 或 1，默认0。</li><li>数值含义：给予细节的法线数据。</li><li>0：不透明物体。</li><li>1：透明物体。</li></ul></li></ul><p>&emsp;剩下就是混合设置和深度设置这些了，就不再过多解释，剩余未解释属性：</p><pre><code class="language-hlsl">_Blend(&quot;__blend&quot;, Float) = 0.0_Cull(&quot;__cull&quot;, Float) = 2.0[ToggleUI] _AlphaClip(&quot;__clip&quot;, Float) = 0.0[HideInInspector] _SrcBlend(&quot;__src&quot;, Float) = 1.0[HideInInspector] _DstBlend(&quot;__dst&quot;, Float) = 0.0[HideInInspector] _SrcBlendAlpha(&quot;__srcA&quot;, Float) = 1.0[HideInInspector] _DstBlendAlpha(&quot;__dstA&quot;, Float) = 0.0[HideInInspector] _ZWrite(&quot;__zw&quot;, Float) = 1.0[HideInInspector] _BlendModePreserveSpecular(&quot;_BlendModePreserveSpecular&quot;, Float) = 1.0[HideInInspector] _AlphaToMask(&quot;__alphaToMask&quot;, Float) = 0.0</code></pre><p><strong>_ReceiveShadows</strong> 也不用多说，<strong>_QueueOffset</strong> 是存在编辑模式下的参数，用于控制渲染队列的，使用的是编辑器代码，在Shader中并无什么操作。</p><p>&emsp;下面的是过时参数，用于兼容的：</p><pre><code class="language-hlsl">// ObsoleteProperties[HideInInspector] _MainTex(&quot;BaseMap&quot;, 2D) = &quot;white&quot; &#123;&#125;[HideInInspector] _Color(&quot;Base Color&quot;, Color) = (1, 1, 1, 1)[HideInInspector] _GlossMapScale(&quot;Smoothness&quot;, Float) = 0.0[HideInInspector] _Glossiness(&quot;Smoothness&quot;, Float) = 0.0[HideInInspector] _GlossyReflections(&quot;EnvironmentReflections&quot;, Float) = 0.0[HideInInspector][NoScaleOffset]unity_Lightmaps(&quot;unity_Lightmaps&quot;, 2DArray) = &quot;&quot; &#123;&#125;[HideInInspector][NoScaleOffset]unity_LightmapsInd(&quot;unity_LightmapsInd&quot;, 2DArray) = &quot;&quot; &#123;&#125;[HideInInspector][NoScaleOffset]unity_ShadowMasks(&quot;unity_ShadowMasks&quot;, 2DArray) = &quot;&quot; &#123;&#125;</code></pre><details><summary> <font color="#007ACC">🐍 编辑器脚本中混合设置代码 </font></summary><pre><code class="language-c#">internal static void SetupMaterialBlendModeInternal(Material material, out int automaticRenderQueue)&#123;    if (material == null)        throw new ArgumentNullException(&quot;material&quot;);    bool alphaClip = false;    if (material.HasProperty(Property.AlphaClip))        alphaClip = material.GetFloat(Property.AlphaClip) &gt;= 0.5;    CoreUtils.SetKeyword(material, ShaderKeywordStrings._ALPHATEST_ON, alphaClip);    // default is to use the shader render queue    int renderQueue = material.shader.renderQueue;    material.SetOverrideTag(&quot;RenderType&quot;, &quot;&quot;);      // clear override tag    if (material.HasProperty(Property.SurfaceType))    &#123;        SurfaceType surfaceType = (SurfaceType)material.GetFloat(Property.SurfaceType);        bool zwrite = false;        CoreUtils.SetKeyword(material, ShaderKeywordStrings._SURFACE_TYPE_TRANSPARENT, surfaceType == SurfaceType.Transparent);        bool alphaToMask = false;        if (surfaceType == SurfaceType.Opaque)        &#123;            if (alphaClip)            &#123;                renderQueue = (int)RenderQueue.AlphaTest;                material.SetOverrideTag(&quot;RenderType&quot;, &quot;TransparentCutout&quot;);                alphaToMask = true;            &#125;            else            &#123;                renderQueue = (int)RenderQueue.Geometry;                material.SetOverrideTag(&quot;RenderType&quot;, &quot;Opaque&quot;);            &#125;            SetMaterialSrcDstBlendProperties(material, UnityEngine.Rendering.BlendMode.One, UnityEngine.Rendering.BlendMode.Zero);            zwrite = true;            material.DisableKeyword(ShaderKeywordStrings._ALPHAPREMULTIPLY_ON);            material.DisableKeyword(ShaderKeywordStrings._ALPHAMODULATE_ON);        &#125;        else // SurfaceType Transparent        &#123;            BlendMode blendMode = (BlendMode)material.GetFloat(Property.BlendMode);            var srcBlendRGB = UnityEngine.Rendering.BlendMode.One;            var dstBlendRGB = UnityEngine.Rendering.BlendMode.OneMinusSrcAlpha;            var srcBlendA = UnityEngine.Rendering.BlendMode.One;            var dstBlendA = UnityEngine.Rendering.BlendMode.OneMinusSrcAlpha;            // Specific Transparent Mode Settings            switch (blendMode)            &#123;                // srcRGB * srcAlpha + dstRGB * (1 - srcAlpha)                // preserve spec:                // srcRGB * (&lt;in shader&gt; ? 1 : srcAlpha) + dstRGB * (1 - srcAlpha)                case BlendMode.Alpha:                    srcBlendRGB = UnityEngine.Rendering.BlendMode.SrcAlpha;                    dstBlendRGB = UnityEngine.Rendering.BlendMode.OneMinusSrcAlpha;                    srcBlendA = UnityEngine.Rendering.BlendMode.One;                    dstBlendA = dstBlendRGB;                    break;                // srcRGB &lt; srcAlpha, (alpha multiplied in asset)                // srcRGB * 1 + dstRGB * (1 - srcAlpha)                case BlendMode.Premultiply:                    srcBlendRGB = UnityEngine.Rendering.BlendMode.One;                    dstBlendRGB = UnityEngine.Rendering.BlendMode.OneMinusSrcAlpha;                    srcBlendA = srcBlendRGB;                    dstBlendA = dstBlendRGB;                    break;                // srcRGB * srcAlpha + dstRGB * 1, (alpha controls amount of addition)                // preserve spec:                // srcRGB * (&lt;in shader&gt; ? 1 : srcAlpha) + dstRGB * (1 - srcAlpha)                case BlendMode.Additive:                    srcBlendRGB = UnityEngine.Rendering.BlendMode.SrcAlpha;                    dstBlendRGB = UnityEngine.Rendering.BlendMode.One;                    srcBlendA = UnityEngine.Rendering.BlendMode.One;                    dstBlendA = dstBlendRGB;                    break;                // srcRGB * 0 + dstRGB * srcRGB                // in shader alpha controls amount of multiplication, lerp(1, srcRGB, srcAlpha)                // Multiply affects color only, keep existing alpha.                case BlendMode.Multiply:                    srcBlendRGB = UnityEngine.Rendering.BlendMode.DstColor;                    dstBlendRGB = UnityEngine.Rendering.BlendMode.Zero;                    srcBlendA = UnityEngine.Rendering.BlendMode.Zero;                    dstBlendA = UnityEngine.Rendering.BlendMode.One;                    break;            &#125;            // Lift alpha multiply from ROP to shader by setting pre-multiplied _SrcBlend mode.            // The intent is to do different blending for diffuse and specular in shader.            // ref: http://advances.realtimerendering.com/other/2016/naughty_dog/NaughtyDog_TechArt_Final.pdf            bool preserveSpecular = (material.HasProperty(Property.BlendModePreserveSpecular) &amp;&amp;                                        material.GetFloat(Property.BlendModePreserveSpecular) &gt; 0) &amp;&amp;                                    blendMode != BlendMode.Multiply &amp;&amp; blendMode != BlendMode.Premultiply;            if (preserveSpecular)            &#123;                srcBlendRGB = UnityEngine.Rendering.BlendMode.One;            &#125;            // When doing off-screen transparency accumulation, we change blend factors as described here: https://developer.nvidia.com/gpugems/GPUGems3/gpugems3_ch23.html            bool offScreenAccumulateAlpha = false;            if (offScreenAccumulateAlpha)                srcBlendA = UnityEngine.Rendering.BlendMode.Zero;            SetMaterialSrcDstBlendProperties(material, srcBlendRGB, dstBlendRGB, // RGB                srcBlendA, dstBlendA); // Alpha            CoreUtils.SetKeyword(material, ShaderKeywordStrings._ALPHAPREMULTIPLY_ON, preserveSpecular);            CoreUtils.SetKeyword(material, ShaderKeywordStrings._ALPHAMODULATE_ON, blendMode == BlendMode.Multiply);            // General Transparent Material Settings            material.SetOverrideTag(&quot;RenderType&quot;, &quot;Transparent&quot;);            zwrite = false;            renderQueue = (int)RenderQueue.Transparent;        &#125;        if (material.HasProperty(Property.AlphaToMask))        &#123;            material.SetFloat(Property.AlphaToMask, alphaToMask ? 1.0f : 0.0f);        &#125;        // check for override enum        if (material.HasProperty(Property.ZWriteControl))        &#123;            var zwriteControl = (UnityEditor.Rendering.Universal.ShaderGraph.ZWriteControl)material.GetFloat(Property.ZWriteControl);            if (zwriteControl == UnityEditor.Rendering.Universal.ShaderGraph.ZWriteControl.ForceEnabled)                zwrite = true;            else if (zwriteControl == UnityEditor.Rendering.Universal.ShaderGraph.ZWriteControl.ForceDisabled)                zwrite = false;        &#125;        SetMaterialZWriteProperty(material, zwrite);        material.SetShaderPassEnabled(&quot;DepthOnly&quot;, zwrite);    &#125;    else    &#123;        // no surface type property -- must be hard-coded by the shadergraph,        // so ensure the pass is enabled at the material level        material.SetShaderPassEnabled(&quot;DepthOnly&quot;, true);    &#125;    // must always apply queue offset, even if not set to material control    if (material.HasProperty(Property.QueueOffset))        renderQueue += (int)material.GetFloat(Property.QueueOffset);    automaticRenderQueue = renderQueue;&#125;</code></pre></details> <hr><h1 id="着色器声明"><a href="#着色器声明" class="headerlink" title="着色器声明"></a>着色器声明</h1><p>&emsp;这是声明顶点着色器和片源着色器，具体的代码可以在 <strong>LitForwardPass.hlsl</strong> 文件中找到，之后会具体详解。</p><pre><code class="language-hlsl">#pragma vertex LitPassVertex#pragma fragment LitPassFragment</code></pre><hr><h1 id="关键字声明"><a href="#关键字声明" class="headerlink" title="关键字声明"></a>关键字声明</h1><pre><code class="language-hlsl">// Material Keywords#pragma shader_feature_local _NORMALMAP     //法线图关键字#pragma shader_feature_local _PARALLAXMAP   //视差高度图关键字#pragma shader_feature_local _RECEIVE_SHADOWS_OFF   //关闭接收阴影关键字#pragma shader_feature_local _ _DETAIL_MULX2 _DETAIL_SCALED //细节贴图缩放关键字，一个是2被缩放，一个是自定义缩放#pragma shader_feature_local_fragment _SURFACE_TYPE_TRANSPARENT //表面类型是透明物体的关键字#pragma shader_feature_local_fragment _ALPHATEST_ON     //AlphTest开启关键字，用于透明度裁剪#pragma shader_feature_local_fragment _ _ALPHAPREMULTIPLY_ON _ALPHAMODULATE_ON      //alpha混合颜色关键字#pragma shader_feature_local_fragment _EMISSION     //自发光#pragma shader_feature_local_fragment _METALLICSPECGLOSSMAP     //金属和高光定义贴图关键字#pragma shader_feature_local_fragment _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A      //光滑度数据是否存放在Albedo贴图的A通道关键字#pragma shader_feature_local_fragment _OCCLUSIONMAP     //遮蔽贴图#pragma shader_feature_local_fragment _SPECULARHIGHLIGHTS_OFF   //高光关闭关键字#pragma shader_feature_local_fragment _ENVIRONMENTREFLECTIONS_OFF   //环境反射关闭关键字#pragma shader_feature_local_fragment _SPECULAR_SETUP   //高光工作流关键字// -------------------------------------// Universal Pipeline keywords#pragma multi_compile _ _MAIN_LIGHT_SHADOWS _MAIN_LIGHT_SHADOWS_CASCADE _MAIN_LIGHT_SHADOWS_SCREEN  //主光阴影关键字#pragma multi_compile _ _ADDITIONAL_LIGHTS_VERTEX _ADDITIONAL_LIGHTS    //额外光关键字#pragma multi_compile _ EVALUATE_SH_MIXED EVALUATE_SH_VERTEX    //计算球谐光照关键字#pragma multi_compile_fragment _ _ADDITIONAL_LIGHT_SHADOWS  //额外光阴影关键字#pragma multi_compile_fragment _ _REFLECTION_PROBE_BLENDING     //反射探针混合关键字，多个反射探针叠加效果#pragma multi_compile_fragment _ _REFLECTION_PROBE_BOX_PROJECTION   //反射探针盒型投影关键字，单个反射探针采样#pragma multi_compile_fragment _ _SHADOWS_SOFT _SHADOWS_SOFT_LOW _SHADOWS_SOFT_MEDIUM _SHADOWS_SOFT_HIGH    //阴影质量关键字#pragma multi_compile_fragment _ _SCREEN_SPACE_OCCLUSION    //屏幕AO关键字#pragma multi_compile_fragment _ _DBUFFER_MRT1 _DBUFFER_MRT2 _DBUFFER_MRT3  //DBuffer关键字，贴花使用#pragma multi_compile_fragment _ _LIGHT_COOKIES     //LightCookie关键字#pragma multi_compile _ _LIGHT_LAYERS       //LightLayer启用关键字#pragma multi_compile _ _FORWARD_PLUS   //前向渲染关键字// Unity defined keywords#pragma multi_compile _ LIGHTMAP_SHADOW_MIXING      //LightMap阴影混合关键字#pragma multi_compile _ SHADOWS_SHADOWMASK  //ShadowMask关键字#pragma multi_compile _ DIRLIGHTMAP_COMBINED    //动态LightMap组合关键字#pragma multi_compile _ LIGHTMAP_ON     //开启LightMap关键字#pragma multi_compile _ DYNAMICLIGHTMAP_ON      //开启动态LightMap关键字#pragma multi_compile_fragment _ LOD_FADE_CROSSFADE     //LOD关键字#pragma multi_compile_fog   //雾效关键字#pragma multi_compile_fragment _ DEBUG_DISPLAY  //Debug显示关键字//--------------------------------------// GPU Instancing#pragma multi_compile_instancing    //GPU实例化关键字#pragma instancing_options renderinglayer   //GPU实例化保存renderinglayerMask数据（使得每个实例可以拥有独立的渲染层配置，而非所有实例共享同一个渲染层）</code></pre><hr><h1 id="引用Shader库"><a href="#引用Shader库" class="headerlink" title="引用Shader库"></a>引用Shader库</h1><pre><code class="language-hlsl">#include_with_pragmas &quot;Packages/com.unity.render-pipelines.core/ShaderLibrary/FoveatedRenderingKeywords.hlsl&quot;#include_with_pragmas &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/RenderingLayers.hlsl&quot;#include_with_pragmas &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/DOTS.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.universal/Shaders/LitInput.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.universal/Shaders/LitForwardPass.hlsl&quot;</code></pre><hr><h1 id="LitInput-hlsl"><a href="#LitInput-hlsl" class="headerlink" title="LitInput.hlsl"></a>LitInput.hlsl</h1><pre><code class="language-hlsl">#ifndef UNIVERSAL_LIT_INPUT_INCLUDED#define UNIVERSAL_LIT_INPUT_INCLUDED#include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/Core.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.core/ShaderLibrary/CommonMaterial.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/SurfaceInput.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.core/ShaderLibrary/ParallaxMapping.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/DBuffer.hlsl&quot;#if defined(_DETAIL_MULX2) || defined(_DETAIL_SCALED)#define _DETAIL#endif// NOTE: Do not ifdef the properties here as SRP batcher can not handle different layouts.CBUFFER_START(UnityPerMaterial)float4 _BaseMap_ST;float4 _DetailAlbedoMap_ST;half4 _BaseColor;half4 _SpecColor;half4 _EmissionColor;half _Cutoff;half _Smoothness;half _Metallic;half _BumpScale;half _Parallax;half _OcclusionStrength;half _ClearCoatMask;half _ClearCoatSmoothness;half _DetailAlbedoMapScale;half _DetailNormalMapScale;half _Surface;CBUFFER_END// NOTE: Do not ifdef the properties for dots instancing, but ifdef the actual usage.// Otherwise you might break CPU-side as property constant-buffer offsets change per variant.// NOTE: Dots instancing is orthogonal to the constant buffer above.#ifdef UNITY_DOTS_INSTANCING_ENABLEDUNITY_DOTS_INSTANCING_START(MaterialPropertyMetadata)    UNITY_DOTS_INSTANCED_PROP(float4, _BaseColor)    UNITY_DOTS_INSTANCED_PROP(float4, _SpecColor)    UNITY_DOTS_INSTANCED_PROP(float4, _EmissionColor)    UNITY_DOTS_INSTANCED_PROP(float , _Cutoff)    UNITY_DOTS_INSTANCED_PROP(float , _Smoothness)    UNITY_DOTS_INSTANCED_PROP(float , _Metallic)    UNITY_DOTS_INSTANCED_PROP(float , _BumpScale)    UNITY_DOTS_INSTANCED_PROP(float , _Parallax)    UNITY_DOTS_INSTANCED_PROP(float , _OcclusionStrength)    UNITY_DOTS_INSTANCED_PROP(float , _ClearCoatMask)    UNITY_DOTS_INSTANCED_PROP(float , _ClearCoatSmoothness)    UNITY_DOTS_INSTANCED_PROP(float , _DetailAlbedoMapScale)    UNITY_DOTS_INSTANCED_PROP(float , _DetailNormalMapScale)    UNITY_DOTS_INSTANCED_PROP(float , _Surface)UNITY_DOTS_INSTANCING_END(MaterialPropertyMetadata)// Here, we want to avoid overriding a property like e.g. _BaseColor with something like this:// #define _BaseColor UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _BaseColor0)//// It would be simpler, but it can cause the compiler to regenerate the property loading code for each use of _BaseColor.//// To avoid this, the property loads are cached in some static values at the beginning of the shader.// The properties such as _BaseColor are then overridden so that it expand directly to the static value like this:// #define _BaseColor unity_DOTS_Sampled_BaseColor//// This simple fix happened to improve GPU performances by ~10% on Meta Quest 2 with URP on some scenes.static float4 unity_DOTS_Sampled_BaseColor;static float4 unity_DOTS_Sampled_SpecColor;static float4 unity_DOTS_Sampled_EmissionColor;static float  unity_DOTS_Sampled_Cutoff;static float  unity_DOTS_Sampled_Smoothness;static float  unity_DOTS_Sampled_Metallic;static float  unity_DOTS_Sampled_BumpScale;static float  unity_DOTS_Sampled_Parallax;static float  unity_DOTS_Sampled_OcclusionStrength;static float  unity_DOTS_Sampled_ClearCoatMask;static float  unity_DOTS_Sampled_ClearCoatSmoothness;static float  unity_DOTS_Sampled_DetailAlbedoMapScale;static float  unity_DOTS_Sampled_DetailNormalMapScale;static float  unity_DOTS_Sampled_Surface;void SetupDOTSLitMaterialPropertyCaches()&#123;    unity_DOTS_Sampled_BaseColor            = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _BaseColor);    unity_DOTS_Sampled_SpecColor            = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _SpecColor);    unity_DOTS_Sampled_EmissionColor        = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _EmissionColor);    unity_DOTS_Sampled_Cutoff               = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Cutoff);    unity_DOTS_Sampled_Smoothness           = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Smoothness);    unity_DOTS_Sampled_Metallic             = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Metallic);    unity_DOTS_Sampled_BumpScale            = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _BumpScale);    unity_DOTS_Sampled_Parallax             = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Parallax);    unity_DOTS_Sampled_OcclusionStrength    = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _OcclusionStrength);    unity_DOTS_Sampled_ClearCoatMask        = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _ClearCoatMask);    unity_DOTS_Sampled_ClearCoatSmoothness  = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _ClearCoatSmoothness);    unity_DOTS_Sampled_DetailAlbedoMapScale = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _DetailAlbedoMapScale);    unity_DOTS_Sampled_DetailNormalMapScale = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _DetailNormalMapScale);    unity_DOTS_Sampled_Surface              = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Surface);&#125;#undef UNITY_SETUP_DOTS_MATERIAL_PROPERTY_CACHES#define UNITY_SETUP_DOTS_MATERIAL_PROPERTY_CACHES() SetupDOTSLitMaterialPropertyCaches()#define _BaseColor              unity_DOTS_Sampled_BaseColor#define _SpecColor              unity_DOTS_Sampled_SpecColor#define _EmissionColor          unity_DOTS_Sampled_EmissionColor#define _Cutoff                 unity_DOTS_Sampled_Cutoff#define _Smoothness             unity_DOTS_Sampled_Smoothness#define _Metallic               unity_DOTS_Sampled_Metallic#define _BumpScale              unity_DOTS_Sampled_BumpScale#define _Parallax               unity_DOTS_Sampled_Parallax#define _OcclusionStrength      unity_DOTS_Sampled_OcclusionStrength#define _ClearCoatMask          unity_DOTS_Sampled_ClearCoatMask#define _ClearCoatSmoothness    unity_DOTS_Sampled_ClearCoatSmoothness#define _DetailAlbedoMapScale   unity_DOTS_Sampled_DetailAlbedoMapScale#define _DetailNormalMapScale   unity_DOTS_Sampled_DetailNormalMapScale#define _Surface                unity_DOTS_Sampled_Surface#endifTEXTURE2D(_ParallaxMap);        SAMPLER(sampler_ParallaxMap);TEXTURE2D(_OcclusionMap);       SAMPLER(sampler_OcclusionMap);TEXTURE2D(_DetailMask);         SAMPLER(sampler_DetailMask);TEXTURE2D(_DetailAlbedoMap);    SAMPLER(sampler_DetailAlbedoMap);TEXTURE2D(_DetailNormalMap);    SAMPLER(sampler_DetailNormalMap);TEXTURE2D(_MetallicGlossMap);   SAMPLER(sampler_MetallicGlossMap);TEXTURE2D(_SpecGlossMap);       SAMPLER(sampler_SpecGlossMap);TEXTURE2D(_ClearCoatMap);       SAMPLER(sampler_ClearCoatMap);#ifdef _SPECULAR_SETUP    #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_SpecGlossMap, sampler_SpecGlossMap, uv)#else    #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_MetallicGlossMap, sampler_MetallicGlossMap, uv)#endifhalf4 SampleMetallicSpecGloss(float2 uv, half albedoAlpha)&#123;    half4 specGloss;#ifdef _METALLICSPECGLOSSMAP    specGloss = half4(SAMPLE_METALLICSPECULAR(uv));    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a *= _Smoothness;    #endif#else // _METALLICSPECGLOSSMAP    #if _SPECULAR_SETUP        specGloss.rgb = _SpecColor.rgb;    #else        specGloss.rgb = _Metallic.rrr;    #endif    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a = _Smoothness;    #endif#endif    return specGloss;&#125;half SampleOcclusion(float2 uv)&#123;    #ifdef _OCCLUSIONMAP        half occ = SAMPLE_TEXTURE2D(_OcclusionMap, sampler_OcclusionMap, uv).g;        return LerpWhiteTo(occ, _OcclusionStrength);    #else        return half(1.0);    #endif&#125;// Returns clear coat parameters// .x/.r == mask// .y/.g == smoothnesshalf2 SampleClearCoat(float2 uv)&#123;#if defined(_CLEARCOAT) || defined(_CLEARCOATMAP)    half2 clearCoatMaskSmoothness = half2(_ClearCoatMask, _ClearCoatSmoothness);#if defined(_CLEARCOATMAP)    clearCoatMaskSmoothness *= SAMPLE_TEXTURE2D(_ClearCoatMap, sampler_ClearCoatMap, uv).rg;#endif    return clearCoatMaskSmoothness;#else    return half2(0.0, 1.0);#endif  // _CLEARCOAT&#125;void ApplyPerPixelDisplacement(half3 viewDirTS, inout float2 uv)&#123;#if defined(_PARALLAXMAP)    uv += ParallaxMapping(TEXTURE2D_ARGS(_ParallaxMap, sampler_ParallaxMap), viewDirTS, _Parallax, uv);#endif&#125;// Used for scaling detail albedo. Main features:// - Depending if detailAlbedo brightens or darkens, scale magnifies effect.// - No effect is applied if detailAlbedo is 0.5.half3 ScaleDetailAlbedo(half3 detailAlbedo, half scale)&#123;    // detailAlbedo = detailAlbedo * 2.0h - 1.0h;    // detailAlbedo *= _DetailAlbedoMapScale;    // detailAlbedo = detailAlbedo * 0.5h + 0.5h;    // return detailAlbedo * 2.0f;    // A bit more optimized    return half(2.0) * detailAlbedo * scale - scale + half(1.0);&#125;half3 ApplyDetailAlbedo(float2 detailUv, half3 albedo, half detailMask)&#123;#if defined(_DETAIL)    half3 detailAlbedo = SAMPLE_TEXTURE2D(_DetailAlbedoMap, sampler_DetailAlbedoMap, detailUv).rgb;    // In order to have same performance as builtin, we do scaling only if scale is not 1.0 (Scaled version has 6 additional instructions)#if defined(_DETAIL_SCALED)    detailAlbedo = ScaleDetailAlbedo(detailAlbedo, _DetailAlbedoMapScale);#else    detailAlbedo = half(2.0) * detailAlbedo;#endif    return albedo * LerpWhiteTo(detailAlbedo, detailMask);#else    return albedo;#endif&#125;half3 ApplyDetailNormal(float2 detailUv, half3 normalTS, half detailMask)&#123;#if defined(_DETAIL)#if BUMP_SCALE_NOT_SUPPORTED    half3 detailNormalTS = UnpackNormal(SAMPLE_TEXTURE2D(_DetailNormalMap, sampler_DetailNormalMap, detailUv));#else    half3 detailNormalTS = UnpackNormalScale(SAMPLE_TEXTURE2D(_DetailNormalMap, sampler_DetailNormalMap, detailUv), _DetailNormalMapScale);#endif    // With UNITY_NO_DXT5nm unpacked vector is not normalized for BlendNormalRNM    // For visual consistancy we going to do in all cases    detailNormalTS = normalize(detailNormalTS);    return lerp(normalTS, BlendNormalRNM(normalTS, detailNormalTS), detailMask); // todo: detailMask should lerp the angle of the quaternion rotation, not the normals#else    return normalTS;#endif&#125;inline void InitializeStandardLitSurfaceData(float2 uv, out SurfaceData outSurfaceData)&#123;    half4 albedoAlpha = SampleAlbedoAlpha(uv, TEXTURE2D_ARGS(_BaseMap, sampler_BaseMap));    outSurfaceData.alpha = Alpha(albedoAlpha.a, _BaseColor, _Cutoff);    half4 specGloss = SampleMetallicSpecGloss(uv, albedoAlpha.a);    outSurfaceData.albedo = albedoAlpha.rgb * _BaseColor.rgb;    outSurfaceData.albedo = AlphaModulate(outSurfaceData.albedo, outSurfaceData.alpha);#if _SPECULAR_SETUP    outSurfaceData.metallic = half(1.0);    outSurfaceData.specular = specGloss.rgb;#else    outSurfaceData.metallic = specGloss.r;    outSurfaceData.specular = half3(0.0, 0.0, 0.0);#endif    outSurfaceData.smoothness = specGloss.a;    outSurfaceData.normalTS = SampleNormal(uv, TEXTURE2D_ARGS(_BumpMap, sampler_BumpMap), _BumpScale);    outSurfaceData.occlusion = SampleOcclusion(uv);    outSurfaceData.emission = SampleEmission(uv, _EmissionColor.rgb, TEXTURE2D_ARGS(_EmissionMap, sampler_EmissionMap));#if defined(_CLEARCOAT) || defined(_CLEARCOATMAP)    half2 clearCoat = SampleClearCoat(uv);    outSurfaceData.clearCoatMask       = clearCoat.r;    outSurfaceData.clearCoatSmoothness = clearCoat.g;#else    outSurfaceData.clearCoatMask       = half(0.0);    outSurfaceData.clearCoatSmoothness = half(0.0);#endif#if defined(_DETAIL)    half detailMask = SAMPLE_TEXTURE2D(_DetailMask, sampler_DetailMask, uv).a;    float2 detailUv = uv * _DetailAlbedoMap_ST.xy + _DetailAlbedoMap_ST.zw;    outSurfaceData.albedo = ApplyDetailAlbedo(detailUv, outSurfaceData.albedo, detailMask);    outSurfaceData.normalTS = ApplyDetailNormal(detailUv, outSurfaceData.normalTS, detailMask);#endif&#125;#endif // UNIVERSAL_INPUT_SURFACE_PBR_INCLUDED</code></pre><p>&emsp;浏览整个hlsl文件，<strong>LitInput.hlsl</strong> 文件就是声明了一些输入内容，现在开始详细解析。</p><h2 id="引用头文件"><a href="#引用头文件" class="headerlink" title="引用头文件"></a>引用头文件</h2><p>&emsp;一开始就先定义了要用到的文件，我们比较熟悉的就是 <strong>Core.hlsl</strong> 和 <strong>SurfaceInput.hlsl</strong> 了，其余文件比如 <strong>ParallaxMapping.hlsl</strong>，很显然是包含了视察映射的一些内容。</p><pre><code class="language-hlsl">#include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/Core.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.core/ShaderLibrary/CommonMaterial.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/SurfaceInput.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.core/ShaderLibrary/ParallaxMapping.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/DBuffer.hlsl&quot;</code></pre><h2 id="预定义细节标签"><a href="#预定义细节标签" class="headerlink" title="预定义细节标签"></a>预定义细节标签</h2><pre><code class="language-hlsl">#if defined(_DETAIL_MULX2) || defined(_DETAIL_SCALED)#define _DETAIL#endif</code></pre><p>&emsp;可以看到声明 <strong>_DETAIL_MULX2</strong> 或者 <strong>_DETAIL_SCALED</strong> 关键字都会定义 <strong>_DETAIL</strong>，而 <strong>_DETAIL</strong> 的作用如下：</p><pre><code class="language-hlsl">half3 ApplyDetailAlbedo(float2 detailUv, half3 albedo, half detailMask)&#123;#if defined(_DETAIL)    half3 detailAlbedo = SAMPLE_TEXTURE2D(_DetailAlbedoMap, sampler_DetailAlbedoMap, detailUv).rgb;    // In order to have same performance as builtin, we do scaling only if scale is not 1.0 (Scaled version has 6 additional instructions)#if defined(_DETAIL_SCALED)    detailAlbedo = ScaleDetailAlbedo(detailAlbedo, _DetailAlbedoMapScale);#else    detailAlbedo = half(2.0) * detailAlbedo;#endif    return albedo * LerpWhiteTo(detailAlbedo, detailMask);#else    return albedo;#endif&#125;half3 ApplyDetailNormal(float2 detailUv, half3 normalTS, half detailMask)&#123;#if defined(_DETAIL)#if BUMP_SCALE_NOT_SUPPORTED    half3 detailNormalTS = UnpackNormal(SAMPLE_TEXTURE2D(_DetailNormalMap, sampler_DetailNormalMap, detailUv));#else    half3 detailNormalTS = UnpackNormalScale(SAMPLE_TEXTURE2D(_DetailNormalMap, sampler_DetailNormalMap, detailUv), _DetailNormalMapScale);#endif    // With UNITY_NO_DXT5nm unpacked vector is not normalized for BlendNormalRNM    // For visual consistancy we going to do in all cases    detailNormalTS = normalize(detailNormalTS);    return lerp(normalTS, BlendNormalRNM(normalTS, detailNormalTS), detailMask); // todo: detailMask should lerp the angle of the quaternion rotation, not the normals#else    return normalTS;#endif&#125;</code></pre><p>[<img src="/images/post/URPLit/2.png">]</p><h2 id="声明变量"><a href="#声明变量" class="headerlink" title="声明变量"></a>声明变量</h2><pre><code class="language-hlsl">// NOTE: Do not ifdef the properties here as SRP batcher can not handle different layouts.CBUFFER_START(UnityPerMaterial)float4 _BaseMap_ST;float4 _DetailAlbedoMap_ST;half4 _BaseColor;half4 _SpecColor;half4 _EmissionColor;half _Cutoff;half _Smoothness;half _Metallic;half _BumpScale;half _Parallax;half _OcclusionStrength;half _ClearCoatMask;half _ClearCoatSmoothness;half _DetailAlbedoMapScale;half _DetailNormalMapScale;half _Surface;CBUFFER_END// NOTE: Do not ifdef the properties for dots instancing, but ifdef the actual usage.// Otherwise you might break CPU-side as property constant-buffer offsets change per variant.// NOTE: Dots instancing is orthogonal to the constant buffer above.#ifdef UNITY_DOTS_INSTANCING_ENABLEDUNITY_DOTS_INSTANCING_START(MaterialPropertyMetadata)    UNITY_DOTS_INSTANCED_PROP(float4, _BaseColor)    UNITY_DOTS_INSTANCED_PROP(float4, _SpecColor)    UNITY_DOTS_INSTANCED_PROP(float4, _EmissionColor)    UNITY_DOTS_INSTANCED_PROP(float , _Cutoff)    UNITY_DOTS_INSTANCED_PROP(float , _Smoothness)    UNITY_DOTS_INSTANCED_PROP(float , _Metallic)    UNITY_DOTS_INSTANCED_PROP(float , _BumpScale)    UNITY_DOTS_INSTANCED_PROP(float , _Parallax)    UNITY_DOTS_INSTANCED_PROP(float , _OcclusionStrength)    UNITY_DOTS_INSTANCED_PROP(float , _ClearCoatMask)    UNITY_DOTS_INSTANCED_PROP(float , _ClearCoatSmoothness)    UNITY_DOTS_INSTANCED_PROP(float , _DetailAlbedoMapScale)    UNITY_DOTS_INSTANCED_PROP(float , _DetailNormalMapScale)    UNITY_DOTS_INSTANCED_PROP(float , _Surface)UNITY_DOTS_INSTANCING_END(MaterialPropertyMetadata)// Here, we want to avoid overriding a property like e.g. _BaseColor with something like this:// #define _BaseColor UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _BaseColor0)//// It would be simpler, but it can cause the compiler to regenerate the property loading code for each use of _BaseColor.//// To avoid this, the property loads are cached in some static values at the beginning of the shader.// The properties such as _BaseColor are then overridden so that it expand directly to the static value like this:// #define _BaseColor unity_DOTS_Sampled_BaseColor//// This simple fix happened to improve GPU performances by ~10% on Meta Quest 2 with URP on some scenes.static float4 unity_DOTS_Sampled_BaseColor;static float4 unity_DOTS_Sampled_SpecColor;static float4 unity_DOTS_Sampled_EmissionColor;static float  unity_DOTS_Sampled_Cutoff;static float  unity_DOTS_Sampled_Smoothness;static float  unity_DOTS_Sampled_Metallic;static float  unity_DOTS_Sampled_BumpScale;static float  unity_DOTS_Sampled_Parallax;static float  unity_DOTS_Sampled_OcclusionStrength;static float  unity_DOTS_Sampled_ClearCoatMask;static float  unity_DOTS_Sampled_ClearCoatSmoothness;static float  unity_DOTS_Sampled_DetailAlbedoMapScale;static float  unity_DOTS_Sampled_DetailNormalMapScale;static float  unity_DOTS_Sampled_Surface;void SetupDOTSLitMaterialPropertyCaches()&#123;    unity_DOTS_Sampled_BaseColor            = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _BaseColor);    unity_DOTS_Sampled_SpecColor            = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _SpecColor);    unity_DOTS_Sampled_EmissionColor        = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _EmissionColor);    unity_DOTS_Sampled_Cutoff               = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Cutoff);    unity_DOTS_Sampled_Smoothness           = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Smoothness);    unity_DOTS_Sampled_Metallic             = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Metallic);    unity_DOTS_Sampled_BumpScale            = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _BumpScale);    unity_DOTS_Sampled_Parallax             = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Parallax);    unity_DOTS_Sampled_OcclusionStrength    = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _OcclusionStrength);    unity_DOTS_Sampled_ClearCoatMask        = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _ClearCoatMask);    unity_DOTS_Sampled_ClearCoatSmoothness  = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _ClearCoatSmoothness);    unity_DOTS_Sampled_DetailAlbedoMapScale = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _DetailAlbedoMapScale);    unity_DOTS_Sampled_DetailNormalMapScale = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _DetailNormalMapScale);    unity_DOTS_Sampled_Surface              = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Surface);&#125;#undef UNITY_SETUP_DOTS_MATERIAL_PROPERTY_CACHES#define UNITY_SETUP_DOTS_MATERIAL_PROPERTY_CACHES() SetupDOTSLitMaterialPropertyCaches()#define _BaseColor              unity_DOTS_Sampled_BaseColor#define _SpecColor              unity_DOTS_Sampled_SpecColor#define _EmissionColor          unity_DOTS_Sampled_EmissionColor#define _Cutoff                 unity_DOTS_Sampled_Cutoff#define _Smoothness             unity_DOTS_Sampled_Smoothness#define _Metallic               unity_DOTS_Sampled_Metallic#define _BumpScale              unity_DOTS_Sampled_BumpScale#define _Parallax               unity_DOTS_Sampled_Parallax#define _OcclusionStrength      unity_DOTS_Sampled_OcclusionStrength#define _ClearCoatMask          unity_DOTS_Sampled_ClearCoatMask#define _ClearCoatSmoothness    unity_DOTS_Sampled_ClearCoatSmoothness#define _DetailAlbedoMapScale   unity_DOTS_Sampled_DetailAlbedoMapScale#define _DetailNormalMapScale   unity_DOTS_Sampled_DetailNormalMapScale#define _Surface                unity_DOTS_Sampled_Surface#endifTEXTURE2D(_ParallaxMap);        SAMPLER(sampler_ParallaxMap);TEXTURE2D(_OcclusionMap);       SAMPLER(sampler_OcclusionMap);TEXTURE2D(_DetailMask);         SAMPLER(sampler_DetailMask);TEXTURE2D(_DetailAlbedoMap);    SAMPLER(sampler_DetailAlbedoMap);TEXTURE2D(_DetailNormalMap);    SAMPLER(sampler_DetailNormalMap);TEXTURE2D(_MetallicGlossMap);   SAMPLER(sampler_MetallicGlossMap);TEXTURE2D(_SpecGlossMap);       SAMPLER(sampler_SpecGlossMap);TEXTURE2D(_ClearCoatMap);       SAMPLER(sampler_ClearCoatMap);</code></pre><p>&emsp;别看这一大片代码很多，实际上就做了三件事：</p><ul><li><p>声明 <strong>CBUFFER_START</strong> 开启SRP合批功能。</p><ul><li><details><summary> <font color="#007ACC">🐍 SRP合批常量缓冲区定义代码 </font></summary><pre><code class="language-hlsl">    CBUFFER_START(UnityPerMaterial)    float4 _BaseMap_ST;    float4 _DetailAlbedoMap_ST;    half4 _BaseColor;    half4 _SpecColor;    half4 _EmissionColor;    half _Cutoff;    half _Smoothness;    half _Metallic;    half _BumpScale;    half _Parallax;    half _OcclusionStrength;    half _ClearCoatMask;    half _ClearCoatSmoothness;    half _DetailAlbedoMapScale;    half _DetailNormalMapScale;    half _Surface;    CBUFFER_END</code></pre></details></li></ul></li><li><p>声明GPU实例化使用的变量 <strong>UNITY_DOTS_INSTANCING_START</strong> 以便支持GPU实例化。</p><ul><li><details><summary> <font color="#007ACC">🐍 GPU实例化变量定义代码 </font></summary><pre><code class="language-hlsl">#ifdef UNITY_DOTS_INSTANCING_ENABLEDUNITY_DOTS_INSTANCING_START(MaterialPropertyMetadata)    UNITY_DOTS_INSTANCED_PROP(float4, _BaseColor)    UNITY_DOTS_INSTANCED_PROP(float4, _SpecColor)    UNITY_DOTS_INSTANCED_PROP(float4, _EmissionColor)    UNITY_DOTS_INSTANCED_PROP(float , _Cutoff)    UNITY_DOTS_INSTANCED_PROP(float , _Smoothness)    UNITY_DOTS_INSTANCED_PROP(float , _Metallic)    UNITY_DOTS_INSTANCED_PROP(float , _BumpScale)    UNITY_DOTS_INSTANCED_PROP(float , _Parallax)    UNITY_DOTS_INSTANCED_PROP(float , _OcclusionStrength)    UNITY_DOTS_INSTANCED_PROP(float , _ClearCoatMask)    UNITY_DOTS_INSTANCED_PROP(float , _ClearCoatSmoothness)    UNITY_DOTS_INSTANCED_PROP(float , _DetailAlbedoMapScale)    UNITY_DOTS_INSTANCED_PROP(float , _DetailNormalMapScale)    UNITY_DOTS_INSTANCED_PROP(float , _Surface)UNITY_DOTS_INSTANCING_END(MaterialPropertyMetadata)// Here, we want to avoid overriding a property like e.g. _BaseColor with something like this:// #define _BaseColor UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _BaseColor0)//// It would be simpler, but it can cause the compiler to regenerate the property loading code for each use of _BaseColor.//// To avoid this, the property loads are cached in some static values at the beginning of the shader.// The properties such as _BaseColor are then overridden so that it expand directly to the static value like this:// #define _BaseColor unity_DOTS_Sampled_BaseColor//// This simple fix happened to improve GPU performances by ~10% on Meta Quest 2 with URP on some scenes.static float4 unity_DOTS_Sampled_BaseColor;static float4 unity_DOTS_Sampled_SpecColor;static float4 unity_DOTS_Sampled_EmissionColor;static float  unity_DOTS_Sampled_Cutoff;static float  unity_DOTS_Sampled_Smoothness;static float  unity_DOTS_Sampled_Metallic;static float  unity_DOTS_Sampled_BumpScale;static float  unity_DOTS_Sampled_Parallax;static float  unity_DOTS_Sampled_OcclusionStrength;static float  unity_DOTS_Sampled_ClearCoatMask;static float  unity_DOTS_Sampled_ClearCoatSmoothness;static float  unity_DOTS_Sampled_DetailAlbedoMapScale;static float  unity_DOTS_Sampled_DetailNormalMapScale;static float  unity_DOTS_Sampled_Surface;void SetupDOTSLitMaterialPropertyCaches()&#123;    unity_DOTS_Sampled_BaseColor            = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _BaseColor);    unity_DOTS_Sampled_SpecColor            = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _SpecColor);    unity_DOTS_Sampled_EmissionColor        = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _EmissionColor);    unity_DOTS_Sampled_Cutoff               = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Cutoff);    unity_DOTS_Sampled_Smoothness           = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Smoothness);    unity_DOTS_Sampled_Metallic             = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Metallic);    unity_DOTS_Sampled_BumpScale            = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _BumpScale);    unity_DOTS_Sampled_Parallax             = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Parallax);    unity_DOTS_Sampled_OcclusionStrength    = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _OcclusionStrength);    unity_DOTS_Sampled_ClearCoatMask        = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _ClearCoatMask);    unity_DOTS_Sampled_ClearCoatSmoothness  = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _ClearCoatSmoothness);    unity_DOTS_Sampled_DetailAlbedoMapScale = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _DetailAlbedoMapScale);    unity_DOTS_Sampled_DetailNormalMapScale = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _DetailNormalMapScale);    unity_DOTS_Sampled_Surface              = UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float , _Surface);&#125;#undef UNITY_SETUP_DOTS_MATERIAL_PROPERTY_CACHES#define UNITY_SETUP_DOTS_MATERIAL_PROPERTY_CACHES() SetupDOTSLitMaterialPropertyCaches()#define _BaseColor              unity_DOTS_Sampled_BaseColor#define _SpecColor              unity_DOTS_Sampled_SpecColor#define _EmissionColor          unity_DOTS_Sampled_EmissionColor#define _Cutoff                 unity_DOTS_Sampled_Cutoff#define _Smoothness             unity_DOTS_Sampled_Smoothness#define _Metallic               unity_DOTS_Sampled_Metallic#define _BumpScale              unity_DOTS_Sampled_BumpScale#define _Parallax               unity_DOTS_Sampled_Parallax#define _OcclusionStrength      unity_DOTS_Sampled_OcclusionStrength#define _ClearCoatMask          unity_DOTS_Sampled_ClearCoatMask#define _ClearCoatSmoothness    unity_DOTS_Sampled_ClearCoatSmoothness#define _DetailAlbedoMapScale   unity_DOTS_Sampled_DetailAlbedoMapScale#define _DetailNormalMapScale   unity_DOTS_Sampled_DetailNormalMapScale#define _Surface                unity_DOTS_Sampled_Surface#endif</code></pre></details></li><li><details><summary> <font color="#007ACC">🐍 为什么要声明这么多字段呢？ </font></summary><blockquote><p>在这里，我们希望避免像这样用某些内容覆盖属性，比如 <code>_BaseColor：#define _BaseColor UNITY_ACCESS_DOTS_INSTANCED_PROP_WITH_DEFAULT(float4, _BaseColor0);</code><br>这样做虽然更简单，但可能会导致编译器在每次使用 <strong>_BaseColor</strong> 时重新生成属性加载代码。<br>为避免这种情况，属性加载在着色器开始处的某些静态值中被缓存。<br>然后像 <strong>_BaseColor</strong> 这样的属性会被覆盖，使其直接展开为静态值，如下所示：<br><code>#define _BaseColor unity_DOTS_Sampled_BaseColor</code><br>这个简单的修复在某些场景下，在 URP 上的 Meta Quest 2 上提高了 GPU 性能约 10%。</p></blockquote></details></li></ul></li><li><p>声明贴图和采样器</p><ul><li><details><summary> <font color="#007ACC">🐍 贴图与采样器定义代码 </font></summary><pre><code class="language-hlsl">TEXTURE2D(_ParallaxMap);        SAMPLER(sampler_ParallaxMap);TEXTURE2D(_OcclusionMap);       SAMPLER(sampler_OcclusionMap);TEXTURE2D(_DetailMask);         SAMPLER(sampler_DetailMask);TEXTURE2D(_DetailAlbedoMap);    SAMPLER(sampler_DetailAlbedoMap);TEXTURE2D(_DetailNormalMap);    SAMPLER(sampler_DetailNormalMap);TEXTURE2D(_MetallicGlossMap);   SAMPLER(sampler_MetallicGlossMap);TEXTURE2D(_SpecGlossMap);       SAMPLER(sampler_SpecGlossMap);TEXTURE2D(_ClearCoatMap);       SAMPLER(sampler_ClearCoatMap);</code></pre></details></li></ul></li></ul><h2 id="预定义一个函数处理如何采样高光和金属工作流中定义的贴图"><a href="#预定义一个函数处理如何采样高光和金属工作流中定义的贴图" class="headerlink" title="预定义一个函数处理如何采样高光和金属工作流中定义的贴图"></a>预定义一个函数处理如何采样高光和金属工作流中定义的贴图</h2><pre><code class="language-hlsl">#ifdef _SPECULAR_SETUP    #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_SpecGlossMap, sampler_SpecGlossMap, uv)#else    #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_MetallicGlossMap, sampler_MetallicGlossMap, uv)#endif</code></pre><p>&emsp;可以看到：</p><ul><li>开启高光工作流（启用关键字 <strong>_SPECULAR_SETUP</strong> ）： <strong>SAMPLE_METALLICSPECULAR</strong> 被定义为采样 <strong>_SpecGlossMap</strong>。</li><li>开启金属工作流（关闭关键字 <strong>_SPECULAR_SETUP</strong> ）： <strong>SAMPLE_METALLICSPECULAR</strong> 被定义为采样 <strong>_MetallicGlossMap</strong>。</li></ul><h2 id="定义采样金属-高光工作流的高光函数"><a href="#定义采样金属-高光工作流的高光函数" class="headerlink" title="定义采样金属&#x2F;高光工作流的高光函数"></a>定义采样金属&#x2F;高光工作流的高光函数</h2><pre><code class="language-hlsl">half4 SampleMetallicSpecGloss(float2 uv, half albedoAlpha)&#123;    half4 specGloss;#ifdef _METALLICSPECGLOSSMAP    specGloss = half4(SAMPLE_METALLICSPECULAR(uv));    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a *= _Smoothness;    #endif#else // _METALLICSPECGLOSSMAP    #if _SPECULAR_SETUP        specGloss.rgb = _SpecColor.rgb;    #else        specGloss.rgb = _Metallic.rrr;    #endif    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a = _Smoothness;    #endif#endif    return specGloss;&#125;</code></pre><ul><li><strong>uv</strong>：输入一个采样uv，一般就是模型的uv。</li><li><strong>albedoAlpha</strong>: albedo贴图的Alpha数值，这个数值一定是albedo贴图的，不能做预处理，比如albedo*_BaseColor，这是不可以的。但如果是自定义，那就不用考虑这种问题了。<pre><code class="language-hlsl">albedoAlpha = SAMPLE_TEXTURE2D(_BaseMap,sampler_BaseMap,i.uv).a;</code></pre></li></ul><p>&emsp;在这个函数中，首先定义了一个变量 <strong>specGloss</strong> ，然后分为两种情况：</p><ol><li>材质面板赋予了金属&#x2F;高光工作流的高光贴图：</li></ol><ul><li>先采样贴图，<strong>SAMPLE_METALLICSPECULAR</strong> 会根据对应的工作流返回对应的贴图的颜色，则获得了 <strong>specGloss</strong> 的rgb颜色，存放高光颜色。</li><li>然后获取光滑度，光滑度存放在 <strong>specGloss</strong> 的alpha通道中，分为两种情况：<ol><li>光滑度数值贴图来自 <strong>albedoAlpha</strong> ，则返回 <strong>albedoAlpha*调节参数（即_Smoothness）</strong>。</li><li>光滑度数值贴图来自高光贴图 ，则数值就是采样好并存储好的 <strong>specGloss.a</strong> ，直接返回 <strong>specGloss.a*调节参数（即_Smoothness）</strong>。</li></ol></li></ul><ol start="2"><li>材质面板未赋予金属&#x2F;高光工作流的高光贴图：</li></ol><ul><li>先获取高光颜色，高光工作流定义了 <strong>_SpecColor</strong> 高光颜色属性，直接赋值即可，但是金属工作流不存在 <strong>_SpecColor</strong> 属性一说，直接赋值为 <strong>_Metallic（金属强度调节参数）</strong> 的灰度颜色。</li><li>然后获取光滑度，光滑度存放在 <strong>specGloss</strong> 的alpha通道中，分为两种情况：<ol><li>光滑度数值贴图来自 <strong>albedoAlpha</strong> ，则返回 <strong>albedoAlpha*调节参数（即_Smoothness）</strong>。</li><li>因为没有金属&#x2F;高光工作流的高光贴图了，因此如果不是来自albedo贴图的alpha通道的话，直接返回 <strong>_Smoothness（光滑度调节参数）</strong>。</li></ol></li></ul><h2 id="定义采样遮蔽图函数"><a href="#定义采样遮蔽图函数" class="headerlink" title="定义采样遮蔽图函数"></a>定义采样遮蔽图函数</h2><pre><code class="language-hlsl">half SampleOcclusion(float2 uv)&#123;    #ifdef _OCCLUSIONMAP        half occ = SAMPLE_TEXTURE2D(_OcclusionMap, sampler_OcclusionMap, uv).g;        return LerpWhiteTo(occ, _OcclusionStrength);    #else        return half(1.0);    #endif&#125;</code></pre><ul><li><p><strong>uv</strong>：输入一个采样uv，一般就是模型的uv。<br>&emsp;函数中分为了有遮蔽贴图和没有遮蔽贴图两种情况。所谓遮蔽就是物体自遮挡产生的暗部区域，也叫做Ao。因为实时计算不可能真的去计算物体自身的遮挡关系，这太消耗性能了，所以实时遮蔽都是采用一张遮罩图作为遮蔽区域的遮罩。数值越靠近0，表示这里暗部更多，越靠近1，表示这里越没有自遮挡。</p></li><li><details><summary> <font color="#007ACC">🐍 LerpWhiteTo函数是什么？ </font></summary><pre><code class="language-hlsl">real LerpWhiteTo(real b, real t)&#123;    real oneMinusT = 1.0 - t;    return oneMinusT + b * t;&#125;</code></pre><blockquote><p>这个函数翻译即为”从白色插值到”，我们可以转换一下公式就知道它其实就是一个 <strong>lerp</strong> 函数而已：$1-t+b\times t &#x3D; 1+(b-1)\times t &#x3D; lerp(1,b,t)$</p></blockquote></li></ul></details><ul><li><details><summary> <font color="#007ACC">🐍 为什么要使用LerpWhiteTo函数？ </font></summary>  &emsp;我们知道 <strong>LerpWhiteTo</strong> 不过是一个封装的从1插值到目标值的函数，回到Shader代码，我们实际可以将其转换成直观的表示：<pre><code class="language-hlsl">half SampleOcclusion(float2 uv)&#123;    #ifdef _OCCLUSIONMAP        half occ = SAMPLE_TEXTURE2D(_OcclusionMap, sampler_OcclusionMap, uv).g;        return lerp(1.0,occ, _OcclusionStrength);    #else        return half(1.0);    #endif&#125;</code></pre>  &emsp;因为我们拥有 <strong>_OcclusionStrength</strong> 强度调节属性，那么当 <strong>_OcclusionStrength</strong> 为0时，则应该没有遮蔽，反之为1时则根据贴图的遮蔽区域进行设置。<br>  &emsp;我们放到宏观来看，遮蔽贴图一定是有的地方没有遮蔽，那么那部分贴图区域就为1，有的地方有遮蔽，那部分贴图区域为0。<ul><li>当 <strong>occ</strong> 为1时，不管如何调节 <strong>_OcclusionStrength</strong> 都为1，这样就保证了这部分区域是没有遮蔽的。</li><li>但 <strong>ooc</strong> 为0时，那么返回的遮蔽强度将会是 <strong>1.0-_OcclusionStrength</strong> 强度，则即使参数越大，那么这个区域就越暗，反之越亮。</li></ul></li></ul></details><h2 id="定义采样清漆贴图函数"><a href="#定义采样清漆贴图函数" class="headerlink" title="定义采样清漆贴图函数"></a>定义采样清漆贴图函数</h2><pre><code class="language-hlsl">// Returns clear coat parameters// .x/.r == mask// .y/.g == smoothnesshalf2 SampleClearCoat(float2 uv)&#123;#if defined(_CLEARCOAT) || defined(_CLEARCOATMAP)    half2 clearCoatMaskSmoothness = half2(_ClearCoatMask, _ClearCoatSmoothness);#if defined(_CLEARCOATMAP)    clearCoatMaskSmoothness *= SAMPLE_TEXTURE2D(_ClearCoatMap, sampler_ClearCoatMap, uv).rg;#endif    return clearCoatMaskSmoothness;#else    return half2(0.0, 1.0);#endif  // _CLEARCOAT&#125;</code></pre><p>&emsp;清漆这里不作详解。</p><h2 id="定义视差映射函数"><a href="#定义视差映射函数" class="headerlink" title="定义视差映射函数"></a>定义视差映射函数</h2><pre><code class="language-hlsl">void ApplyPerPixelDisplacement(half3 viewDirTS, inout float2 uv)&#123;#if defined(_PARALLAXMAP)    uv += ParallaxMapping(TEXTURE2D_ARGS(_ParallaxMap, sampler_ParallaxMap), viewDirTS, _Parallax, uv);#endif&#125;</code></pre><details><summary> <font color="#007ACC">🐍 ParallaxMapping的算法是？ </font></summary><pre><code class="language-hlsl">half2 ParallaxOffset1Step(half height, half amplitude, half3 viewDirTS)&#123;    height = height * amplitude - amplitude / 2.0;    half3 v = normalize(viewDirTS);    v.z += 0.42;    return height * (v.xy / v.z);&#125;float2 ParallaxMapping(TEXTURE2D_PARAM(heightMap, sampler_heightMap), half3 viewDirTS, half scale, float2 uv)&#123;    half h = SAMPLE_TEXTURE2D(heightMap, sampler_heightMap, uv).g;    float2 offset = ParallaxOffset1Step(h, scale, viewDirTS);    return offset;&#125;</code></pre></details><h2 id="定义应用细节贴图函数与采样细节贴图函数"><a href="#定义应用细节贴图函数与采样细节贴图函数" class="headerlink" title="定义应用细节贴图函数与采样细节贴图函数"></a>定义应用细节贴图函数与采样细节贴图函数</h2><pre><code class="language-hlsl">// Used for scaling detail albedo. Main features:// - Depending if detailAlbedo brightens or darkens, scale magnifies effect.// - No effect is applied if detailAlbedo is 0.5.half3 ScaleDetailAlbedo(half3 detailAlbedo, half scale)&#123;    // detailAlbedo = detailAlbedo * 2.0h - 1.0h;    // detailAlbedo *= _DetailAlbedoMapScale;    // detailAlbedo = detailAlbedo * 0.5h + 0.5h;    // return detailAlbedo * 2.0f;    // A bit more optimized    return half(2.0) * detailAlbedo * scale - scale + half(1.0);&#125;half3 ApplyDetailAlbedo(float2 detailUv, half3 albedo, half detailMask)&#123;#if defined(_DETAIL)    half3 detailAlbedo = SAMPLE_TEXTURE2D(_DetailAlbedoMap, sampler_DetailAlbedoMap, detailUv).rgb;    // In order to have same performance as builtin, we do scaling only if scale is not 1.0 (Scaled version has 6 additional instructions)#if defined(_DETAIL_SCALED)    detailAlbedo = ScaleDetailAlbedo(detailAlbedo, _DetailAlbedoMapScale);#else    detailAlbedo = half(2.0) * detailAlbedo;#endif    return albedo * LerpWhiteTo(detailAlbedo, detailMask);#else    return albedo;#endif&#125;Unity对细节贴图做了两倍亮度的增量，让细节贴图和原贴图颜色混合能够更加清晰。</code></pre><h2 id="定义采样细节法线贴图函数"><a href="#定义采样细节法线贴图函数" class="headerlink" title="定义采样细节法线贴图函数"></a>定义采样细节法线贴图函数</h2><pre><code class="language-hlsl">half3 ApplyDetailNormal(float2 detailUv, half3 normalTS, half detailMask)&#123;#if defined(_DETAIL)#if BUMP_SCALE_NOT_SUPPORTED    half3 detailNormalTS = UnpackNormal(SAMPLE_TEXTURE2D(_DetailNormalMap, sampler_DetailNormalMap, detailUv));#else    half3 detailNormalTS = UnpackNormalScale(SAMPLE_TEXTURE2D(_DetailNormalMap, sampler_DetailNormalMap, detailUv), _DetailNormalMapScale);#endif    // With UNITY_NO_DXT5nm unpacked vector is not normalized for BlendNormalRNM    // For visual consistancy we going to do in all cases    detailNormalTS = normalize(detailNormalTS);    return lerp(normalTS, BlendNormalRNM(normalTS, detailNormalTS), detailMask); // todo: detailMask should lerp the angle of the quaternion rotation, not the normals#else    return normalTS;#endif&#125;</code></pre><h2 id="初始化表面数据函数"><a href="#初始化表面数据函数" class="headerlink" title="初始化表面数据函数"></a>初始化表面数据函数</h2><pre><code class="language-hlsl">inline void InitializeStandardLitSurfaceData(float2 uv, out SurfaceData outSurfaceData)&#123;    half4 albedoAlpha = SampleAlbedoAlpha(uv, TEXTURE2D_ARGS(_BaseMap, sampler_BaseMap));    outSurfaceData.alpha = Alpha(albedoAlpha.a, _BaseColor, _Cutoff);    half4 specGloss = SampleMetallicSpecGloss(uv, albedoAlpha.a);    outSurfaceData.albedo = albedoAlpha.rgb * _BaseColor.rgb;    outSurfaceData.albedo = AlphaModulate(outSurfaceData.albedo, outSurfaceData.alpha);#if _SPECULAR_SETUP    outSurfaceData.metallic = half(1.0);    outSurfaceData.specular = specGloss.rgb;#else    outSurfaceData.metallic = specGloss.r;    outSurfaceData.specular = half3(0.0, 0.0, 0.0);#endif    outSurfaceData.smoothness = specGloss.a;    outSurfaceData.normalTS = SampleNormal(uv, TEXTURE2D_ARGS(_BumpMap, sampler_BumpMap), _BumpScale);    outSurfaceData.occlusion = SampleOcclusion(uv);    outSurfaceData.emission = SampleEmission(uv, _EmissionColor.rgb, TEXTURE2D_ARGS(_EmissionMap, sampler_EmissionMap));#if defined(_CLEARCOAT) || defined(_CLEARCOATMAP)    half2 clearCoat = SampleClearCoat(uv);    outSurfaceData.clearCoatMask       = clearCoat.r;    outSurfaceData.clearCoatSmoothness = clearCoat.g;#else    outSurfaceData.clearCoatMask       = half(0.0);    outSurfaceData.clearCoatSmoothness = half(0.0);#endif#if defined(_DETAIL)    half detailMask = SAMPLE_TEXTURE2D(_DetailMask, sampler_DetailMask, uv).a;    float2 detailUv = uv * _DetailAlbedoMap_ST.xy + _DetailAlbedoMap_ST.zw;    outSurfaceData.albedo = ApplyDetailAlbedo(detailUv, outSurfaceData.albedo, detailMask);    outSurfaceData.normalTS = ApplyDetailNormal(detailUv, outSurfaceData.normalTS, detailMask);#endif&#125;</code></pre><p>&emsp;其实没有什么需要多说的，这个函数就是封装了贴图采样，初始化表面数据，解释一下不常见的函数。</p><ul><li><p><strong>SampleAlbedoAlpha</strong>：只是封装了一下采样函数而已：</p><pre><code class="language-hlsl">half4 SampleAlbedoAlpha(float2 uv, TEXTURE2D_PARAM(albedoAlphaMap, sampler_albedoAlphaMap))&#123;    return half4(SAMPLE_TEXTURE2D(albedoAlphaMap, sampler_albedoAlphaMap, uv));&#125;</code></pre></li><li><p><strong>Alpha</strong>: 对透明度进行处理，包含了透明度裁剪，需要注意其中对abledoAlpha的alpha通道处理，因为在URP设置中这个alpha可能代表光滑度，不能被用来裁剪：</p><pre><code class="language-hlsl">  half Alpha(half albedoAlpha, half4 color, half cutoff)  &#123;  #if !defined(_SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A) &amp;&amp; !defined(_GLOSSINESS_FROM_BASE_ALPHA)      half alpha = albedoAlpha * color.a;  #else      half alpha = color.a;  #endif      alpha = AlphaDiscard(alpha, cutoff);      return alpha;  &#125;</code></pre></li><li><p><strong>SampleMetallicSpecGloss</strong>：在之前已经做过介绍，这是采样金属&#x2F;高光工作流的高光便返回高光颜色与光滑度的，光滑度存放在返回值的a通道中：</p><pre><code class="language-hlsl">  #ifdef _SPECULAR_SETUP      #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_SpecGlossMap, sampler_SpecGlossMap, uv)  #else      #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_MetallicGlossMap, sampler_MetallicGlossMap, uv)  #endif  half4 SampleMetallicSpecGloss(float2 uv, half albedoAlpha)  &#123;      half4 specGloss;  #ifdef _METALLICSPECGLOSSMAP      specGloss = half4(SAMPLE_METALLICSPECULAR(uv));      #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A          specGloss.a = albedoAlpha * _Smoothness;      #else          specGloss.a *= _Smoothness;      #endif  #else // _METALLICSPECGLOSSMAP      #if _SPECULAR_SETUP          specGloss.rgb = _SpecColor.rgb;      #else          specGloss.rgb = _Metallic.rrr;      #endif      #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A          specGloss.a = albedoAlpha * _Smoothness;      #else          specGloss.a = _Smoothness;      #endif  #endif      return specGloss;  &#125;</code></pre></li><li><p><strong>AlphaModulate</strong>：这个函数是为了对应关键字 <strong>_ALPHAMODULATE_ON</strong> 的。通过使用 <strong>alpha</strong> 将 <strong>albedo</strong> 向 1（白色）线性插值来为 <strong>multiply</strong> 混合创建假 <strong>alpha</strong>。手动调整以获得“更亮”的 <strong>multiply</strong> 效果（类似于“预乘 alpha”），将是在纹理中绘制更白的像素。这在着色器中模拟了该过程，因此应应用于基础&#x2F;源颜色。代码如下：</p><pre><code class="language-hlsl">  half3 AlphaModulate(half3 albedo, half alpha)  &#123;      // Fake alpha for multiply blend by lerping albedo towards 1 (white) using alpha.      // Manual adjustment for &quot;lighter&quot; multiply effect (similar to &quot;premultiplied alpha&quot;)      // would be painting whiter pixels in the texture.      // This emulates that procedure in shader, so it should be applied to the base/source color.      #if defined(_ALPHAMODULATE_ON)          return lerp(half3(1.0, 1.0, 1.0), albedo, alpha);      #else          return albedo;      #endif  &#125;</code></pre></li><li><p><strong>SampleNormal</strong>: 法线采样封装函数:</p><pre><code class="language-hlsl">  half3 SampleNormal(float2 uv, TEXTURE2D_PARAM(bumpMap, sampler_bumpMap), half scale = half(1.0))  &#123;  #ifdef _NORMALMAP      half4 n = SAMPLE_TEXTURE2D(bumpMap, sampler_bumpMap, uv);      #if BUMP_SCALE_NOT_SUPPORTED          return UnpackNormal(n);      #else          return UnpackNormalScale(n, scale);      #endif  #else      return half3(0.0h, 0.0h, 1.0h);  #endif  &#125;</code></pre></li><li><p><strong>SampleEmission</strong>：采样自发光函数：</p><pre><code class="language-hlsl">  half3 SampleEmission(float2 uv, half3 emissionColor, TEXTURE2D_PARAM(emissionMap, sampler_emissionMap))  &#123;  #ifndef _EMISSION      return 0;  #else      return SAMPLE_TEXTURE2D(emissionMap, sampler_emissionMap, uv).rgb * emissionColor;  #endif  &#125;</code></pre></li></ul><details><summary> <font color="#007ACC">🐍 SurfaceData定义是什么？ </font></summary><pre><code class="language-hlsl">struct SurfaceData&#123;    half3 albedo;    half3 specular;    half  metallic;    half  smoothness;    half3 normalTS;    half3 emission;    half  occlusion;    half  alpha;    half  clearCoatMask;    half  clearCoatSmoothness;&#125;;</code></pre></details><hr><h1 id="LitForwardPass-hlsl"><a href="#LitForwardPass-hlsl" class="headerlink" title="LitForwardPass.hlsl"></a>LitForwardPass.hlsl</h1><pre><code class="language-hlsl">#ifndef UNIVERSAL_FORWARD_LIT_PASS_INCLUDED#define UNIVERSAL_FORWARD_LIT_PASS_INCLUDED#include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/Lighting.hlsl&quot;#if defined(LOD_FADE_CROSSFADE)    #include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/LODCrossFade.hlsl&quot;#endif// GLES2 has limited amount of interpolators#if defined(_PARALLAXMAP) &amp;&amp; !defined(SHADER_API_GLES)#define REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR#endif#if (defined(_NORMALMAP) || (defined(_PARALLAXMAP) &amp;&amp; !defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR))) || defined(_DETAIL)#define REQUIRES_WORLD_SPACE_TANGENT_INTERPOLATOR#endif// keep this file in sync with LitGBufferPass.hlslstruct Attributes&#123;    float4 positionOS   : POSITION;    float3 normalOS     : NORMAL;    float4 tangentOS    : TANGENT;    float2 texcoord     : TEXCOORD0;    float2 staticLightmapUV   : TEXCOORD1;    float2 dynamicLightmapUV  : TEXCOORD2;    UNITY_VERTEX_INPUT_INSTANCE_ID&#125;;struct Varyings&#123;    float2 uv                       : TEXCOORD0;#if defined(REQUIRES_WORLD_SPACE_POS_INTERPOLATOR)    float3 positionWS               : TEXCOORD1;#endif    float3 normalWS                 : TEXCOORD2;#if defined(REQUIRES_WORLD_SPACE_TANGENT_INTERPOLATOR)    half4 tangentWS                : TEXCOORD3;    // xyz: tangent, w: sign#endif#ifdef _ADDITIONAL_LIGHTS_VERTEX    half4 fogFactorAndVertexLight   : TEXCOORD5; // x: fogFactor, yzw: vertex light#else    half  fogFactor                 : TEXCOORD5;#endif#if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)    float4 shadowCoord              : TEXCOORD6;#endif#if defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR)    half3 viewDirTS                : TEXCOORD7;#endif    DECLARE_LIGHTMAP_OR_SH(staticLightmapUV, vertexSH, 8);#ifdef DYNAMICLIGHTMAP_ON    float2  dynamicLightmapUV : TEXCOORD9; // Dynamic lightmap UVs#endif    float4 positionCS               : SV_POSITION;    UNITY_VERTEX_INPUT_INSTANCE_ID    UNITY_VERTEX_OUTPUT_STEREO&#125;;void InitializeInputData(Varyings input, half3 normalTS, out InputData inputData)&#123;    inputData = (InputData)0;#if defined(REQUIRES_WORLD_SPACE_POS_INTERPOLATOR)    inputData.positionWS = input.positionWS;#endif    half3 viewDirWS = GetWorldSpaceNormalizeViewDir(input.positionWS);#if defined(_NORMALMAP) || defined(_DETAIL)    float sgn = input.tangentWS.w;      // should be either +1 or -1    float3 bitangent = sgn * cross(input.normalWS.xyz, input.tangentWS.xyz);    half3x3 tangentToWorld = half3x3(input.tangentWS.xyz, bitangent.xyz, input.normalWS.xyz);    #if defined(_NORMALMAP)    inputData.tangentToWorld = tangentToWorld;    #endif    inputData.normalWS = TransformTangentToWorld(normalTS, tangentToWorld);#else    inputData.normalWS = input.normalWS;#endif    inputData.normalWS = NormalizeNormalPerPixel(inputData.normalWS);    inputData.viewDirectionWS = viewDirWS;#if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)    inputData.shadowCoord = input.shadowCoord;#elif defined(MAIN_LIGHT_CALCULATE_SHADOWS)    inputData.shadowCoord = TransformWorldToShadowCoord(inputData.positionWS);#else    inputData.shadowCoord = float4(0, 0, 0, 0);#endif#ifdef _ADDITIONAL_LIGHTS_VERTEX    inputData.fogCoord = InitializeInputDataFog(float4(input.positionWS, 1.0), input.fogFactorAndVertexLight.x);    inputData.vertexLighting = input.fogFactorAndVertexLight.yzw;#else    inputData.fogCoord = InitializeInputDataFog(float4(input.positionWS, 1.0), input.fogFactor);#endif#if defined(DYNAMICLIGHTMAP_ON)    inputData.bakedGI = SAMPLE_GI(input.staticLightmapUV, input.dynamicLightmapUV, input.vertexSH, inputData.normalWS);#else    inputData.bakedGI = SAMPLE_GI(input.staticLightmapUV, input.vertexSH, inputData.normalWS);#endif    inputData.normalizedScreenSpaceUV = GetNormalizedScreenSpaceUV(input.positionCS);    inputData.shadowMask = SAMPLE_SHADOWMASK(input.staticLightmapUV);    #if defined(DEBUG_DISPLAY)    #if defined(DYNAMICLIGHTMAP_ON)    inputData.dynamicLightmapUV = input.dynamicLightmapUV;    #endif    #if defined(LIGHTMAP_ON)    inputData.staticLightmapUV = input.staticLightmapUV;    #else    inputData.vertexSH = input.vertexSH;    #endif    #endif&#125;/////////////////////////////////////////////////////////////////////////////////                  Vertex and Fragment functions                            /////////////////////////////////////////////////////////////////////////////////// Used in Standard (Physically Based) shaderVaryings LitPassVertex(Attributes input)&#123;    Varyings output = (Varyings)0;    UNITY_SETUP_INSTANCE_ID(input);    UNITY_TRANSFER_INSTANCE_ID(input, output);    UNITY_INITIALIZE_VERTEX_OUTPUT_STEREO(output);    VertexPositionInputs vertexInput = GetVertexPositionInputs(input.positionOS.xyz);    // normalWS and tangentWS already normalize.    // this is required to avoid skewing the direction during interpolation    // also required for per-vertex lighting and SH evaluation    VertexNormalInputs normalInput = GetVertexNormalInputs(input.normalOS, input.tangentOS);    half3 vertexLight = VertexLighting(vertexInput.positionWS, normalInput.normalWS);    half fogFactor = 0;    #if !defined(_FOG_FRAGMENT)        fogFactor = ComputeFogFactor(vertexInput.positionCS.z);    #endif    output.uv = TRANSFORM_TEX(input.texcoord, _BaseMap);    // already normalized from normal transform to WS.    output.normalWS = normalInput.normalWS;#if defined(REQUIRES_WORLD_SPACE_TANGENT_INTERPOLATOR) || defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR)    real sign = input.tangentOS.w * GetOddNegativeScale();    half4 tangentWS = half4(normalInput.tangentWS.xyz, sign);#endif#if defined(REQUIRES_WORLD_SPACE_TANGENT_INTERPOLATOR)    output.tangentWS = tangentWS;#endif#if defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR)    half3 viewDirWS = GetWorldSpaceNormalizeViewDir(vertexInput.positionWS);    half3 viewDirTS = GetViewDirectionTangentSpace(tangentWS, output.normalWS, viewDirWS);    output.viewDirTS = viewDirTS;#endif    OUTPUT_LIGHTMAP_UV(input.staticLightmapUV, unity_LightmapST, output.staticLightmapUV);#ifdef DYNAMICLIGHTMAP_ON    output.dynamicLightmapUV = input.dynamicLightmapUV.xy * unity_DynamicLightmapST.xy + unity_DynamicLightmapST.zw;#endif    OUTPUT_SH(output.normalWS.xyz, output.vertexSH);#ifdef _ADDITIONAL_LIGHTS_VERTEX    output.fogFactorAndVertexLight = half4(fogFactor, vertexLight);#else    output.fogFactor = fogFactor;#endif#if defined(REQUIRES_WORLD_SPACE_POS_INTERPOLATOR)    output.positionWS = vertexInput.positionWS;#endif#if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)    output.shadowCoord = GetShadowCoord(vertexInput);#endif    output.positionCS = vertexInput.positionCS;    return output;&#125;// Used in Standard (Physically Based) shadervoid LitPassFragment(    Varyings input    , out half4 outColor : SV_Target0#ifdef _WRITE_RENDERING_LAYERS    , out float4 outRenderingLayers : SV_Target1#endif)&#123;    UNITY_SETUP_INSTANCE_ID(input);    UNITY_SETUP_STEREO_EYE_INDEX_POST_VERTEX(input);#if defined(_PARALLAXMAP)#if defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR)    half3 viewDirTS = input.viewDirTS;#else    half3 viewDirWS = GetWorldSpaceNormalizeViewDir(input.positionWS);    half3 viewDirTS = GetViewDirectionTangentSpace(input.tangentWS, input.normalWS, viewDirWS);#endif    ApplyPerPixelDisplacement(viewDirTS, input.uv);#endif    SurfaceData surfaceData;    InitializeStandardLitSurfaceData(input.uv, surfaceData);#ifdef LOD_FADE_CROSSFADE    LODFadeCrossFade(input.positionCS);#endif    InputData inputData;    InitializeInputData(input, surfaceData.normalTS, inputData);    SETUP_DEBUG_TEXTURE_DATA(inputData, input.uv, _BaseMap);#ifdef _DBUFFER    ApplyDecalToSurfaceData(input.positionCS, surfaceData, inputData);#endif    half4 color = UniversalFragmentPBR(inputData, surfaceData);    color.rgb = MixFog(color.rgb, inputData.fogCoord);    color.a = OutputAlpha(color.a, IsSurfaceTypeTransparent(_Surface));    outColor = color;#ifdef _WRITE_RENDERING_LAYERS    uint renderingLayers = GetMeshRenderingLayer();    outRenderingLayers = float4(EncodeMeshRenderingLayer(renderingLayers), 0, 0, 0);#endif&#125;#endif</code></pre><h2 id="引用光照头文件"><a href="#引用光照头文件" class="headerlink" title="引用光照头文件"></a>引用光照头文件</h2><pre><code class="language-hlsl">#include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/Lighting.hlsl&quot;</code></pre><h2 id="引用LODGroup效果头文件"><a href="#引用LODGroup效果头文件" class="headerlink" title="引用LODGroup效果头文件"></a>引用LODGroup效果头文件</h2><pre><code class="language-hlsl">#if defined(LOD_FADE_CROSSFADE)    #include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/LODCrossFade.hlsl&quot;#endif</code></pre><p>可以参考 <a href="https://blog.csdn.net/qq_39574690/article/details/144896857">https://blog.csdn.net/qq_39574690/article/details/144896857</a></p><h2 id="预定义视差映射标签"><a href="#预定义视差映射标签" class="headerlink" title="预定义视差映射标签"></a>预定义视差映射标签</h2><pre><code class="language-hlsl">// GLES2 has limited amount of interpolators#if defined(_PARALLAXMAP) &amp;&amp; !defined(SHADER_API_GLES)#define REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR#endif</code></pre><h2 id="预定义法线贴图与视察映射需要切线空间标签"><a href="#预定义法线贴图与视察映射需要切线空间标签" class="headerlink" title="预定义法线贴图与视察映射需要切线空间标签"></a>预定义法线贴图与视察映射需要切线空间标签</h2><pre><code class="language-hlsl">#if (defined(_NORMALMAP) || (defined(_PARALLAXMAP) &amp;&amp; !defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR))) || defined(_DETAIL)#define REQUIRES_WORLD_SPACE_TANGENT_INTERPOLATOR#endif</code></pre><h2 id="声明顶点输入结构体"><a href="#声明顶点输入结构体" class="headerlink" title="声明顶点输入结构体"></a>声明顶点输入结构体</h2><pre><code class="language-hlsl">struct Attributes&#123;    float4 positionOS   : POSITION;    float3 normalOS     : NORMAL;    float4 tangentOS    : TANGENT;    float2 texcoord     : TEXCOORD0;    float2 staticLightmapUV   : TEXCOORD1;    float2 dynamicLightmapUV  : TEXCOORD2;    UNITY_VERTEX_INPUT_INSTANCE_ID&#125;;</code></pre><h2 id="声明顶点输出结构体"><a href="#声明顶点输出结构体" class="headerlink" title="声明顶点输出结构体"></a>声明顶点输出结构体</h2><pre><code class="language-hlsl">struct Varyings&#123;    float2 uv                       : TEXCOORD0;#if defined(REQUIRES_WORLD_SPACE_POS_INTERPOLATOR)    float3 positionWS               : TEXCOORD1;#endif    float3 normalWS                 : TEXCOORD2;#if defined(REQUIRES_WORLD_SPACE_TANGENT_INTERPOLATOR)    half4 tangentWS                : TEXCOORD3;    // xyz: tangent, w: sign#endif#ifdef _ADDITIONAL_LIGHTS_VERTEX    half4 fogFactorAndVertexLight   : TEXCOORD5; // x: fogFactor, yzw: vertex light#else    half  fogFactor                 : TEXCOORD5;#endif#if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)    float4 shadowCoord              : TEXCOORD6;#endif#if defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR)    half3 viewDirTS                : TEXCOORD7;#endif    DECLARE_LIGHTMAP_OR_SH(staticLightmapUV, vertexSH, 8);#ifdef DYNAMICLIGHTMAP_ON    float2  dynamicLightmapUV : TEXCOORD9; // Dynamic lightmap UVs#endif    float4 positionCS               : SV_POSITION;    UNITY_VERTEX_INPUT_INSTANCE_ID    UNITY_VERTEX_OUTPUT_STEREO&#125;;</code></pre><details><summary> <font color="#007ACC">🐍 DECLARE_LIGHTMAP_OR_SH的定义是？ </font></summary><pre><code class="language-hlsl">#if defined(LIGHTMAP_ON)    #define DECLARE_LIGHTMAP_OR_SH(lmName, shName, index) float2 lmName : TEXCOORD##index    #define OUTPUT_LIGHTMAP_UV(lightmapUV, lightmapScaleOffset, OUT) OUT.xy = lightmapUV.xy * lightmapScaleOffset.xy + lightmapScaleOffset.zw;    #define OUTPUT_SH(normalWS, OUT)#else    #define DECLARE_LIGHTMAP_OR_SH(lmName, shName, index) half3 shName : TEXCOORD##index    #define OUTPUT_LIGHTMAP_UV(lightmapUV, lightmapScaleOffset, OUT)    #define OUTPUT_SH(normalWS, OUT) OUT.xyz = SampleSHVertex(normalWS)#endif</code></pre></details><h2 id="初始化输入数据函数"><a href="#初始化输入数据函数" class="headerlink" title="初始化输入数据函数"></a>初始化输入数据函数</h2><pre><code class="language-hlsl">void InitializeInputData(Varyings input, half3 normalTS, out InputData inputData)&#123;    inputData = (InputData)0;#if defined(REQUIRES_WORLD_SPACE_POS_INTERPOLATOR)    inputData.positionWS = input.positionWS;#endif    half3 viewDirWS = GetWorldSpaceNormalizeViewDir(input.positionWS);#if defined(_NORMALMAP) || defined(_DETAIL)    float sgn = input.tangentWS.w;      // should be either +1 or -1    float3 bitangent = sgn * cross(input.normalWS.xyz, input.tangentWS.xyz);    half3x3 tangentToWorld = half3x3(input.tangentWS.xyz, bitangent.xyz, input.normalWS.xyz);    #if defined(_NORMALMAP)    inputData.tangentToWorld = tangentToWorld;    #endif    inputData.normalWS = TransformTangentToWorld(normalTS, tangentToWorld);#else    inputData.normalWS = input.normalWS;#endif    inputData.normalWS = NormalizeNormalPerPixel(inputData.normalWS);    inputData.viewDirectionWS = viewDirWS;#if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)    inputData.shadowCoord = input.shadowCoord;#elif defined(MAIN_LIGHT_CALCULATE_SHADOWS)    inputData.shadowCoord = TransformWorldToShadowCoord(inputData.positionWS);#else    inputData.shadowCoord = float4(0, 0, 0, 0);#endif#ifdef _ADDITIONAL_LIGHTS_VERTEX    inputData.fogCoord = InitializeInputDataFog(float4(input.positionWS, 1.0), input.fogFactorAndVertexLight.x);    inputData.vertexLighting = input.fogFactorAndVertexLight.yzw;#else    inputData.fogCoord = InitializeInputDataFog(float4(input.positionWS, 1.0), input.fogFactor);#endif#if defined(DYNAMICLIGHTMAP_ON)    inputData.bakedGI = SAMPLE_GI(input.staticLightmapUV, input.dynamicLightmapUV, input.vertexSH, inputData.normalWS);#else    inputData.bakedGI = SAMPLE_GI(input.staticLightmapUV, input.vertexSH, inputData.normalWS);#endif    inputData.normalizedScreenSpaceUV = GetNormalizedScreenSpaceUV(input.positionCS);    inputData.shadowMask = SAMPLE_SHADOWMASK(input.staticLightmapUV);    #if defined(DEBUG_DISPLAY)    #if defined(DYNAMICLIGHTMAP_ON)    inputData.dynamicLightmapUV = input.dynamicLightmapUV;    #endif    #if defined(LIGHTMAP_ON)    inputData.staticLightmapUV = input.staticLightmapUV;    #else    inputData.vertexSH = input.vertexSH;    #endif    #endif&#125;</code></pre><details><summary> <font color="#007ACC">🐍 InputData定义是什么？ </font></summary><pre><code class="language-hlsl">struct InputData&#123;    float3  positionWS;    float4  positionCS;    float3  normalWS;    half3   viewDirectionWS;    float4  shadowCoord;    half    fogCoord;    half3   vertexLighting;    half3   bakedGI;    float2  normalizedScreenSpaceUV;    half4   shadowMask;    half3x3 tangentToWorld;    #if defined(DEBUG_DISPLAY)    half2   dynamicLightmapUV;    half2   staticLightmapUV;    float3  vertexSH;    half3 brdfDiffuse;    half3 brdfSpecular;    float2 uv;    uint mipCount;    // texelSize :    // x = 1 / width    // y = 1 / height    // z = width    // w = height    float4 texelSize;    // mipInfo :    // x = quality settings minStreamingMipLevel    // y = original mip count for texture    // z = desired on screen mip level    // w = loaded mip level    float4 mipInfo;    #endif&#125;;</code></pre></details><p>其实这里面也没有什么要具体说的，主要就是初始化之后需要用的到输入数据，就是要用到的一些方向向量，灯光颜色，环境光颜色等，解释一下不常见的函数。</p><ul><li><p><strong>GetWorldSpaceNormalizeViewDir</strong>：获取世界空间归一化的观察向量：</p><pre><code class="language-hlsl">  half3 GetWorldSpaceNormalizeViewDir(float3 positionWS)  &#123;      if (IsPerspectiveProjection())      &#123;          // Perspective          float3 V = GetCurrentViewPosition() - positionWS;          return half3(normalize(V));      &#125;      else      &#123;          // Orthographic          return half3(-GetViewForwardDir());      &#125;  &#125;  float3 GetCurrentViewPosition()  &#123;      // Currently we do not support Camera Relative Rendering so      // we simply return the _WorldSpaceCameraPos until then      return GetCameraPositionWS();      // We will replace the code above with this one once      // we start supporting Camera Relative Rendering      //#if defined(SHADERPASS) &amp;&amp; (SHADERPASS != SHADERPASS_SHADOWS)      //    return GetCameraPositionWS();      //#else      //    // This is a generic solution.      //    // However, for the primary camera, using &#39;_WorldSpaceCameraPos&#39; is better for cache locality,      //    // and in case we enable camera-relative rendering, we can statically set the position is 0.      //    return UNITY_MATRIX_I_V._14_24_34;      //#endif  &#125;  // Returns the forward (central) direction of the current view in the world space.  float3 GetViewForwardDir()  &#123;      float4x4 viewMat = GetWorldToViewMatrix();      return -viewMat[2].xyz;  &#125;</code></pre></li><li><p><strong>NormalizeNormalPerPixel</strong>：逐片元归一化法线：</p><pre><code class="language-hlsl">  half3 NormalizeNormalPerPixel(half3 normalWS)  &#123;  // With XYZ normal map encoding we sporadically sample normals with near-zero-length causing Inf/NaN  #if defined(UNITY_NO_DXT5nm) &amp;&amp; defined(_NORMALMAP)      return SafeNormalize(normalWS);  #else      return normalize(normalWS);  #endif  &#125;  float3 NormalizeNormalPerPixel(float3 normalWS)  &#123;  #if defined(UNITY_NO_DXT5nm) &amp;&amp; defined(_NORMALMAP)      return SafeNormalize(normalWS);  #else      return normalize(normalWS);  #endif  &#125;</code></pre></li><li><p><strong>REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR</strong>：需要阴影坐标：</p><pre><code class="language-hlsl">  #if !defined(_RECEIVE_SHADOWS_OFF)      #if defined(_MAIN_LIGHT_SHADOWS) || defined(_MAIN_LIGHT_SHADOWS_CASCADE) || defined(_MAIN_LIGHT_SHADOWS_SCREEN)          #define MAIN_LIGHT_CALCULATE_SHADOWS          #if defined(_MAIN_LIGHT_SHADOWS) || (defined(_MAIN_LIGHT_SHADOWS_SCREEN) &amp;&amp; !defined(_SURFACE_TYPE_TRANSPARENT))              #define REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR          #endif      #endif      #if defined(_ADDITIONAL_LIGHT_SHADOWS)          #define ADDITIONAL_LIGHT_CALCULATE_SHADOWS      #endif  #endif</code></pre></li><li><p><strong>InitializeInputDataFog</strong>：初始化InputData的雾效因子：</p><pre><code class="language-hlsl">  real InitializeInputDataFog(float4 positionWS, real vertFogFactor)  &#123;      real fogFactor = 0.0;  #if defined(_FOG_FRAGMENT)      #if (defined(FOG_LINEAR) || defined(FOG_EXP) || defined(FOG_EXP2))          // Compiler eliminates unused math --&gt; matrix.column_z * vec          float viewZ = -(mul(UNITY_MATRIX_V, positionWS).z);          // View Z is 0 at camera pos, remap 0 to near plane.          float nearToFarZ = max(viewZ - _ProjectionParams.y, 0);          fogFactor = ComputeFogFactorZ0ToFar(nearToFarZ);      #endif  #else      fogFactor = vertFogFactor;  #endif      return fogFactor;  &#125;</code></pre></li><li><p><strong>GetNormalizedScreenSpaceUV</strong>：获取屏幕空间归一化uv坐标：</p><pre><code class="language-hlsl">  float2 GetNormalizedScreenSpaceUV(float2 positionCS)  &#123;      float2 normalizedScreenSpaceUV = positionCS.xy * rcp(GetScaledScreenParams().xy);      TransformNormalizedScreenUV(normalizedScreenSpaceUV);      return normalizedScreenSpaceUV;  &#125;  float2 GetNormalizedScreenSpaceUV(float4 positionCS)  &#123;      return GetNormalizedScreenSpaceUV(positionCS.xy);  &#125;  void TransformNormalizedScreenUV(inout float2 uv)  &#123;      #if UNITY_UV_STARTS_AT_TOP      TransformScreenUV(uv, 1.0);      #endif  &#125;  void TransformScreenUV(inout float2 uv, float screenHeight)  &#123;      #if UNITY_UV_STARTS_AT_TOP      uv.y = screenHeight - (uv.y * _ScaleBiasRt.x + _ScaleBiasRt.y * screenHeight);      #endif  &#125;  void TransformScreenUV(inout float2 uv)  &#123;      #if UNITY_UV_STARTS_AT_TOP      TransformScreenUV(uv, GetScaledScreenParams().y);      #endif  &#125;</code></pre></li><li><p><strong>SAMPLE_SHADOWMASK</strong>：采样ShadowMask：</p><pre><code class="language-hlsl">  #if defined(SHADOWS_SHADOWMASK) &amp;&amp; defined(LIGHTMAP_ON)      #define SAMPLE_SHADOWMASK(uv) SAMPLE_TEXTURE2D_LIGHTMAP(SHADOWMASK_NAME, SHADOWMASK_SAMPLER_NAME, uv SHADOWMASK_SAMPLE_EXTRA_ARGS);  #elif !defined (LIGHTMAP_ON)      #define SAMPLE_SHADOWMASK(uv) unity_ProbesOcclusion;  #else      #define SAMPLE_SHADOWMASK(uv) half4(1, 1, 1, 1);  #endif</code></pre></li></ul><h2 id="顶点着色器LitPassVertex"><a href="#顶点着色器LitPassVertex" class="headerlink" title="顶点着色器LitPassVertex"></a>顶点着色器LitPassVertex</h2><pre><code class="language-hlsl">Varyings LitPassVertex(Attributes input)&#123;    Varyings output = (Varyings)0;    UNITY_SETUP_INSTANCE_ID(input);    UNITY_TRANSFER_INSTANCE_ID(input, output);    UNITY_INITIALIZE_VERTEX_OUTPUT_STEREO(output);    VertexPositionInputs vertexInput = GetVertexPositionInputs(input.positionOS.xyz);    // normalWS and tangentWS already normalize.    // this is required to avoid skewing the direction during interpolation    // also required for per-vertex lighting and SH evaluation    VertexNormalInputs normalInput = GetVertexNormalInputs(input.normalOS, input.tangentOS);    half3 vertexLight = VertexLighting(vertexInput.positionWS, normalInput.normalWS);    half fogFactor = 0;    #if !defined(_FOG_FRAGMENT)        fogFactor = ComputeFogFactor(vertexInput.positionCS.z);    #endif    output.uv = TRANSFORM_TEX(input.texcoord, _BaseMap);    // already normalized from normal transform to WS.    output.normalWS = normalInput.normalWS;#if defined(REQUIRES_WORLD_SPACE_TANGENT_INTERPOLATOR) || defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR)    real sign = input.tangentOS.w * GetOddNegativeScale();    half4 tangentWS = half4(normalInput.tangentWS.xyz, sign);#endif#if defined(REQUIRES_WORLD_SPACE_TANGENT_INTERPOLATOR)    output.tangentWS = tangentWS;#endif#if defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR)    half3 viewDirWS = GetWorldSpaceNormalizeViewDir(vertexInput.positionWS);    half3 viewDirTS = GetViewDirectionTangentSpace(tangentWS, output.normalWS, viewDirWS);    output.viewDirTS = viewDirTS;#endif    OUTPUT_LIGHTMAP_UV(input.staticLightmapUV, unity_LightmapST, output.staticLightmapUV);#ifdef DYNAMICLIGHTMAP_ON    output.dynamicLightmapUV = input.dynamicLightmapUV.xy * unity_DynamicLightmapST.xy + unity_DynamicLightmapST.zw;#endif    OUTPUT_SH(output.normalWS.xyz, output.vertexSH);#ifdef _ADDITIONAL_LIGHTS_VERTEX    output.fogFactorAndVertexLight = half4(fogFactor, vertexLight);#else    output.fogFactor = fogFactor;#endif#if defined(REQUIRES_WORLD_SPACE_POS_INTERPOLATOR)    output.positionWS = vertexInput.positionWS;#endif#if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)    output.shadowCoord = GetShadowCoord(vertexInput);#endif    output.positionCS = vertexInput.positionCS;    return output;&#125;</code></pre><p>&emsp;顶点着色器也没有需要详细解释的，主要还是看一些函数，顶点Shader的作用就是初始化我们的顶点输出结构体而已。</p><ul><li><p><strong>GetVertexPositionInputs</strong>：获取顶点位置信息函数：</p><pre><code class="language-hlsl">  VertexPositionInputs GetVertexPositionInputs(float3 positionOS)  &#123;      VertexPositionInputs input;      input.positionWS = TransformObjectToWorld(positionOS);      input.positionVS = TransformWorldToView(input.positionWS);      input.positionCS = TransformWorldToHClip(input.positionWS);      float4 ndc = input.positionCS * 0.5f;      input.positionNDC.xy = float2(ndc.x, ndc.y * _ProjectionParams.x) + ndc.w;      input.positionNDC.zw = input.positionCS.zw;      return input;  &#125;</code></pre><details><summary> <font color="#007ACC">🐍 VertexPositionInputs定义是？ </font></summary><pre><code class="language-hlsl">struct VertexPositionInputs&#123;    float3 positionWS; // World space position    float3 positionVS; // View space position    float4 positionCS; // Homogeneous clip space position    float4 positionNDC;// Homogeneous normalized device coordinates&#125;;</code></pre></details></li><li><p><strong>GetVertexNormalInputs</strong>：获取顶点法线信息函数：</p><pre><code class="language-hlsl">  VertexPositionInputs GetVertexPositionInputs(float3 positionOS)  &#123;      VertexPositionInputs input;      input.positionWS = TransformObjectToWorld(positionOS);      input.positionVS = TransformWorldToView(input.positionWS);      input.positionCS = TransformWorldToHClip(input.positionWS);      float4 ndc = input.positionCS * 0.5f;      input.positionNDC.xy = float2(ndc.x, ndc.y * _ProjectionParams.x) + ndc.w;      input.positionNDC.zw = input.positionCS.zw;      return input;  &#125;  VertexNormalInputs GetVertexNormalInputs(float3 normalOS)  &#123;      VertexNormalInputs tbn;      tbn.tangentWS = real3(1.0, 0.0, 0.0);      tbn.bitangentWS = real3(0.0, 1.0, 0.0);      tbn.normalWS = TransformObjectToWorldNormal(normalOS);      return tbn;  &#125;  VertexNormalInputs GetVertexNormalInputs(float3 normalOS, float4 tangentOS)  &#123;      VertexNormalInputs tbn;      // mikkts space compliant. only normalize when extracting normal at frag.      real sign = real(tangentOS.w) * GetOddNegativeScale();      tbn.normalWS = TransformObjectToWorldNormal(normalOS);      tbn.tangentWS = real3(TransformObjectToWorldDir(tangentOS.xyz));      tbn.bitangentWS = real3(cross(tbn.normalWS, float3(tbn.tangentWS))) * sign;      return tbn;  &#125;</code></pre><details><summary> <font color="#007ACC">🐍 VertexNormalInputs定义是？ </font></summary><pre><code class="language-hlsl">struct VertexNormalInputs&#123;    real3 tangentWS;    real3 bitangentWS;    float3 normalWS;&#125;;</code></pre></details></li><li><p><strong>VertexLighting</strong>：获取顶点灯光颜色：</p><pre><code class="language-hlsl">  half3 VertexLighting(float3 positionWS, half3 normalWS)  &#123;      half3 vertexLightColor = half3(0.0, 0.0, 0.0);  #ifdef _ADDITIONAL_LIGHTS_VERTEX      uint lightsCount = GetAdditionalLightsCount();      uint meshRenderingLayers = GetMeshRenderingLayer();      LIGHT_LOOP_BEGIN(lightsCount)          Light light = GetAdditionalLight(lightIndex, positionWS);  #ifdef _LIGHT_LAYERS      if (IsMatchingLightLayer(light.layerMask, meshRenderingLayers))  #endif      &#123;          half3 lightColor = light.color * light.distanceAttenuation;          vertexLightColor += LightingLambert(lightColor, light.direction, normalWS);      &#125;      LIGHT_LOOP_END  #endif      return vertexLightColor;  &#125;  half3 LightingLambert(half3 lightColor, half3 lightDir, half3 normal)  &#123;      half NdotL = saturate(dot(normal, lightDir));      return lightColor * NdotL;  &#125;</code></pre></li><li><p><strong>GetViewDirectionTangentSpace</strong>：获取切线空间视角方向：</p><pre><code class="language-hlsl">  // Return view direction in tangent space, make sure tangentWS.w is already multiplied by GetOddNegativeScale()  half3 GetViewDirectionTangentSpace(half4 tangentWS, half3 normalWS, half3 viewDirWS)  &#123;      // must use interpolated tangent, bitangent and normal before they are normalized in the pixel shader.      half3 unnormalizedNormalWS = normalWS;      const half renormFactor = 1.0 / length(unnormalizedNormalWS);      // use bitangent on the fly like in hdrp      // IMPORTANT! If we ever support Flip on double sided materials ensure bitangent and tangent are NOT flipped.      half crossSign = (tangentWS.w &gt; 0.0 ? 1.0 : -1.0); // we do not need to multiple GetOddNegativeScale() here, as it is done in vertex shader      half3 bitang = crossSign * cross(normalWS.xyz, tangentWS.xyz);      half3 WorldSpaceNormal = renormFactor * normalWS.xyz;       // we want a unit length Normal Vector node in shader graph      // to preserve mikktspace compliance we use same scale renormFactor as was used on the normal.      // This is explained in section 2.2 in &quot;surface gradient based bump mapping framework&quot;      half3 WorldSpaceTangent = renormFactor * tangentWS.xyz;      half3 WorldSpaceBiTangent = renormFactor * bitang;      half3x3 tangentSpaceTransform = half3x3(WorldSpaceTangent, WorldSpaceBiTangent, WorldSpaceNormal);      half3 viewDirTS = mul(tangentSpaceTransform, viewDirWS);      return viewDirTS;  &#125;</code></pre></li><li><p><strong>OUTPUT_LIGHTMAP_UV</strong>：获取LightMap的uv：</p><pre><code class="language-hlsl">  #if defined(LIGHTMAP_ON)      #define DECLARE_LIGHTMAP_OR_SH(lmName, shName, index) float2 lmName : TEXCOORD##index      #define OUTPUT_LIGHTMAP_UV(lightmapUV, lightmapScaleOffset, OUT) OUT.xy = lightmapUV.xy * lightmapScaleOffset.xy + lightmapScaleOffset.zw;      #define OUTPUT_SH(normalWS, OUT)  #else      #define DECLARE_LIGHTMAP_OR_SH(lmName, shName, index) half3 shName : TEXCOORD##index      #define OUTPUT_LIGHTMAP_UV(lightmapUV, lightmapScaleOffset, OUT)      #define OUTPUT_SH(normalWS, OUT) OUT.xyz = SampleSHVertex(normalWS)  #endif</code></pre></li></ul><h2 id="片元着色器LitPassFragment"><a href="#片元着色器LitPassFragment" class="headerlink" title="片元着色器LitPassFragment"></a>片元着色器LitPassFragment</h2><pre><code class="language-hlsl">void LitPassFragment(Varyings input, out half4 outColor : SV_Target0#ifdef _WRITE_RENDERING_LAYERS    , out float4 outRenderingLayers : SV_Target1#endif)&#123;    UNITY_SETUP_INSTANCE_ID(input);    UNITY_SETUP_STEREO_EYE_INDEX_POST_VERTEX(input);#if defined(_PARALLAXMAP)#if defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR)    half3 viewDirTS = input.viewDirTS;#else    half3 viewDirWS = GetWorldSpaceNormalizeViewDir(input.positionWS);    half3 viewDirTS = GetViewDirectionTangentSpace(input.tangentWS, input.normalWS, viewDirWS);#endif    ApplyPerPixelDisplacement(viewDirTS, input.uv);#endif    SurfaceData surfaceData;    InitializeStandardLitSurfaceData(input.uv, surfaceData);#ifdef LOD_FADE_CROSSFADE    LODFadeCrossFade(input.positionCS);#endif    InputData inputData;    InitializeInputData(input, surfaceData.normalTS, inputData);    SETUP_DEBUG_TEXTURE_DATA(inputData, input.uv, _BaseMap);#ifdef _DBUFFER    ApplyDecalToSurfaceData(input.positionCS, surfaceData, inputData);#endif    half4 color = UniversalFragmentPBR(inputData, surfaceData);    color.rgb = MixFog(color.rgb, inputData.fogCoord);    color.a = OutputAlpha(color.a, IsSurfaceTypeTransparent(_Surface));    outColor = color;#ifdef _WRITE_RENDERING_LAYERS    uint renderingLayers = GetMeshRenderingLayer();    outRenderingLayers = float4(EncodeMeshRenderingLayer(renderingLayers), 0, 0, 0);#endif&#125;</code></pre><p>&emsp;其实流程不多，可分为以下步骤：</p><ol><li><p>若有视差映射，对视差映射uv进行计算：</p><pre><code class="language-hlsl"> #if defined(_PARALLAXMAP) #if defined(REQUIRES_TANGENT_SPACE_VIEW_DIR_INTERPOLATOR)     half3 viewDirTS = input.viewDirTS; #else     half3 viewDirWS = GetWorldSpaceNormalizeViewDir(input.positionWS);     half3 viewDirTS = GetViewDirectionTangentSpace(input.tangentWS, input.normalWS, viewDirWS); #endif     ApplyPerPixelDisplacement(viewDirTS, input.uv); #endif</code></pre></li><li><p>初始化SurfaceData，也就是准备好贴图数据：</p><pre><code class="language-hlsl"> SurfaceData surfaceData; InitializeStandardLitSurfaceData(input.uv, surfaceData);</code></pre></li><li><p>若开启了LodGroup功能，计算LODFade：</p><pre><code class="language-hlsl"> #ifdef LOD_FADE_CROSSFADE     LODFadeCrossFade(input.positionCS); #endif</code></pre></li><li><p>初始化InputData，也就是处理顶点输入数据，准备之后所需要的方向向量，阴影等变量参数：</p><pre><code class="language-hlsl"> InputData inputData; InitializeInputData(input, surfaceData.normalTS, inputData);</code></pre></li><li><p>设置Debug贴图数据，这主要是用于Debug渲染的，实际自定义的时候可以不用管这个：</p><pre><code class="language-hlsl"> SETUP_DEBUG_TEXTURE_DATA(inputData, input.uv, _BaseMap);</code></pre></li><li><p>将贴花数据应用在表面数据上</p><pre><code class="language-hlsl"> #ifdef _DBUFFER     ApplyDecalToSurfaceData(input.positionCS, surfaceData, inputData); #endif</code></pre><p>具体代码如下：</p><pre><code class="language-hlsl"> void ApplyDecalToSurfaceData(float4 positionCS, inout SurfaceData surfaceData, inout InputData inputData) &#123; #ifdef _SPECULAR_SETUP     half metallic = 0;     ApplyDecal(positionCS,         surfaceData.albedo,         surfaceData.specular,         inputData.normalWS,         metallic,         surfaceData.occlusion,         surfaceData.smoothness); #else     half3 specular = 0;     ApplyDecal(positionCS,         surfaceData.albedo,         specular,         inputData.normalWS,         surfaceData.metallic,         surfaceData.occlusion,         surfaceData.smoothness); #endif &#125; void ApplyDecal(float4 positionCS,     inout half3 baseColor,     inout half3 specularColor,     inout half3 normalWS,     inout half metallic,     inout half occlusion,     inout half smoothness) &#123;     FETCH_DBUFFER(DBuffer, _DBufferTexture, int2(positionCS.xy));     DecalSurfaceData decalSurfaceData;     DECODE_FROM_DBUFFER(DBuffer, decalSurfaceData);     // using alpha compositing https://developer.nvidia.com/gpugems/GPUGems3/gpugems3_ch23.html, mean weight of 1 is neutral     // Note: We only test weight (i.e decalSurfaceData.xxx.w is &lt; 1.0) if it can save something     baseColor.xyz = baseColor.xyz * decalSurfaceData.baseColor.w + decalSurfaceData.baseColor.xyz; #if defined(_DBUFFER_MRT2) || defined(_DBUFFER_MRT3)     // Always test the normal as we can have decompression artifact     if (decalSurfaceData.normalWS.w &lt; 1.0)     &#123;         normalWS.xyz = normalize(normalWS.xyz * decalSurfaceData.normalWS.w + decalSurfaceData.normalWS.xyz);     &#125; #endif #if defined(_DBUFFER_MRT3) #ifdef _SPECULAR_SETUP     if (decalSurfaceData.MAOSAlpha.x &lt; 1.0)     &#123;         half3 decalSpecularColor = ComputeFresnel0((decalSurfaceData.baseColor.w &lt; 1.0) ? decalSurfaceData.baseColor.xyz : half3(1.0, 1.0, 1.0), decalSurfaceData.metallic, DEFAULT_SPECULAR_VALUE);         specularColor = specularColor * decalSurfaceData.MAOSAlpha + decalSpecularColor * (1.0f - decalSurfaceData.MAOSAlpha);     &#125; #else     metallic = metallic * decalSurfaceData.MAOSAlpha + decalSurfaceData.metallic; #endif     occlusion = occlusion * decalSurfaceData.MAOSAlpha + decalSurfaceData.occlusion;     smoothness = smoothness * decalSurfaceData.MAOSAlpha + decalSurfaceData.smoothness; #endif &#125;</code></pre></li><li><p>物理光照PBR（核心）：</p><pre><code class="language-hlsl"> half4 color = UniversalFragmentPBR(inputData, surfaceData);</code></pre><p>其核心代码如下：</p><pre><code class="language-hlsl"> half4 UniversalFragmentPBR(InputData inputData, SurfaceData surfaceData) &#123;     #if defined(_SPECULARHIGHLIGHTS_OFF)     bool specularHighlightsOff = true;     #else     bool specularHighlightsOff = false;     #endif     BRDFData brdfData;     // NOTE: can modify &quot;surfaceData&quot;...     InitializeBRDFData(surfaceData, brdfData);     #if defined(DEBUG_DISPLAY)     half4 debugColor;     if (CanDebugOverrideOutputColor(inputData, surfaceData, brdfData, debugColor))     &#123;         return debugColor;     &#125;     #endif     // Clear-coat calculation...     BRDFData brdfDataClearCoat = CreateClearCoatBRDFData(surfaceData, brdfData);     half4 shadowMask = CalculateShadowMask(inputData);     AmbientOcclusionFactor aoFactor = CreateAmbientOcclusionFactor(inputData, surfaceData);     uint meshRenderingLayers = GetMeshRenderingLayer();     Light mainLight = GetMainLight(inputData, shadowMask, aoFactor);     // NOTE: We don&#39;t apply AO to the GI here because it&#39;s done in the lighting calculation below...     MixRealtimeAndBakedGI(mainLight, inputData.normalWS, inputData.bakedGI);     LightingData lightingData = CreateLightingData(inputData, surfaceData);     lightingData.giColor = GlobalIllumination(brdfData, brdfDataClearCoat, surfaceData.clearCoatMask,                                             inputData.bakedGI, aoFactor.indirectAmbientOcclusion, inputData.positionWS,                                             inputData.normalWS, inputData.viewDirectionWS, inputData.normalizedScreenSpaceUV); #ifdef _LIGHT_LAYERS     if (IsMatchingLightLayer(mainLight.layerMask, meshRenderingLayers)) #endif     &#123;         lightingData.mainLightColor = LightingPhysicallyBased(brdfData, brdfDataClearCoat,                                                             mainLight,                                                             inputData.normalWS, inputData.viewDirectionWS,                                                             surfaceData.clearCoatMask, specularHighlightsOff);     &#125;     #if defined(_ADDITIONAL_LIGHTS)     uint pixelLightCount = GetAdditionalLightsCount();     #if USE_FORWARD_PLUS     for (uint lightIndex = 0; lightIndex &lt; min(URP_FP_DIRECTIONAL_LIGHTS_COUNT, MAX_VISIBLE_LIGHTS); lightIndex++)     &#123;         FORWARD_PLUS_SUBTRACTIVE_LIGHT_CHECK         Light light = GetAdditionalLight(lightIndex, inputData, shadowMask, aoFactor); #ifdef _LIGHT_LAYERS         if (IsMatchingLightLayer(light.layerMask, meshRenderingLayers)) #endif         &#123;             lightingData.additionalLightsColor += LightingPhysicallyBased(brdfData, brdfDataClearCoat, light,                                                                         inputData.normalWS, inputData.viewDirectionWS,                                                                         surfaceData.clearCoatMask, specularHighlightsOff);         &#125;     &#125;     #endif     LIGHT_LOOP_BEGIN(pixelLightCount)         Light light = GetAdditionalLight(lightIndex, inputData, shadowMask, aoFactor); #ifdef _LIGHT_LAYERS         if (IsMatchingLightLayer(light.layerMask, meshRenderingLayers)) #endif         &#123;             lightingData.additionalLightsColor += LightingPhysicallyBased(brdfData, brdfDataClearCoat, light,                                                                         inputData.normalWS, inputData.viewDirectionWS,                                                                         surfaceData.clearCoatMask, specularHighlightsOff);         &#125;     LIGHT_LOOP_END     #endif     #if defined(_ADDITIONAL_LIGHTS_VERTEX)     lightingData.vertexLightingColor += inputData.vertexLighting * brdfData.diffuse;     #endif #if REAL_IS_HALF     // Clamp any half.inf+ to HALF_MAX     return min(CalculateFinalColor(lightingData, surfaceData.alpha), HALF_MAX); #else     return CalculateFinalColor(lightingData, surfaceData.alpha); #endif &#125;</code></pre><p>我们一个一个来看。</p><ol><li><p>首先是定义了一个bool变量来决定是不是关闭了高光：</p><pre><code class="language-hlsl">  #if defined(_SPECULARHIGHLIGHTS_OFF)  bool specularHighlightsOff = true;  #else  bool specularHighlightsOff = false;  #endif</code></pre></li><li><p>然后定义BRDFData变量并且初始化它：</p><pre><code class="language-hlsl">  BRDFData brdfData;  // NOTE: can modify &quot;surfaceData&quot;...  InitializeBRDFData(surfaceData, brdfData);</code></pre><details><summary> <font color="#007ACC">🐍 BRDFData的定义是？ </font></summary><pre><code class="language-hlsl">    struct BRDFData    &#123;        half3 albedo;        half3 diffuse;        half3 specular;        half reflectivity;        half perceptualRoughness;        half roughness;        half roughness2;        half grazingTerm;        // We save some light invariant BRDF terms so we don&#39;t have to recompute        // them in the light loop. Take a look at DirectBRDF function for detailed explaination.        half normalizationTerm;     // roughness * 4.0 + 2.0        half roughness2MinusOne;    // roughness^2 - 1.0    &#125;;</code></pre></details><details><summary> <font color="#007ACC">🐍 InitializeBRDFData函数详解 </font></summary><pre><code class="language-hlsl">    // Initialize BRDFData for material, managing both specular and metallic setup using shader keyword _SPECULAR_SETUP.    inline void InitializeBRDFData(half3 albedo, half metallic, half3 specular, half smoothness, inout half alpha, out BRDFData outBRDFData)    &#123;    #ifdef _SPECULAR_SETUP        half reflectivity = ReflectivitySpecular(specular);        half oneMinusReflectivity = half(1.0) - reflectivity;        half3 brdfDiffuse = albedo * oneMinusReflectivity;        half3 brdfSpecular = specular;    #else        half oneMinusReflectivity = OneMinusReflectivityMetallic(metallic);        half reflectivity = half(1.0) - oneMinusReflectivity;        half3 brdfDiffuse = albedo * oneMinusReflectivity;        half3 brdfSpecular = lerp(kDieletricSpec.rgb, albedo, metallic);    #endif        InitializeBRDFDataDirect(albedo, brdfDiffuse, brdfSpecular, reflectivity, oneMinusReflectivity, smoothness, alpha, outBRDFData);    &#125;    inline void InitializeBRDFData(inout SurfaceData surfaceData, out BRDFData brdfData)    &#123;        InitializeBRDFData(surfaceData.albedo, surfaceData.metallic, surfaceData.specular, surfaceData.smoothness, surfaceData.alpha, brdfData);    &#125;</code></pre><blockquote><p>有两个函数，我们主要就看代码多的实现细节，也就是第一个函数。<br>  在第一个函数中我们可以看到一开始就先区分了金属工作流和高光工作流：</p></blockquote><pre><code class="language-hlsl">    #ifdef _SPECULAR_SETUP        half reflectivity = ReflectivitySpecular(specular);        half oneMinusReflectivity = half(1.0) - reflectivity;        half3 brdfDiffuse = albedo * oneMinusReflectivity;        half3 brdfSpecular = specular;    #else        half oneMinusReflectivity = OneMinusReflectivityMetallic(metallic);        half reflectivity = half(1.0) - oneMinusReflectivity;        half3 brdfDiffuse = albedo * oneMinusReflectivity;        half3 brdfSpecular = lerp(kDieletricSpec.rgb, albedo, metallic);    #endif</code></pre><ul><li><p>当开启 <strong>_SPECULAR_SETUP</strong> 关键字的时候启用高光工作流：</p><ul><li><strong>reflectivity</strong>：镜面反射率，由高光颜色获得：</li></ul><pre><code class="language-hlsl">    half ReflectivitySpecular(half3 specular)    &#123;    #if defined(SHADER_API_GLES)        return specular.r; // Red channel - because most metals are either monochrome or with redish/yellowish tint    #else        return Max3(specular.r, specular.g, specular.b);    #endif    &#125;</code></pre><ul><li><strong>oneMinusReflectivity</strong>:漫反射率，与镜面反射率相加为1。</li><li><strong>brdfDiffuse</strong>：物理的漫反射颜色分量，镜面反射率越低，漫反射越高。</li><li><strong>brdfSpecular</strong>:镜面反射颜色的分量，即高光色。</li></ul></li><li><p>当关闭 <strong>_SPECULAR_SETUP</strong> 关键字的时候启用金属工作流：</p><ul><li><strong>oneMinusReflectivity</strong>:漫反射率，由金属度插值介电质常数与0，当金属度越高，也就越能反射高光，漫反射率也就越低，反之金属度越低，漫反射率也就越高，越能反射物体本身的颜色，但最高为$1-0.04&#x3D;0.96$，其中$0.04$为最低介电质常数，为经验值：</li></ul><pre><code class="language-hlsl">    half OneMinusReflectivityMetallic(half metallic)    &#123;        // We&#39;ll need oneMinusReflectivity, so        //   1-reflectivity = 1-lerp(dielectricSpec, 1, metallic) = lerp(1-dielectricSpec, 0, metallic)        // store (1-dielectricSpec) in kDielectricSpec.a, then        //   1-reflectivity = lerp(alpha, 0, metallic) = alpha + metallic*(0 - alpha) =        //                  = alpha - metallic * alpha        half oneMinusDielectricSpec = kDielectricSpec.a;        return oneMinusDielectricSpec - metallic * oneMinusDielectricSpec;    &#125;</code></pre><ul><li><strong>reflectivity</strong>：镜面反射率，与漫反射率相加为1。</li><li><strong>brdfDiffuse</strong>：物理的漫反射颜色分量，镜面反射率越低，漫反射越高。</li><li><strong>brdfSpecular</strong>:镜面反射颜色的分量，即高光色，因为是金属度调节，当金属度越高，那么高光反射的颜色越接近基础色（比如黄金的高光也是金色），而金属度越低，高光颜色越接近中性灰。</li></ul>  <details><summary> <font color="#007ACC">🐍 为什么两种工作流的计算看起来不太一样？ </font></summary><p>  首先我们要明确一个前提：材质的总反射能量&#x3D;漫反射能量+镜面反射能量。<br>  这两种工作流的差异，本质是用不同的参数体系来分配这两部分能量，最终实现相同的物理效果，但适配不同的美术调优习惯。<br>  正因为体系不同，导致了反射分量计算逻辑的差异：</p><ul><li>金属度工作流：用 <strong>metallic</strong> 参数来约束漫反射与镜面反射的分配（金属无漫反射）。</li><li>高光工作流：用 <strong>specular</strong> 的亮度来分配能量（高光越亮，镜面反射率越高，漫反射越少）。<blockquote><p>bedfDiffuse计算差异：</p></blockquote></li></ul><p>  金属度工作流：<code>brdfDiffuse = albedo × lerp(0.96, 0.0, metallic);</code></p><ul><li>强制金属无漫反射（真实世界中金属的漫反射趋近于 0，所有反射都是镜面反射），而非金属保留 96% 的漫反射能量（符合 4% 基础镜面反射率的物理规律）。</li></ul><p>  高光工作流：<code>brdfDiffuse = albedo × Luminance(specular);</code></p><ul><li>让美术直接通过高光色控制反射能量分配，高光色越亮（反射率越高），漫反射就越少，无需区分 “金属 &#x2F; 非金属”，适配需要精准调高光颜色的场景（比如彩色高光的塑料玩具）。<blockquote><p>bedfSpecular计算差异：</p></blockquote></li></ul><p>  金属度工作流：<code>brdfSpecular = lerp(kDieletricSpec.rgb, albedo, metallic);</code></p><ul><li>非金属（ <strong>metallic&#x3D;0</strong> ）时，镜面反射使用中性灰，符合物理，不需要美术手动调节；金属（ <strong>metallic&#x3D;1</strong> ）时，镜面反射色等于基础色（真实金属的镜面反射就是自身颜色，比如黄金的高光也是金色）；半金属线性插值过渡，保证视觉自然。</li></ul><p>  高光工作流：<code>brdfDiffuse = albedo × Luminance(specular);</code></p><ul><li>把高光色的控制权完全交给美术，想让高光偏红就设 <code>specular=(1,0,0)</code>，想让高光偏蓝就设 <code>specular=(0,0,1)</code>，无需受 “金属 &#x2F; 非金属” 的物理规则约束，适配非写实风格（卡通、奇幻材质）或需要特殊高光的写实场景（比如带彩色高光的车漆）。    </details></li></ul></li></ul><p>  然后就是初始化直接光BRDF：<strong>InitializeBRDFDataDirect</strong>：</p><pre><code class="language-hlsl">    inline void InitializeBRDFDataDirect(half3 albedo, half3 diffuse, half3 specular, half reflectivity, half oneMinusReflectivity, half smoothness, inout half alpha, out BRDFData outBRDFData)    &#123;        outBRDFData = (BRDFData)0;        outBRDFData.albedo = albedo;        outBRDFData.diffuse = diffuse;        outBRDFData.specular = specular;        outBRDFData.reflectivity = reflectivity;        outBRDFData.perceptualRoughness = PerceptualSmoothnessToPerceptualRoughness(smoothness);        outBRDFData.roughness           = max(PerceptualRoughnessToRoughness(outBRDFData.perceptualRoughness), HALF_MIN_SQRT);        outBRDFData.roughness2          = max(outBRDFData.roughness * outBRDFData.roughness, HALF_MIN);        outBRDFData.grazingTerm         = saturate(smoothness + reflectivity);        outBRDFData.normalizationTerm   = outBRDFData.roughness * half(4.0) + half(2.0);        outBRDFData.roughness2MinusOne  = outBRDFData.roughness2 - half(1.0);        // Input is expected to be non-alpha-premultiplied while ROP is set to pre-multiplied blend.        // We use input color for specular, but (pre-)multiply the diffuse with alpha to complete the standard alpha blend equation.        // In shader: Cs&#39; = Cs * As, in ROP: Cs&#39; + Cd(1-As);        // i.e. we only alpha blend the diffuse part to background (transmittance).        #if defined(_ALPHAPREMULTIPLY_ON)            // TODO: would be clearer to multiply this once to accumulated diffuse lighting at end instead of the surface property.            outBRDFData.diffuse *= alpha;        #endif    &#125;    // Legacy: do not call, will not correctly initialize albedo property.    inline void InitializeBRDFDataDirect(half3 diffuse, half3 specular, half reflectivity, half oneMinusReflectivity, half smoothness, inout half alpha, out BRDFData outBRDFData)    &#123;        InitializeBRDFDataDirect(half3(0.0, 0.0, 0.0), diffuse, specular, reflectivity, oneMinusReflectivity, smoothness, alpha, outBRDFData);    &#125;</code></pre><p>  这里就没有什么多说的，这里就是给BRDFData数据赋值为后面PBR计算提供数据。<br>  </details></p></li><li><p>接着有一个Debug声明函数，这个函数就是为了Debug用的，不作详解：</p><pre><code class="language-hlsl">  #if defined(DEBUG_DISPLAY)  half4 debugColor;  if (CanDebugOverrideOutputColor(inputData, surfaceData, brdfData, debugColor))  &#123;      return debugColor;  &#125;  #endif</code></pre></li><li><p>然后跳过清漆BRDF计算：</p><pre><code class="language-hlsl">  BRDFData brdfDataClearCoat = CreateClearCoatBRDFData(surfaceData, brdfData);</code></pre></li><li><p>计算ShdowMask:</p><pre><code class="language-hlsl">  half4 shadowMask = CalculateShadowMask(inputData);</code></pre><details><summary> <font color="#007ACC">🐍 CalculateShadowMask的定义是？ </font></summary><pre><code class="language-hlsl">half4 CalculateShadowMask(InputData inputData)&#123;    // To ensure backward compatibility we have to avoid using shadowMask input, as it is not present in older shaders    #if defined(SHADOWS_SHADOWMASK) &amp;&amp; defined(LIGHTMAP_ON)    half4 shadowMask = inputData.shadowMask;    #elif !defined (LIGHTMAP_ON)    half4 shadowMask = unity_ProbesOcclusion;    #else    half4 shadowMask = half4(1, 1, 1, 1);    #endif    return shadowMask;&#125;</code></pre></details></li><li><p>创建AO因子 <strong>AmbientOcclusionFactor</strong> ：</p><pre><code class="language-hlsl">  AmbientOcclusionFactor aoFactor = CreateAmbientOcclusionFactor(inputData, surfaceData);</code></pre><details><summary> <font color="#007ACC">🐍 AmbientOcclusionFactor的定义是？ </font></summary><pre><code class="language-hlsl">struct AmbientOcclusionFactor&#123;    half indirectAmbientOcclusion;    half directAmbientOcclusion;&#125;;</code></pre><blockquote><p>包含直接光AO和间接光AO。</p></blockquote></details><details><summary> <font color="#007ACC">🐍 CreateAmbientOcclusionFactor的定义是？ </font></summary><pre><code class="language-hlsl">AmbientOcclusionFactor CreateAmbientOcclusionFactor(float2 normalizedScreenSpaceUV, half occlusion)&#123;    AmbientOcclusionFactor aoFactor = GetScreenSpaceAmbientOcclusion(normalizedScreenSpaceUV);    aoFactor.indirectAmbientOcclusion = min(aoFactor.indirectAmbientOcclusion, occlusion);    return aoFactor;&#125;AmbientOcclusionFactor CreateAmbientOcclusionFactor(InputData inputData, SurfaceData surfaceData)&#123;    return CreateAmbientOcclusionFactor(inputData.normalizedScreenSpaceUV, surfaceData.occlusion);&#125;</code></pre><blockquote><p>可以看到函数时先计算了屏幕空间AO，而简接光AO是取屏幕空间AO和遮蔽图计算出的occlusion中的最小值。</p></blockquote></details><details><summary> <font color="#007ACC">🐍 GetScreenSpaceAmbientOcclusion的定义是？ </font></summary><pre><code class="language-hlsl">half SampleAmbientOcclusion(float2 normalizedScreenSpaceUV)&#123;    float2 uv = UnityStereoTransformScreenSpaceTex(normalizedScreenSpaceUV);    return half(SAMPLE_TEXTURE2D_X(_ScreenSpaceOcclusionTexture, sampler_ScreenSpaceOcclusionTexture, uv).x);&#125;AmbientOcclusionFactor GetScreenSpaceAmbientOcclusion(float2 normalizedScreenSpaceUV)&#123;    AmbientOcclusionFactor aoFactor;    #if defined(_SCREEN_SPACE_OCCLUSION) &amp;&amp; !defined(_SURFACE_TYPE_TRANSPARENT)        float ssao = saturate(SampleAmbientOcclusion(normalizedScreenSpaceUV) + (1.0 - _AmbientOcclusionParam.x));        aoFactor.indirectAmbientOcclusion = ssao;        aoFactor.directAmbientOcclusion = lerp(half(1.0), ssao, _AmbientOcclusionParam.w);    #else        aoFactor.directAmbientOcclusion = half(1.0);        aoFactor.indirectAmbientOcclusion = half(1.0);    #endif    #if defined(DEBUG_DISPLAY)    switch(_DebugLightingMode)    &#123;        case DEBUGLIGHTINGMODE_LIGHTING_WITHOUT_NORMAL_MAPS:            aoFactor.directAmbientOcclusion = 0.5;            aoFactor.indirectAmbientOcclusion = 0.5;            break;        case DEBUGLIGHTINGMODE_LIGHTING_WITH_NORMAL_MAPS:            aoFactor.directAmbientOcclusion *= 0.5;            aoFactor.indirectAmbientOcclusion *= 0.5;            break;    &#125;    #endif    return aoFactor;&#125;</code></pre><blockquote><p>首先我们忽略 <strong>DEBUG_DISPLAY</strong> 预定义中的代码，这部分是Debug的，可以不管。然后可以看到，返回数值有两种情况：</p></blockquote><ol><li>定义了屏幕空间AO预定义标签 <strong>_SCREEN_SPACE_OCCLUSION</strong> (也就是SSAO)并且不是透明物体：<br><code>hlsl  float ssao = saturate(SampleAmbientOcclusion(normalizedScreenSpaceUV) + (1.0 - _AmbientOcclusionParam.x));  aoFactor.indirectAmbientOcclusion = ssao;  aoFactor.directAmbientOcclusion = lerp(half(1.0), ssao, _AmbientOcclusionParam.w); </code><br>- <strong>SampleAmbientOcclusion</strong>：采样SSAO贴图（ <strong>_ScreenSpaceOcclusionTexture</strong> ）, <strong>UnityStereoTransformScreenSpaceTex</strong>函数看起来花里胡哨，其实就是返回一个uv而已：<pre><code class="language-hlsl">#if defined(UNITY_SINGLE_PASS_STEREO)    float2 TransformStereoScreenSpaceTex(float2 uv, float w)    &#123;        // TODO: RVS support can be added here, if Universal decides to support it        float4 scaleOffset = unity_StereoScaleOffset[unity_StereoEyeIndex];        return uv.xy * scaleOffset.xy + scaleOffset.zw * w;    &#125;    float2 UnityStereoTransformScreenSpaceTex(float2 uv)    &#123;        return TransformStereoScreenSpaceTex(saturate(uv), 1.0);    &#125;#else    #define UnityStereoTransformScreenSpaceTex(uv) uv#endif // defined(UNITY_SINGLE_PASS_STEREO)</code></pre></li></ol><pre><code>- **_AmbientOcclusionParam**:  ```hlsl  // x: SSAO Enabled/Disabled (Needed for situations when OFF keyword is stripped out but feature disabled in runtime)  // yz are currently unused  // w: directLightStrength  half4 _AmbientOcclusionParam;  ```</code></pre></li><li><p>获取物体的RenderLayer，用于对渲染计算的优化：</p><pre><code class="language-hlsl">  uint meshRenderingLayers = GetMeshRenderingLayer();</code></pre></li><li><p>获取主光源数据：</p><pre><code class="language-hlsl">  Light mainLight = GetMainLight(inputData, shadowMask, aoFactor);</code></pre></li><li><p>烘焙光照与实时阴影融合：</p><pre><code class="language-hlsl"> MixRealtimeAndBakedGI(mainLight, inputData.normalWS, inputData.bakedGI);</code></pre> <details><summary> <font color="#007ACC">🐍 MixRealtimeAndBakedGI的定义是？ </font></summary>      <pre><code class="language-hlsl">void MixRealtimeAndBakedGI(inout Light light, half3 normalWS, inout half3 bakedGI)&#123;#if defined(LIGHTMAP_ON) &amp;&amp; defined(_MIXED_LIGHTING_SUBTRACTIVE)    bakedGI = SubtractDirectMainLightFromLightmap(light, normalWS, bakedGI);#endif&#125;</code></pre> </details>  <details><summary> <font color="#007ACC">🐍 SubtractDirectMainLightFromLightmap的定义是？ </font></summary><pre><code class="language-hlsl">half3 SubtractDirectMainLightFromLightmap(Light mainLight, half3 normalWS, half3 bakedGI)&#123;    // Let&#39;s try to make realtime shadows work on a surface, which already contains    // baked lighting and shadowing from the main sun light.    // Summary:    // 1) Calculate possible value in the shadow by subtracting estimated light contribution from the places occluded by realtime shadow:    //      a) preserves other baked lights and light bounces    //      b) eliminates shadows on the geometry facing away from the light    // 2) Clamp against user defined ShadowColor.    // 3) Pick original lightmap value, if it is the darkest one.    // 1) Gives good estimate of illumination as if light would&#39;ve been shadowed during the bake.    // We only subtract the main direction light. This is accounted in the contribution term below.    half shadowStrength = GetMainLightShadowStrength();    half contributionTerm = saturate(dot(mainLight.direction, normalWS));    half3 lambert = mainLight.color * contributionTerm;    half3 estimatedLightContributionMaskedByInverseOfShadow = lambert * (1.0 - mainLight.shadowAttenuation);    half3 subtractedLightmap = bakedGI - estimatedLightContributionMaskedByInverseOfShadow;    // 2) Allows user to define overall ambient of the scene and control situation when realtime shadow becomes too dark.    half3 realtimeShadow = max(subtractedLightmap, _SubtractiveShadowColor.xyz);    realtimeShadow = lerp(bakedGI, realtimeShadow, shadowStrength);    // 3) Pick darkest color    return min(bakedGI, realtimeShadow);&#125;</code></pre>  </details>    <details><summary> <font color="#007ACC">🐍 为什么要混合？ </font></summary><p>  该函数通过从烘焙光照中扣除 “本应被实时阴影遮挡的主光源贡献”，并结合自定义的阴影颜色做限制，最终选择 “烘焙值” 和 “修正后实时阴影值” 中更暗的结果，实现烘焙光照与实时阴影的自然融合。</p><blockquote><p>核心解决的问题：<br>如果场景烘焙了主光源（比如太阳光）的光照，但运行时需要动态物体投射实时阴影到烘焙表面，直接叠加实时阴影会导致 “烘焙亮区被实时阴影完全覆盖”（过暗），该函数通过精准扣除主光源贡献，保留烘焙的间接光 &#x2F; 其他光源，让实时阴影更自然。</p></blockquote><p>  AI解释如下：</p><pre><code class="language-hlsl">    half3 SubtractDirectMainLightFromLightmap(Light mainLight, half3 normalWS, half3 bakedGI)    &#123;        // ========== 步骤1：计算“应扣除的主光源贡献”（模拟实时阴影遮挡） ==========        // 1.1 获取用户设置的主光源阴影强度（0=无实时阴影，1=完全生效）        half shadowStrength = GetMainLightShadowStrength();                // 1.2 计算“表面朝向与主光源的夹角贡献”：仅向光面（dot&gt;0）才扣除主光源，背光面不处理        // saturate：将结果限制在0~1（避免dot为负时出现错误扣除）        half contributionTerm = saturate(dot(mainLight.direction, normalWS));                // 1.3 计算主光源对当前像素的兰伯特光照贡献（基础漫反射模型）        half3 lambert = mainLight.color * contributionTerm;                // 1.4 计算“本应被实时阴影遮挡的主光源贡献”：        // (1 - shadowAttenuation)：阴影区域该值=1（完全遮挡），无阴影区域=0（不遮挡）        // 最终：阴影区 = 主光源贡献 × 1（扣除全部），无阴影区 = 主光源贡献 × 0（不扣除）        half3 estimatedLightContributionMaskedByInverseOfShadow = lambert * (1.0 - mainLight.shadowAttenuation);                // 1.5 从烘焙光照中扣除上述贡献：得到“模拟烘焙时就有实时阴影”的光照值        // 保留了烘焙的间接光、其他光源，仅扣除主光源的阴影部分        half3 subtractedLightmap = bakedGI - estimatedLightContributionMaskedByInverseOfShadow;        // ========== 步骤2：限制阴影过暗 + 混合阴影强度 ==========        // 2.1 用用户自定义阴影色限制：避免扣除后过黑（比如阴影区不能比_SubtractiveShadowColor更暗）        half3 realtimeShadow = max(subtractedLightmap, _SubtractiveShadowColor.xyz);                // 2.2 按阴影强度混合“原始烘焙值”和“修正后阴影值”：shadowStrength=0时完全用烘焙值        realtimeShadow = lerp(bakedGI, realtimeShadow, shadowStrength);        // ========== 步骤3：选择更暗的值（保证阴影只暗不亮，符合物理） ==========        // min：如果修正后的阴影值比烘焙值亮，就用烘焙值；否则用阴影值        return min(bakedGI, realtimeShadow);    &#125;</code></pre>  </details></li><li><p>创建灯光数据 <strong>LightingData</strong> 变量：</p></li></ol><pre><code class="language-hlsl">  LightingData lightingData = CreateLightingData(inputData, surfaceData);</code></pre>   <details><summary> <font color="#007ACC">🐍 CreateLightingData的定义是？ </font></summary><pre><code class="language-hlsl">  LightingData CreateLightingData(InputData inputData, SurfaceData surfaceData)  &#123;      LightingData lightingData;      lightingData.giColor = inputData.bakedGI;      lightingData.emissionColor = surfaceData.emission;      lightingData.vertexLightingColor = 0;      lightingData.mainLightColor = 0;      lightingData.additionalLightsColor = 0;      return lightingData;  &#125;</code></pre>   </details><ol start="11"><li>GI光计算：</li></ol><pre><code class="language-hlsl">  lightingData.giColor = GlobalIllumination(brdfData, brdfDataClearCoat, surfaceData.clearCoatMask,                                          inputData.bakedGI, aoFactor.indirectAmbientOcclusion, inputData.positionWS,                                          inputData.normalWS, inputData.viewDirectionWS, inputData.normalizedScreenSpaceUV);</code></pre>   <details><summary> <font color="#007ACC">🐍 GlobalIllumination的详解 </font></summary><pre><code class="language-hlsl">  half3 GlobalIllumination(BRDFData brdfData, BRDFData brdfDataClearCoat, float clearCoatMask,      half3 bakedGI, half occlusion, float3 positionWS,      half3 normalWS, half3 viewDirectionWS, float2 normalizedScreenSpaceUV)  &#123;      half3 reflectVector = reflect(-viewDirectionWS, normalWS);      half NoV = saturate(dot(normalWS, viewDirectionWS));      half fresnelTerm = Pow4(1.0 - NoV);      half3 indirectDiffuse = bakedGI;      half3 indirectSpecular = GlossyEnvironmentReflection(reflectVector, positionWS, brdfData.perceptualRoughness, 1.0h, normalizedScreenSpaceUV);      half3 color = EnvironmentBRDF(brdfData, indirectDiffuse, indirectSpecular, fresnelTerm);      if (IsOnlyAOLightingFeatureEnabled())      &#123;          color = half3(1,1,1); // &quot;Base white&quot; for AO debug lighting mode      &#125;  #if defined(_CLEARCOAT) || defined(_CLEARCOATMAP)      half3 coatIndirectSpecular = GlossyEnvironmentReflection(reflectVector, positionWS, brdfDataClearCoat.perceptualRoughness, 1.0h, normalizedScreenSpaceUV);      // TODO: &quot;grazing term&quot; causes problems on full roughness      half3 coatColor = EnvironmentBRDFClearCoat(brdfDataClearCoat, clearCoatMask, coatIndirectSpecular, fresnelTerm);      // Blend with base layer using khronos glTF recommended way using NoV      // Smooth surface &amp; &quot;ambiguous&quot; lighting      // NOTE: fresnelTerm (above) is pow4 instead of pow5, but should be ok as blend weight.      half coatFresnel = kDielectricSpec.x + kDielectricSpec.a * fresnelTerm;      return (color * (1.0 - coatFresnel * clearCoatMask) + coatColor) * occlusion;  #else      return color * occlusion;  #endif  &#125;</code></pre><blockquote><p>我们可以看到，函数先获取菲涅尔项，Unity使用的是$Pow4$，因为在表面数据计算中我们就已经采样了烘焙光，所以能直接获得间接光的漫反射就是 <strong>surfaceData.bakedGi</strong>，而间接光的高光则采用了环境光反射高光函数计算获得。获得间接光的漫反射颜色和高光颜色后，使用BRDF计算求取获得brdf颜色，最后乘以间接光遮罩区域就得到了间接光颜色。</p>   <details><summary> <font color="#007ACC">🐍 GlossyEnvironmentReflection的定义是？ </font></summary></blockquote><pre><code class="language-hlsl">  half3 GlossyEnvironmentReflection(half3 reflectVector, half perceptualRoughness, half occlusion)  &#123;  #if !defined(_ENVIRONMENTREFLECTIONS_OFF)      half3 irradiance;      half mip = PerceptualRoughnessToMipmapLevel(perceptualRoughness);      half4 encodedIrradiance = half4(SAMPLE_TEXTURECUBE_LOD(unity_SpecCube0, samplerunity_SpecCube0, reflectVector, mip));      irradiance = DecodeHDREnvironment(encodedIrradiance, unity_SpecCube0_HDR);      return irradiance * occlusion;  #else      return _GlossyEnvironmentColor.rgb * occlusion;  #endif // _ENVIRONMENTREFLECTIONS_OFF  &#125;</code></pre>   </details>   </details><ol start="12"><li>匹配主灯光层级并进行物理光照计算：</li></ol><pre><code class="language-hlsl">  if (IsMatchingLightLayer(mainLight.layerMask, meshRenderingLayers))  &#123;      lightingData.mainLightColor = LightingPhysicallyBased(brdfData, brdfDataClearCoat,                                                          mainLight,                                                          inputData.normalWS, inputData.viewDirectionWS,                                                          surfaceData.clearCoatMask, specularHighlightsOff);  &#125;</code></pre><p>   这里的物理光照是最核心的地方，我们会在下面详解。</p><ol start="13"><li>匹配额外灯光层级并进行物理光照计算：</li></ol><pre><code class="language-hlsl">  #if defined(_ADDITIONAL_LIGHTS)      uint pixelLightCount = GetAdditionalLightsCount();      #if USE_CLUSTERED_LIGHTING      for (uint lightIndex = 0; lightIndex &lt; min(_AdditionalLightsDirectionalCount, MAX_VISIBLE_LIGHTS); lightIndex++)      &#123;          Light light = GetAdditionalLight(lightIndex, inputData, shadowMask, aoFactor);          if (IsMatchingLightLayer(light.layerMask, meshRenderingLayers))          &#123;              lightingData.additionalLightsColor += LightingPhysicallyBased(brdfData, brdfDataClearCoat, light,                                                                          inputData.normalWS, inputData.viewDirectionWS,                                                                          surfaceData.clearCoatMask, specularHighlightsOff);          &#125;      &#125;      #endif      LIGHT_LOOP_BEGIN(pixelLightCount)          Light light = GetAdditionalLight(lightIndex, inputData, shadowMask, aoFactor);          if (IsMatchingLightLayer(light.layerMask, meshRenderingLayers))          &#123;              lightingData.additionalLightsColor += LightingPhysicallyBased(brdfData, brdfDataClearCoat, light,                                                                          inputData.normalWS, inputData.viewDirectionWS,                                                                          surfaceData.clearCoatMask, specularHighlightsOff);          &#125;      LIGHT_LOOP_END      #endif</code></pre>   <details><summary> <font color="#007ACC">🐍 USE_CLUSTERED_LIGHTING是什么宏定义？ </font></summary><pre><code class="language-hlsl">  #if defined(_CLUSTERED_RENDERING)  #define _ADDITIONAL_LIGHTS 1  #undef _ADDITIONAL_LIGHTS_VERTEX  #define USE_CLUSTERED_LIGHTING 1  #else  #define USE_CLUSTERED_LIGHTING 0  #endif</code></pre><p>   集群化光照（Clustered Lighting）是一种优化多光源渲染的技术，能大幅提升大量光源场景下的性能。<br>   </details></p><ol start="14"><li>叠加顶点着色光照：</li></ol><pre><code class="language-hlsl">  #if defined(_ADDITIONAL_LIGHTS_VERTEX)  lightingData.vertexLightingColor += inputData.vertexLighting * brdfData.diffuse;  #endif</code></pre><ol start="15"><li>计算最终颜色并返回：</li></ol><pre><code class="language-hlsl">  return CalculateFinalColor(lightingData, surfaceData.alpha);</code></pre><pre><code class="language-hlsl">  half3 CalculateLightingColor(LightingData lightingData, half3 albedo)  &#123;      half3 lightingColor = 0;      if (IsOnlyAOLightingFeatureEnabled())      &#123;          return lightingData.giColor; // Contains white + AO      &#125;      if (IsLightingFeatureEnabled(DEBUGLIGHTINGFEATUREFLAGS_GLOBAL_ILLUMINATION))      &#123;          lightingColor += lightingData.giColor;      &#125;      if (IsLightingFeatureEnabled(DEBUGLIGHTINGFEATUREFLAGS_MAIN_LIGHT))      &#123;          lightingColor += lightingData.mainLightColor;      &#125;      if (IsLightingFeatureEnabled(DEBUGLIGHTINGFEATUREFLAGS_ADDITIONAL_LIGHTS))      &#123;          lightingColor += lightingData.additionalLightsColor;      &#125;      if (IsLightingFeatureEnabled(DEBUGLIGHTINGFEATUREFLAGS_VERTEX_LIGHTING))      &#123;          lightingColor += lightingData.vertexLightingColor;      &#125;      lightingColor *= albedo;      if (IsLightingFeatureEnabled(DEBUGLIGHTINGFEATUREFLAGS_EMISSION))      &#123;          lightingColor += lightingData.emissionColor;      &#125;      return lightingColor;  &#125;  half4 CalculateFinalColor(LightingData lightingData, half alpha)  &#123;      half3 finalColor = CalculateLightingColor(lightingData, 1);      return half4(finalColor, alpha);  &#125;</code></pre><p>   这里还是比较容易理解的，就是判断每个功能有没有开启，开启了就叠加光的颜色。   </p></li><li><p>混合雾效：</p></li></ol><pre><code class="language-hlsl">color.rgb = MixFog(color.rgb, inputData.fogCoord);</code></pre><ol start="9"><li>根据类型设置透明度：</li></ol><pre><code class="language-hlsl">color.a = OutputAlpha(color.a, _Surface);half OutputAlpha(half outputAlpha, half surfaceType = half(0.0))&#123;    return surfaceType == 1 ? outputAlpha : half(1.0);&#125;</code></pre><ol start="10"><li>返回颜色。</li></ol><h3 id="物理光照LightingPhysicallyBased详解"><a href="#物理光照LightingPhysicallyBased详解" class="headerlink" title="物理光照LightingPhysicallyBased详解"></a>物理光照LightingPhysicallyBased详解</h3><p>&emsp;URP有五个构造函数，一个主函数，现附上五个构造函数代码：</p><pre><code class="language-hlsl">half3 LightingPhysicallyBased(BRDFData brdfData, BRDFData brdfDataClearCoat, Light light, half3 normalWS, half3 viewDirectionWS, half clearCoatMask, bool specularHighlightsOff)&#123;    return LightingPhysicallyBased(brdfData, brdfDataClearCoat, light.color, light.direction, light.distanceAttenuation * light.shadowAttenuation, normalWS, viewDirectionWS, clearCoatMask, specularHighlightsOff);&#125;// Backwards compatibilityhalf3 LightingPhysicallyBased(BRDFData brdfData, Light light, half3 normalWS, half3 viewDirectionWS)&#123;    #ifdef _SPECULARHIGHLIGHTS_OFF    bool specularHighlightsOff = true;#else    bool specularHighlightsOff = false;#endif    const BRDFData noClearCoat = (BRDFData)0;    return LightingPhysicallyBased(brdfData, noClearCoat, light, normalWS, viewDirectionWS, 0.0, specularHighlightsOff);&#125;half3 LightingPhysicallyBased(BRDFData brdfData, half3 lightColor, half3 lightDirectionWS, half lightAttenuation, half3 normalWS, half3 viewDirectionWS)&#123;    Light light;    light.color = lightColor;    light.direction = lightDirectionWS;    light.distanceAttenuation = lightAttenuation;    light.shadowAttenuation   = 1;    return LightingPhysicallyBased(brdfData, light, normalWS, viewDirectionWS);&#125;half3 LightingPhysicallyBased(BRDFData brdfData, Light light, half3 normalWS, half3 viewDirectionWS, bool specularHighlightsOff)&#123;    const BRDFData noClearCoat = (BRDFData)0;    return LightingPhysicallyBased(brdfData, noClearCoat, light, normalWS, viewDirectionWS, 0.0, specularHighlightsOff);&#125;half3 LightingPhysicallyBased(BRDFData brdfData, half3 lightColor, half3 lightDirectionWS, half lightAttenuation, half3 normalWS, half3 viewDirectionWS, bool specularHighlightsOff)&#123;    Light light;    light.color = lightColor;    light.direction = lightDirectionWS;    light.distanceAttenuation = lightAttenuation;    light.shadowAttenuation   = 1;    return LightingPhysicallyBased(brdfData, light, viewDirectionWS, specularHighlightsOff, specularHighlightsOff);&#125;</code></pre><p>&emsp;核心还是要看主函数：</p><pre><code class="language-hlsl">half3 LightingPhysicallyBased(BRDFData brdfData, BRDFData brdfDataClearCoat,    half3 lightColor, half3 lightDirectionWS, half lightAttenuation,    half3 normalWS, half3 viewDirectionWS,    half clearCoatMask, bool specularHighlightsOff)&#123;    half NdotL = saturate(dot(normalWS, lightDirectionWS));    half3 radiance = lightColor * (lightAttenuation * NdotL);    half3 brdf = brdfData.diffuse;#ifndef _SPECULARHIGHLIGHTS_OFF    [branch] if (!specularHighlightsOff)    &#123;        brdf += brdfData.specular * DirectBRDFSpecular(brdfData, normalWS, lightDirectionWS, viewDirectionWS);#if defined(_CLEARCOAT) || defined(_CLEARCOATMAP)        // Clear coat evaluates the specular a second timw and has some common terms with the base specular.        // We rely on the compiler to merge these and compute them only once.        half brdfCoat = kDielectricSpec.r * DirectBRDFSpecular(brdfDataClearCoat, normalWS, lightDirectionWS, viewDirectionWS);            // Mix clear coat and base layer using khronos glTF recommended formula            // https://github.com/KhronosGroup/glTF/blob/master/extensions/2.0/Khronos/KHR_materials_clearcoat/README.md            // Use NoV for direct too instead of LoH as an optimization (NoV is light invariant).            half NoV = saturate(dot(normalWS, viewDirectionWS));            // Use slightly simpler fresnelTerm (Pow4 vs Pow5) as a small optimization.            // It is matching fresnel used in the GI/Env, so should produce a consistent clear coat blend (env vs. direct)            half coatFresnel = kDielectricSpec.x + kDielectricSpec.a * Pow4(1.0 - NoV);        brdf = brdf * (1.0 - clearCoatMask * coatFresnel) + brdfCoat * clearCoatMask;#endif // _CLEARCOAT    &#125;#endif // _SPECULARHIGHLIGHTS_OFF    return brdf * radiance;&#125;</code></pre><p>&emsp;清漆的我们可以不看，那代码就可以简化成：</p><pre><code class="language-hlsl">half3 LightingPhysicallyBased(BRDFData brdfData, BRDFData brdfDataClearCoat,    half3 lightColor, half3 lightDirectionWS, half lightAttenuation,    half3 normalWS, half3 viewDirectionWS,    half clearCoatMask, bool specularHighlightsOff)&#123;    half NdotL = saturate(dot(normalWS, lightDirectionWS));    half3 radiance = lightColor * (lightAttenuation * NdotL);    half3 brdf = brdfData.diffuse;#ifndef _SPECULARHIGHLIGHTS_OFF    [branch] if (!specularHighlightsOff)    &#123;        brdf += brdfData.specular * DirectBRDFSpecular(brdfData, normalWS, lightDirectionWS, viewDirectionWS);    &#125;#endif // _SPECULARHIGHLIGHTS_OFF    return brdf * radiance;&#125;</code></pre><ol><li><p>首先就是获取入射光的出射亮度radiance，这可以在上一篇URP-PBR里找到理论详解：</p><pre><code class="language-hlsl">    half NdotL = saturate(dot(normalWS, lightDirectionWS));    half3 radiance = lightColor * (lightAttenuation * NdotL);</code></pre><p> &emsp;简单来说，辐照度也就是我们的albedo，光的albedo也就是光照颜色，因为点光和一些其余光源会有光照衰弱的特性（随距离增加能量减少），那么我们就获得了辐照度：<code>float3 irradiance = light.color * light.distanceAttenuation * light.shadowAttenuation;</code>。光的辐射亮度与辐照度之间是正比关系，这个数值就是物体表面法线与光线的余弦，那么我们自然而然就获取了光照的辐照亮度了。</p></li><li><p>brdf漫反射项没什么要说的，就是根据金属度或者高光求出的物体漫反射项。</p></li><li><p>brdf高光项比较复杂，传统的就是CookTorrance的模型，不过Unity对其做了一些优化和曲线拟合，和传统的PBR有所不同：</p><pre><code class="language-hlsl">brdf += brdfData.specular * DirectBRDFSpecular(brdfData, normalWS, lightDirectionWS, viewDirectionWS);</code></pre><p> 其核心函数就是<strong>DirectBRDFSpecular</strong>：</p><pre><code class="language-hlsl">// Computes the scalar specular term for Minimalist CookTorrance BRDF// NOTE: needs to be multiplied with reflectance f0, i.e. specular color to completehalf DirectBRDFSpecular(BRDFData brdfData, half3 normalWS, half3 lightDirectionWS, half3 viewDirectionWS)&#123;    float3 lightDirectionWSFloat3 = float3(lightDirectionWS);    float3 halfDir = SafeNormalize(lightDirectionWSFloat3 + float3(viewDirectionWS));    float NoH = saturate(dot(float3(normalWS), halfDir));    half LoH = half(saturate(dot(lightDirectionWSFloat3, halfDir)));    // GGX Distribution multiplied by combined approximation of Visibility and Fresnel    // BRDFspec = (D * V * F) / 4.0    // D = roughness^2 / ( NoH^2 * (roughness^2 - 1) + 1 )^2    // V * F = 1.0 / ( LoH^2 * (roughness + 0.5) )    // See &quot;Optimizing PBR for Mobile&quot; from Siggraph 2015 moving mobile graphics course    // https://community.arm.com/events/1155    // Final BRDFspec = roughness^2 / ( NoH^2 * (roughness^2 - 1) + 1 )^2 * (LoH^2 * (roughness + 0.5) * 4.0)    // We further optimize a few light invariant terms    // brdfData.normalizationTerm = (roughness + 0.5) * 4.0 rewritten as roughness * 4.0 + 2.0 to a fit a MAD.    float d = NoH * NoH * brdfData.roughness2MinusOne + 1.00001f;    half LoH2 = LoH * LoH;    half specularTerm = brdfData.roughness2 / ((d * d) * max(0.1h, LoH2) * brdfData.normalizationTerm);    // On platforms where half actually means something, the denominator has a risk of overflow    // clamp below was added specifically to &quot;fix&quot; that, but dx compiler (we convert bytecode to metal/gles)    // sees that specularTerm have only non-negative terms, so it skips max(0,..) in clamp (leaving only min(100,...))#if REAL_IS_HALF    specularTerm = specularTerm - HALF_MIN;    // Update: Conservative bump from 100.0 to 1000.0 to better match the full float specular look.    // Roughly 65504.0 / 32*2 == 1023.5,    // or HALF_MAX / ((mobile) MAX_VISIBLE_LIGHTS * 2),    // to reserve half of the per light range for specular and half for diffuse + indirect + emissive.    specularTerm = clamp(specularTerm, 0.0, 1000.0); // Prevent FP16 overflow on mobiles#endif    return specularTerm;&#125;</code></pre><blockquote><p>&emsp;我们首先可以注意到注释中的<strong>D</strong>项目，它就是<strong>GGX</strong>中的<strong>D</strong>项计算，只是没有除以$\pi$，这是Unity故意去掉的，让整体画面看起来亮一些。</p><p>&emsp;<strong>V*F</strong>也就是<strong>G*F</strong>，这是拟合的近似公式:$\frac{1}{max((l·h)^2,0.1)(roughness+0.5)}$.</p><p>&emsp;在官方介绍的文档中$V &#x3D; \frac{G(n·v,n·l,roughness)}{(n·v)(n·l)}$。<strong>G</strong>用的就是$G_{sksm} &#x3D; \frac{(n·l)(n·v)}{lerp(roughness^2,1,(l·h)^2)}$，因此$V &#x3D; \frac{1}{lerp(roughness,1,(l·h)^2)}$.</p><p>&emsp;$F &#x3D; (1-(l·h))^5$.<br>&emsp;那么$V·F &#x3D; \frac{(1-(l·h)^5)}{(l·h)^2(1-roughness^2)+roughness^2}$.</p><p>&emsp;当(l·h)接近1的时候，曲线会呈现平坦状态，当l与h角度接近90度也就是为0时候，曲线倾斜度非常剧烈，光线掠过表面并几乎与相机方向相反照射，斜率会迅速增加。<br>&emsp;本来这样的优化就是对移动端进行操作的，Unity又进行了魔改成最后的曲线：$(V·F) &#x3D; \frac{1}{(l·h)^2 * (roughness + 0.5)}$，这样的曲线变化率变得更高了，也导致看起来的高光很亮，但在移动端看起来还是可以的，这样的优化极大增加了渲染的效率，所以Unity内置的并不算是完全物理的，有很多近似的地方在里面。</p></blockquote> <details> <summary>  <font color="#007ACC">🐍 为什么会推导出G_sksm这样的函数出来呢？ </font> </summary><p> &emsp;参考连接: <a href="https://community.arm.com/cfs-file/__key/communityserver-blogs-components-weblogfiles/00-00-00-20-66/siggraph2015_2D00_mmg_2D00_renaldas_2D00_notes.pdf">https://community.arm.com/cfs-file/__key/communityserver-blogs-components-weblogfiles/00-00-00-20-66/siggraph2015_2D00_mmg_2D00_renaldas_2D00_notes.pdf</a><br> &emsp;原因是：&amp;GGX&amp;本身就是Smith模型：&amp;G_{Smith} &#x3D; \frac{(n·l)(n·v)}{lerp(k,1,(n·l))lerp(k,1,(n·v))}&amp;，因为&amp;V&amp;和&amp;G&amp;的关系是提取了&amp;f_{cook-torrance}&amp;的分母并进行了约分，公式如下：<br> $$<br> \begin{align}<br> V_{Smith} &amp;&#x3D; \frac{G_{Smith}}{(n·l)(n·v)}\\<br> &amp; &#x3D; \frac{(n·l)(n·v)}{lerp(k,1,(n·l))lerp(k,1,(n·v))(n·l)(n·v)}\\<br> &amp; &#x3D; \frac{1}{lerp(k,1,(n·l))lerp(k,1,(n·v))}<br> \end{align}<br> $$<br> &emsp;而有一种廉价的近似方法KSK，它原本的公式应该为：$G_{ksk原版}\frac{2(n·h)^2}{(l·h)(v·h)}$，但Unity对移动端进行了优化，$G_{ksk优化}&#x3D;\frac{(n·l)(n·v)}{(l·h)^2}$，保留原本的ksk模型可以在PC上更加高效的物理计算，我们这里并没有处理。因此$V_{ksk} &#x3D; \frac{1}{(l·h)^2}$。<br> &emsp;但是这样的模型缺少了粗糙度控制，后面<strong>J.Hable</strong>对其进行了改造，则变成了$V_{sksm} &#x3D; \frac{1}{(l·h)^2(1-roughness^2)+roughness^2}$ ，所以这也是$G_{sksm}$的由来。</p> <div class="note warning flat"><p>注意：公式推导的roughness与代码中的roughnes不一样，代码中的roughness是Untiy做了预处理，是公式推导的roughness的平法。</p></div> </details></li><li><p>计算完高光项，k_s乘以镜面反射系数+k_d，最后再乘以灯光辐射亮度，就是最终输出的颜色。</p></li></ol>]]></content>
      
      
      <categories>
          
          <category> Shader </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Shader </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>URP-PBR</title>
      <link href="/undefined/bb6f3960.html"/>
      <url>/undefined/bb6f3960.html</url>
      
        <content type="html"><![CDATA[<div class="note warning flat"><p>注意：本文使用的Unity版本号为：2022.3.63f2c1，不同版本所含有的URPShader库可能有所不同，请注意辨别。</p></div><p>参考连接 <a href="https://bdffzi-blog.netlify.app/posts/2656087290?random=iusot2rhaza">https://bdffzi-blog.netlify.app/posts/2656087290?random=iusot2rhaza</a></p><h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p><strong>PBR</strong>，全称：<strong>Physically-Based Rendering</strong>，即基于物理的渲染，旨在通过物理属性模拟光线与物体表面的交互，以实现更真实的视觉效果。其核心思想是利用能量守恒定律和微表面理论，通过基础颜色、金属度、粗糙度等物理参数来还原真实材质的光学特性。</p><hr><h2 id="光"><a href="#光" class="headerlink" title="光"></a>光</h2><h3 id="光的特性"><a href="#光的特性" class="headerlink" title="光的特性"></a>光的特性</h3><ul><li>可传递：光可以通过介质进行传递</li><li>可叠加：光的能量可以直接相加进行合成，大部分情况下光是由多种不同类型的光进行组合而成的。</li></ul><h3 id="光的分类"><a href="#光的分类" class="headerlink" title="光的分类"></a>光的分类</h3><ul><li>直接光：是光源打到物体后反射到人眼的光。</li><li>间接光：是光源打到其他物体，其他物体反射光，反射光再打到当前物体后反射到人眼的光，也叫做环境光。</li><li>自发光：物体自身发出的光，如火焰。</li></ul><h3 id="光的传递方式"><a href="#光的传递方式" class="headerlink" title="光的传递方式"></a>光的传递方式</h3><ul><li>反射光：光碰到介质后返回原介质的光。</li><li>透射光：光穿过介质后从另一端射出的光。</li><li>自发光：物体本身作为光源射入眼睛的光。</li></ul><h3 id="光的反射方式"><a href="#光的反射方式" class="headerlink" title="光的反射方式"></a>光的反射方式</h3><ul><li>漫反射：光再物体表面或内部多次弹射后反射进眼睛的光。</li><li>镜面反射：光在物体表面直接反射进眼睛的光。</li></ul><h3 id="光的透射方式"><a href="#光的透射方式" class="headerlink" title="光的透射方式"></a>光的透射方式</h3><ul><li>漫透射：光在穿过介质过程中多次弹射后从介质另一端射入眼睛的光。</li><li>镜面透射：光在穿过介质过程中，没有额外弹射，从介质另一段射入眼睛的光。</li></ul><h3 id="光的反射位置"><a href="#光的反射位置" class="headerlink" title="光的反射位置"></a>光的反射位置</h3><ul><li>次表面：入射光和出射光位置不同，如皮肤的次表面反射。</li><li>非次表面：入射光与出射光位置相同，如漫反射、镜面反射。</li></ul><h3 id="光物理中的一些常用变量"><a href="#光物理中的一些常用变量" class="headerlink" title="光物理中的一些常用变量"></a>光物理中的一些常用变量</h3><ul><li><strong>float3 n</strong>：法线方向</li><li><strong>float3 v</strong>：视线方向</li><li><strong>float3 l</strong>：光线方向</li><li><strong>float3 h</strong>：半角向量</li></ul><pre><code class="language-hlsl">h = normalize(v+l);</code></pre><ul><li><strong>half3 albedo</strong>：反照率，物体反射光照的比例，通俗名称叫颜色，在材质中就是物体本身的颜色，如采样物体的贴图颜色。</li><li><strong>float metallic</strong>：金属度，控制漫反射与镜面反射的比例。</li><li><strong>float smoothness</strong>：光滑度，控制物体表面的光滑程度，对镜面反射影响很大。</li><li><strong>float roughness</strong>：粗糙度，粗糙度和光滑度可以互相转换。</li></ul><pre><code class="language-hlsl">roughness = max(HALF_MIN_SQRT, pow(1 - smoothness, 2));smoothness = 1.0-sqrt(roughness);</code></pre><div class="note warning flat"><p>注意：这里的roughness并不是我们属性值0~1的那个roughness，而是那个roughness的平方，Unity把它作为roughness是为了方便后续计算而已。  </p></div><blockquote><p>如果按照逻辑，应该是：<br><code>roughness = 1.0-smoothness;</code><br><code>smoothness = 1.0 - roughness;</code><br>$\alpha &#x3D; roughness*roughness$<br>光照计算中存在光滑度和粗糙度两种工作流，UnityURP中PBR代码使用的是光滑度，而DDC一般使用的是粗糙度。<br>PBR工作流又分为<strong>金属度-粗糙度</strong>工作流和<strong>高光-光泽度</strong>工作流，具体可以参考连接 <a href="https://blog.csdn.net/weixin_29232121/article/details/158633169">https://blog.csdn.net/weixin_29232121/article/details/158633169</a></p></blockquote><hr><h2 id="辐射度量学"><a href="#辐射度量学" class="headerlink" title="辐射度量学"></a>辐射度量学</h2><p>参考连接 <a href="https://tadazly.github.io/pbr-book/4_Radiometry_Spectra_and_Color/4_1_Radiometry.html">https://tadazly.github.io/pbr-book/4_Radiometry_Spectra_and_Color/4_1_Radiometry.html</a><br>辐射度量学是对物理渲染而提出的学科，我们并不需要对它们进行深入了解，有一个概念就可以了。</p><ul><li><p>辐射能（<strong>radiant energy</strong>）[$Q$]：起点是能量，单位为焦耳（$J$）。</p><blockquote><p>光源发射光子，每个光子具有特定的波长并携带特定的能量。波长为$\lambda$的光子携带能量为：$Q&#x3D;\frac{hc}{\lambda}$</p><p>其中$c$是光速：$299,472,458m&#x2F;s$，而$h$是普朗克常数，$h≈6.626×10^{−34}m^2kg&#x2F;s$</p></blockquote></li><li><p>辐射通量（<strong>radiant flux</strong>）[$\phi$]：单位为焦耳&#x2F;秒（$J&#x2F;s$），或者更为常见的瓦特（$W$），单位时间释放、反射、透射或者接收的能量。</p><blockquote><p>例如给定一个光，此光在一小时内发出的辐射能$Q&#x3D;200,200J$，如果在一小时内始终发出相同的能量，我们可以得到光源的通量为：$\phi&#x3D;200,000J&#x2F;3600s≈55.6W$。</p><p>相反，给定通量，我们可以在一段时间范围内进行积分，来计算总能量：$Q&#x3D;\int_{t_0}^{t_1}\phi(t)dt$。</p></blockquote></li><li><p>辐照度（<strong>irradiance</strong>）[$E$]：单位为$W&#x2F;m^2$，给定一个有限的面积$A$，我们可以定义该区域的平均功率密度为：$E&#x3D;\frac{\phi}{A}$。</p><blockquote><p>辐照度方程还可以帮助我们理解<strong>朗伯定律</strong>的起源，该定律指出，到达表面的光能量与光线方向和法线方向之间的夹角的余弦成正比。</p><p>考虑一个面积为$A$、辐射通量为$\phi$的光源正在照射一个表面。</p><p>如果光线垂直照射表面，那么接收光线的表面面积$A_1&#x3D;A$，在$A_1$内部的任何点的辐照度为：$E_1&#x3D;\frac{\phi}{A}$。</p><p>如果光线与表面有一定角度，则接收光的表面的面积就会变大，如果$A$很小，则接收辐射通量的面积$A_2$大约是$A_2&#x3D;\frac{A}{cos\theta}$，对于$A_2$内部的点，辐照度此时为：$E_2&#x3D;\frac{\phi cos\theta}{A}$。</p></blockquote></li><li><p>辐射强度（<strong>intensity</strong>）[$I$]：单位$W&#x2F;sr$，强度描述了光的分布方向，但它仅对点光源有意义。</p><blockquote><p>现在考虑一个发光光子的无穷小光源，如果我们把光源放于单位球体的中心点，我们就可以计算发射功率的角密度，强度就是这个值。</p><p>在整个球体的方向上，$I&#x3D;\frac{\phi}{4\pi}$。</p><p>如之前对辐射通量与辐射能之间的关系所说，我们一样可以通过积分还原功率。</p><p>给定强度作为方向的函数$I(\omega)$，我们可以在有限的方向集$\Omega$上进行积分还原功率：$\phi&#x3D;\int_{\Omega}I(\omega)d\omega$。</p></blockquote></li><li><p>辐射亮度（<strong>radiance</strong>）[$L$]：单位为$W&#x2F;m^2sr$，辐射亮度是单位面积单位立体角的通量密度，它的定义为：$L&#x3D;\frac{d^2\phi}{d\omega dA^n}$。</p><blockquote><p>辐照度为我们提供了在某一点$p$处的微分功率与微分面积的比值，但它们并未区分功率的分布方向，它的定义为：$L(p,\omega)&#x3D;\lim_{\Delta\omega \to 0}\frac{\Delta E_\omega(p)}{\Delta \omega}&#x3D;\frac{dE_\omega(p)}{d\omega}$。</p><p>$E_\omega$表示垂直于方向$\omega$的表面辐照度，换句话说，辐射亮度并不是相对于$p$所处的表面上入射的辐照度来测量的，实际上这种测量区域的改变有助于在辐射亮度的定义中消除<strong>朗伯定律</strong>中的$cos\theta$因子。</p><p>其中$dA^n$是$dA$在垂直于$\omega$的假设表面上的投影。</p></blockquote></li></ul><p>以上的概念其实在实际的计算中，我们只需要考虑<strong>获取辐照度</strong>和<strong>计算辐射亮度</strong>就行，这个过程也不是一定必须物理的，只是单纯用它们的名字和关系来表示一些计算中的光照参数。<br>也就是说在实际计算中，我们使用的<strong>irradiance</strong>和计算的<strong>radiance</strong>也许不一定就是我们上述概念所定义的计算，因为在实时计算引擎中，绝对依照物理计算是不存在的，不论是出于对性能、实现、配置等因素的考虑。<br>其中<strong>辐射亮度</strong>是基于<strong>辐照度</strong>计算的，这两者分别代表<strong>出射光每单位的总光强</strong>和<strong>入射光每单位（微分立体角）的总光强</strong>。<br>可想而知，如果两者面积总量一样，根据能量守恒，两者的值应该是相等的。但实际情况中，由于角度倾斜的原因，入射光的截面积和接收光的表面积是不一定对等的，所以反射时每单位面积的光强就会被均分。</p><blockquote><p>具体而言，在计算中,它们的关系与法线有关，公式如下：</p></blockquote><pre><code class="language-hlsl">float radiance = saturate(dot(n,l))*irradiance;</code></pre><hr><h2 id="光照分布函数"><a href="#光照分布函数" class="headerlink" title="光照分布函数"></a>光照分布函数</h2><p>光照分布函数被分为3类：</p><ul><li>BRDF：双向反射分布函数（描述反射现象）</li><li>BTDF：双向透射分布函数（描述透射现象）</li><li>BSDF：双向散射分布函数（BRDF+BTDF）</li></ul><p>上述分布函数基本就是漫反射和镜面反射，反射和透射的组合：</p><ul><li>漫反射：低频模糊，光线柔和。</li><li>镜面反射：高频清晰，能形成清晰的光源画面。</li></ul><h3 id="反射系数"><a href="#反射系数" class="headerlink" title="反射系数"></a>反射系数</h3><p>反射系数决定了物体反射光的权重，比如漫反射系数，镜面反射系数。</p><h4 id="漫反射系数"><a href="#漫反射系数" class="headerlink" title="漫反射系数"></a>漫反射系数</h4><p>漫反射是光照射到物体，经过多次弹射再反射回眼睛的光，因为多次弹射导致光能量的消失，入射信息大幅丢失，就会产生均匀柔和的效果。</p><h5 id="Lambert"><a href="#Lambert" class="headerlink" title="Lambert"></a>Lambert</h5><pre><code class="language-hlsl">float diffuseTerm = saturate(dot(n,l));</code></pre><h5 id="Half-Lambert"><a href="#Half-Lambert" class="headerlink" title="Half-Lambert"></a>Half-Lambert</h5><ul><li><code>float b</code>：亮度，影响背面光面亮度，越低越暗，越高越亮，当为0.5时就是经典的半兰伯特模型</li></ul><pre><code class="language-hlsl">float diffuseTerm = saturate(dot(n,l) * (1-b) + b);</code></pre><h4 id="镜面反射系数"><a href="#镜面反射系数" class="headerlink" title="镜面反射系数"></a>镜面反射系数</h4><h5 id="Phong"><a href="#Phong" class="headerlink" title="Phong"></a>Phong</h5><ul><li><code>float s</code>：光泽度，越大光斑越小。</li><li><code>float b</code>：亮度，越大光斑越亮。</li></ul><pre><code class="language-hlsl">float specularTerm = pow(saturate(dot(reflect(-l,n),v)), s) * b;</code></pre><h5 id="Blinn-Phong"><a href="#Blinn-Phong" class="headerlink" title="Blinn-Phong"></a>Blinn-Phong</h5><p>这是在Phong基础上改进的光照模型，采用半角向量<strong>h</strong>进行计算。</p><pre><code class="language-hlsl">float3 h = normalize(v + l);float specularTerm = pow(saturate(dot(n,h)), s) * b;</code></pre><h4 id="GGX"><a href="#GGX" class="headerlink" title="GGX"></a>GGX</h4><p>采用半角向量进行计算，省去“光泽度”和“亮度”，改用一种基于01范围的光滑度或粗糙度来控制（两者可以相互转化），其光斑效果更加真实可控。<br>该高光可以满足能量守恒，其光斑越大亮度越低，光斑越小时亮度越高。</p><pre><code class="language-hlsl">float a = roughness*roughnessfloat a2 = a * a;float specularTerm = a2 / pow(1.0001 + (a2 - 1) * pow(saturate(dot(n,h)), 2), 2)；</code></pre><ul><li>粗糙度越大时，视角权重越小，光斑越分散；</li><li>分子分母都受粗糙度权重影响，但分母的变化速度约等于分子的平方，故粗糙度较小时，其强度成指数变大。</li></ul><h4 id="PBR-Blinn-Phong"><a href="#PBR-Blinn-Phong" class="headerlink" title="PBR Blinn-Phong"></a>PBR Blinn-Phong</h4><p>其效果特意实现与GGX等PBR镜射光接近，因此可以自由替换，同时不影响 PBR 效果。</p><pre><code class="language-hlsl">float specularTerm = (1 / a2) * pow(saturate(dot(n,h)), max(2 / a2 - 2, 0.0001));</code></pre><details><summary>   <font color="#007ACC">🐍 有了GGX为什么还要Blinn-Phong? </font></summary><blockquote><p>虽然 Blinn-Phng 可能没有 GGX 那样物理正确，但其优点在于易于改造，也因此兼容性强。其中的基数部分可以任意替换成其他内容还依旧可用。<br>例如著名的头发高光，kk模型，就是把 Blinn-Phong 中的法线计算项换成了切线计算，结果依旧可用。这种兼容性，对 PBR Blinn-Phong 同样有效，所以 Blinn 模型并没有过时，其依然是各种特殊光照场景下的全能选手。</p></blockquote></details><hr><h2 id="双向反射分布函数-BRDF-CookTorrance"><a href="#双向反射分布函数-BRDF-CookTorrance" class="headerlink" title="双向反射分布函数-BRDF-CookTorrance"></a>双向反射分布函数-BRDF-CookTorrance</h2><p>参考文章 <a href="https://graphicscompendium.com/gamedev/15-pbr">https://graphicscompendium.com/gamedev/15-pbr</a><br>参考文章 <a href="https://zhuanlan.zhihu.com/p/152226698">https://zhuanlan.zhihu.com/p/152226698</a></p><div class="note info flat"><p>1982年，罗伯特·库克和肯尼斯·托兰斯发表了一个反射模型，该模型比Phong模型和Blin-Phong更加准确地反映了光反射率的物理现实，至今仍在广泛使用。<br>该方法具有通用性，它有三个“可插拔”的组件，可以选择其它方程来替代。它本身也有效表示多种材料，而像Blin-Phong的BRDF则实际上只适合表示塑料和某些金属（尽管这一点有争议）。</p></div><p>实时渲染中的PBR方程：</p><center>$L_o=\int_{\Omega}{}f_rL_icos\theta_i d\omega_i$</center><center>$f_{r} = k_{d}f_{lambert}+k_{s}f_{cook-toorance}$</center>> 其中$k_d$，$k_s$分别指入射光线中被漫反射的比例以及镜面反射的比例，$f_{lambert}$指代漫反射的BRDF，$f_{cook-torrance}$指代镜面反射的BRDF。<h3 id="漫反射项推导"><a href="#漫反射项推导" class="headerlink" title="漫反射项推导"></a>漫反射项推导</h3><p>漫反射会均匀向每个方向反射，因此漫反射的BRDF一定是一个常数。假设入射光是均匀且遍布整个半球方向，可以得到如下方程：</p><center>$$\begin{align}L_o(\omega_o) &= \int{H^2}{}f_rL_i(\omega_i)cos\theta_i d\omega_i\\\\&= f_rL_i\int{H^2}{}cos\theta_i d\omega_i\\\\&= \pi f_rL_i\end{align}$$</center><p>由于假设入射光是均匀且遍布整个半球方向的，所以与$L_i$的方向无关，且$L_i &#x3D; L_o$，约去两边的$L_o$：$f_r&#x3D;\frac{1}{\pi}$。<br>这里没有考虑能量被吸收，所以将反射率albedo考虑进来后，最终的漫反射BRDF为:$f_{lambert}&#x3D;\frac{\rho}{\pi}$（这里的$\rho$本质是一个三维向量，含有物体表面的颜色信息，也就是我们的albedo）。</p><h3 id="镜面反射项推导"><a href="#镜面反射项推导" class="headerlink" title="镜面反射项推导"></a>镜面反射项推导</h3><center>$f_{cook-torrance} = \frac{D(h)G(l,v)F(l,h)}{4(n·l)(n·v)}$</center>> 其中$v$为观察方向，$l$为入射光方向，$n$为宏观表面法线方向，$h$为微平面法线方向（实际从为平面来看，积分后这个$h$也就是半角向量【参考$D$项推导】）。- $D$：法线分布函数(Normal Distribution Function)，其代表了所有微观角度下微小镜面法线的分布情况，粗糙表面法线分布相对均匀，光滑表面法线分布相对集中。- $G$：几何函数(Geometry Function)，描述了微平面自遮挡的属性，当一个平面相对比较粗糙的时候，平面表面上的微平面有可能挡住其他的微平面从而减少表面所反射的光线。- $F$：菲涅尔方程(Fresnel Rquation)，描述了物体表面在不同入射光角度下反射光线所占的比率。<h4 id="D项推导"><a href="#D项推导" class="headerlink" title="D项推导"></a>D项推导</h4><p>具体推导参考连接吧，太多就不做具体记录了，这里列出最终公式如下：  </p><center>$\alpha = roughness^2$</center><center>$D = \frac{\alpha^2}{\pi((n·h)^2(\alpha^2-1)+1)^2}$</center><h4 id="G项推导"><a href="#G项推导" class="headerlink" title="G项推导"></a>G项推导</h4><center>$k_{direct} = \frac{(\alpha+1)^2}{8}$</center><center>$k_{IBL} = \frac{(\alpha+1)^2}{2}$</center><center>$G = \frac{n·l}{(n·l)(1-k)+k}*\frac{n·v}{(n·v)(1-k)+k}$</center><p>或者可写为：$G &#x3D; \frac{n·l}{lerp(k,1,n·l)}*\frac{n·v}{lerp(k,1,n·v)}$<br>此外还有一种效果近似，但性能更好的方法：$G_{sksm} &#x3D; \frac{(n·l)(n·v)}{lerp(\alpha,1,(n·h)^2)}$</p><h4 id="F项推导"><a href="#F项推导" class="headerlink" title="F项推导"></a>F项推导</h4><p>参考连接 <a href="https://www.cnblogs.com/wickedpriest/p/13363205.html">https://www.cnblogs.com/wickedpriest/p/13363205.html</a><br>菲涅尔方程是为了描述物理世界当中，观察角度与法线夹角越大反射程度一般越大的一种情形，但是精确计算计算消耗较大，一般用Fresnel-Schlick近似法求得近似解。  </p><center>$F_{Schlick}(h,v,F_0) = F_0+(1-F_0)(1-(h·v))^5$</center>或者可以写为:$F_{Schlick}(h,v,F_0) = lerp(F_0,1,(1-(h·v))^5)$ > $F_0$表示平面的基础反射率，它是利用所谓折射系数(Indices of Refraction)计算得出的。越是朝球面掠角的方向上看（此时视线和表面法线的夹角接近90度）菲涅尔现象就越明显，反光就越强。> $F_0 = \frac{(r-1)^2}{(r+1)^2}$，r为物体的折射率，不过只使用透明物体，不透明物体F_0 = k_s<div class="note info flat"><p>$F_0$表示平面的基础反射率，这是一个随材质而不同的参数，在一些数据库中可以查到不同材质的$F_0$值。</p></div><p>金属材料和非金属材料的$F_0$值是存在差异的，非金属材料的$F_0$三个通道的值一般不会高于0.17，金属材料则可能到1.0；另外非金属材料三个通道的值是不相等的，金属材料相反。因为金属和非金属在菲涅尔效应上的不同特性，物理学上将金属称为导体（Conductor），非金属称为电介质（Dielectric）。</p><pre><code class="language-hlsl">float3 F0 = float3(0.04);F0 = lerp(F0, albedo.rgb, metallic);</code></pre><div class="note info flat"><p>如果metallic为1，那么$F_0$完全用的是金属自己的颜色，此时只有高光反射，没有漫反射了，而越接近0，则越是使用电介质的颜色，0.04是经验值。</p></div><h3 id="kd与ks"><a href="#kd与ks" class="headerlink" title="kd与ks"></a>kd与ks</h3><p>因为要考虑能量守恒，因此$kd+ks&#x3D;1$，$ks$的值直接就是求出的$F$菲涅尔项。</p><details><summary>   <font color="#007ACC">🐍 什么是BRDF? </font></summary><p>参考连接 <a href="https://zhuanlan.zhihu.com/p/158025828">https://zhuanlan.zhihu.com/p/158025828</a><br>BRDF定义为出射的radiance与入射的irradiance的比值，其衡量了物体表面与光线进行反射的特性，表示出物体的材质效果。<br>根据之前的辐射度量学介绍，我们可以推理BRDF的函数公式：$f(l,v)&#x3D;\frac{dL_o(v)}{dE(l)}$。</p><blockquote><p>辐射能量（radiant energy）[$J$]：指电磁辐射的能量，用符号$Q$表示。<br>辐射通量（radiant flux）[$W$]：指单位时间辐射而出的能量，$\phi &#x3D; \frac{dQ}{dt}$。<br>辐照度（irradiance）[$W&#x2F;s$]：指单位面积单位时间所接收到的能量，$E &#x3D; \frac{d\phi}{dA}$。<br>辐射亮度（radiance）[$W&#x2F;m^2sr$]：指每单位垂直面，单位立体角，单位时间接收到的能量，$L &#x3D; \frac{d\phi}{d\omega dA_n}$。</p></blockquote><ul><li>其中$l$是入射光方向，$v$是观察方向。</li><li>$dL_o(v)$表示物体表面反射到方向$v$的微分辐射亮度，与$L_o(v)$的区别在于$L_o(v)$计算了物体表面上来自所有方向的入射光贡献，而$dL_o(v)$只计算了物体表面来自入射光方向$l$上的贡献。</li><li>$dE(l)$表示来自入射光方向$l$的辐照度，而$E(l)$则计算了物体表面来自所有方向的辐照度。</li></ul></details><hr><h2 id="Unity中的一些计算"><a href="#Unity中的一些计算" class="headerlink" title="Unity中的一些计算"></a>Unity中的一些计算</h2><h3 id="介电质全反射"><a href="#介电质全反射" class="headerlink" title="介电质全反射"></a>介电质全反射</h3><p>一般来说，现实中的物体都会一定程度发生轻微的全反射，这在镜面反射和菲涅尔效应中会有体现，最低全反射率是一个常量，为<strong>0.04</strong>。</p><pre><code class="language-hlsl">float dielectricSpec = 0.04;</code></pre><hr><h3 id="能量守恒"><a href="#能量守恒" class="headerlink" title="能量守恒"></a>能量守恒</h3><p>对于一个真实物理光照，我们需要考虑能量守恒。根据<strong>双向反射分布函数</strong>，我们可以把光拆分为漫反射光和镜面反射光分开计算，我们需要分配它们在光能量上的权重。<br>而在常见的物理光照变量中，金属度<strong>metallic</strong>就是来分配两者的权重的，金属度越高，漫反射光越弱，镜面反射越强。</p><pre><code class="language-hlsl">float3 diffuse = lerp(albedo * (1 - dielectricSpec), 0, metallic); //漫反射float3 specular = lerp(dielectricSpec, albedo, metallic); //镜面反射</code></pre><hr><h3 id="菲涅尔效应"><a href="#菲涅尔效应" class="headerlink" title="菲涅尔效应"></a>菲涅尔效应</h3><p>菲涅尔效应是一种物理现象，反应物体的反照率随物体折射率、法线、观察视角不同而产生的变化。最显著的例子是水面，垂直看水面能看清水底，平行看水面能看见倒影，很难看见水底。<br>菲尼尔只会影响到镜面反射，可以简单将它视作为时对镜面反射的一种后处理。</p><pre><code class="language-hlsl">float F = pow(1 - saturate(dot(n, v)), _Power); //反射率增强系数（非涅尔系数）,Unity中_Power数值为4</code></pre><p>在Unity中，非涅尔引起的反射率是有上限的，故不一定全反射，具体和物体的镜射率和光滑度有关。</p><pre><code class="language-hlsl">float reflectivity = lerp(dielectricSpec, 1, metallic) //镜面反射率，金属度越高，镜面反射率越接近1，反之越低，则越接近最低全反射常量（参考介电质全反射）float grazingTerm = saturate(reflectivity + smoothness);//掠角反射率specular = lerp(specular, grazingTerm, F);</code></pre><hr><h3 id="几何遮蔽"><a href="#几何遮蔽" class="headerlink" title="几何遮蔽"></a>几何遮蔽</h3><p>几何遮蔽用于表现微表面之间的遮挡导致的光线衰减现象，这在粗糙的表面尤为明显。<br>几何遮蔽有多种近似公式，在Unity中对直接光计算使用的是 sksm 几何遮蔽算法。</p><pre><code class="language-hlsl">float a2 = roughness*roughness;float G = saturate(dot(n, l) * dot(n, v)) / lerp(a2, 1, pow(saturate(dot(l,h)), 2));</code></pre><ul><li>当点乘结果不为1时（即光照视角倾斜于表面），粗糙度将作为分母影响结果大小（越粗糙分母越大，得到的结果越小），从而满足了几何遮蔽衰减光照的效果。<br>对于间接光，因为不用考虑角度问题，Unity则比较简单，直接根据粗糙度衰减即可。</li></ul><pre><code class="language-hlsl">float G = 1 / (1 + pow(a2,2));</code></pre><hr><h3 id="入射光计算"><a href="#入射光计算" class="headerlink" title="入射光计算"></a>入射光计算</h3><p>计算反射光之前，需要先计算入射光。</p><h3 id="直接光"><a href="#直接光" class="headerlink" title="直接光"></a>直接光</h3><p>对于直接光来说，入射光等价于<strong>辐照度（irradiance）</strong>，所以在反射计算开始之前，需要转化为<strong>辐射亮度（radiance）</strong>。<br>Unity中灯光的辐照度计算如下：</p><ul><li><code>Light light</code>：从Unity中获取灯光信息。</li></ul><pre><code class="language-hlsl">float3 irradiance = light.color * light.distanceAttenuation * light.shadowAttenuation;</code></pre><hr><h3 id="实时灯光"><a href="#实时灯光" class="headerlink" title="实时灯光"></a>实时灯光</h3><h4 id="如何计算静态lightMapUV"><a href="#如何计算静态lightMapUV" class="headerlink" title="如何计算静态lightMapUV"></a>如何计算静态lightMapUV</h4><div class="note info flat"><p>需要在顶点着色器中计算<strong>o.staticLightmapUV</strong>，同时要注意在顶点输入结构体和输出结构体中定义<strong>staticLightmapUV</strong>。</p></div><pre><code class="language-hlsl">o.staticLightmapUV.xy = v.staticLightmapUV.xy * unity_LightmapST.xy + unity_LightmapST.zw;</code></pre><p>Unity URP <strong>Lighting</strong>库自定义的函数为：</p><pre><code class="language-hlsl">OUTPUT_LIGHTMAP_UV(v.staticLightmapUV, unity_LightmapST, o.staticLightmapUV);</code></pre><h4 id="如何计算动态lightMapUV"><a href="#如何计算动态lightMapUV" class="headerlink" title="如何计算动态lightMapUV"></a>如何计算动态lightMapUV</h4><div class="note info flat"><p>需要在顶点着色器中计算<strong>output.dynamicLightmapUV</strong>，同时要注意在顶点输入结构体和输出结构体中定义<strong>dynamicLightmapUV</strong>。</p></div><pre><code class="language-hlsl">#ifdef DYNAMICLIGHTMAP_ON    output.dynamicLightmapUV = input.dynamicLightmapUV.xy * unity_DynamicLightmapST.xy + unity_DynamicLightmapST.zw;#endif</code></pre><h4 id="如何计算shadowMask"><a href="#如何计算shadowMask" class="headerlink" title="如何计算shadowMask"></a>如何计算shadowMask</h4><p>阴影遮罩技术，用于实现超远距离烘焙阴影。</p><pre><code class="language-hlsl">inputData.shadowMask = SAMPLE_SHADOWMASK(i.staticLightmapUV);//从顶点着色器中传递过来的staticLightmapUV</code></pre><p>其中，<strong>SAMPLE_SHADOWMASK</strong>的定义可以在URPShader库的<strong>Shadow.hlsl</strong>中找到，如下：</p><pre><code class="language-hlsl">#if defined(SHADOWS_SHADOWMASK) &amp;&amp; defined(LIGHTMAP_ON)    #define SAMPLE_SHADOWMASK(uv) SAMPLE_TEXTURE2D_LIGHTMAP(SHADOWMASK_NAME, SHADOWMASK_SAMPLER_NAME, uv SHADOWMASK_SAMPLE_EXTRA_ARGS);#elif !defined (LIGHTMAP_ON)    #define SAMPLE_SHADOWMASK(uv) unity_ProbesOcclusion;#else    #define SAMPLE_SHADOWMASK(uv) half4(1, 1, 1, 1);#endif</code></pre><h4 id="如何计算阴影uv"><a href="#如何计算阴影uv" class="headerlink" title="如何计算阴影uv"></a>如何计算阴影uv</h4><div class="note info flat"><p>需要在顶点着色器中计算<strong>o.shadowCoord</strong>，同时要注意在输出结构体中定义<strong>shadowCoord</strong>。</p></div><pre><code class="language-hlsl">#if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)    #if defined(_MAIN_LIGHT_SHADOWS_SCREEN)        o.shadowCoord=ComputeScreenPos(o.positionCS);    #else        o.shadowCoord=TransformWorldToShadowCoord(o.positionWS);    #endif#endif</code></pre><p>其中，<strong>REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR</strong>定义可以在URPShader库中的<strong>Shadow.hlsl</strong>中找到:</p><pre><code class="language-hlsl">#if !defined(_RECEIVE_SHADOWS_OFF)    #if defined(_MAIN_LIGHT_SHADOWS) || defined(_MAIN_LIGHT_SHADOWS_CASCADE) || defined(_MAIN_LIGHT_SHADOWS_SCREEN)        #define MAIN_LIGHT_CALCULATE_SHADOWS        #if defined(_MAIN_LIGHT_SHADOWS) || (defined(_MAIN_LIGHT_SHADOWS_SCREEN) &amp;&amp; !defined(_SURFACE_TYPE_TRANSPARENT))            #define REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR        #endif    #endif    #if defined(_ADDITIONAL_LIGHT_SHADOWS)        #define ADDITIONAL_LIGHT_CALCULATE_SHADOWS    #endif#endif</code></pre><p>其实其中的**_SURFACE_TYPE_TRANSPARENT<strong>是URP内置Lit光照所用的关键字，实际上我们自己写代码可以不关注它，比如在</strong>shadowCoord<strong>中计算时就去除了</strong>_SURFACE_TYPE_TRANSPARENT**。<br>原代码应该为：</p><pre><code class="language-hlsl">#if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)    #if defined(_MAIN_LIGHT_SHADOWS_SCREEN)&amp;&amp; !defined(_SURFACE_TYPE_TRANSPARENT)        o.shadowCoord=ComputeScreenPos(o.positionCS);    #else        o.shadowCoord=TransformWorldToShadowCoord(o.positionWS);    #endif#endif</code></pre><div class="note warning flat"><p>注意：需要在Pass中添加关键字。</p></div><pre><code class="language-hlsl">#pragma multi_compile _ _MAIN_LIGHT_SHADOWS _MAIN_LIGHT_SHADOWS_CASCADE _MAIN_LIGHT_SHADOWS_SCREEN  //主光阴影关键字#pragma multi_compile_fragment _ _SHADOWS_SOFT _SHADOWS_SOFT_LOW _SHADOWS_SOFT_MEDIUM _SHADOWS_SOFT_HIGH    //阴影质量关键字#pragma multi_compile_fragment _ _ADDITIONAL_LIGHT_SHADOWS      //额外光阴影关键字</code></pre><h4 id="实时灯光计算"><a href="#实时灯光计算" class="headerlink" title="实时灯光计算"></a>实时灯光计算</h4><pre><code class="language-hlsl">Light mainLight=GetMainLight(inputData.shadowCoord,inputData.positionWS,inputData.shadowMask);#ifdef _LIGHT_LAYERS    if (IsMatchingLightLayer(mainLight.layerMask, meshRenderingLayers))//匹配主光源的LightLayer#endif    &#123;        //执行主光光照计算    &#125;#if defined(_ADDITIONAL_LIGHTS)uint pixelLightCount = GetAdditionalLightsCount();LIGHT_LOOP_BEGIN(pixelLightCount)    Light light = GetAdditionalLight(lightIndex,inputData.positionWS,inputData.shadowMask);    #ifdef _LIGHT_LAYERS            if (IsMatchingLightLayer(light.layerMask, meshRenderingLayers))//匹配额外光的LightLayer    #endif            &#123;                //执行额外光光照计算            &#125;LIGHT_LOOP_END#endif</code></pre><div class="note warning flat"><p>注意：需要在Pass中添加关键字。</p></div><pre><code class="language-hlsl">#pragma multi_compile _ _ADDITIONAL_LIGHTS_VERTEX _ADDITIONAL_LIGHTS    //额外光关键字#pragma multi_compile _ _LIGHT_LAYERS   //LightLayer关键字</code></pre><hr><h3 id="间接光"><a href="#间接光" class="headerlink" title="间接光"></a>间接光</h3><p>间接光又叫环境光，是来自于环境中即四面八方的光。<br>相比直接光，环境光是不可数的，因此无法直接实时计算，故环境光一般是**基于图像的照明（IBL）**的烘焙灯光技术实现。（将HDR环境贴图视作光源，均匀的随机采样若干次，再以实时灯光的方式计算累加）。<br>由于“双向反射分布”的存在，故环境光针对漫射与镜射两种光，根据其特性，Unity有三种不同的烘焙方式：“光照贴图”、“光照探针”和“反射探针”。  </p><ul><li><strong>光照贴图LightMap</strong>，针对于静态物体。在使用烘焙LightMap时，我们的探针是均匀地摆放在物体的表面，然后将烘焙的结果保存这些名为 LightMap 的贴图中，这样我们在渲染物体是直接通过uv2在LightMap中采样烘焙好的光照信息。相对于LightProbe，LightMap 是不需要保存背面的光照信息的，如果继续使用球谐系数来保存，就会造成存储空间的浪费。</li><li><strong>光照探针LightPrope</strong>，适用于静态光照环境下的可自由移动的动态物体，此时无法使用 LightMap 来烘焙光照了，我们使用LightProbe的方式来预计算漫反射烘焙光照。</li><li><strong>反射探针ReflectionPrope</strong>，烘焙周围能够被镜面反射的物体。</li></ul><div class="note warning flat"><p>注意：间接光是预计算的，其存储的已经是计算好的<strong>辐照度irradiance</strong>，所以不需要额外转换。</p></div><h4 id="光照贴图"><a href="#光照贴图" class="headerlink" title="光照贴图"></a>光照贴图</h4><pre><code class="language-hlsl">float3 irradiance = SampleLightmap(i.staticLightmapUV,i.dynamicLightmapUV,normalWS);</code></pre><h4 id="光照探针"><a href="#光照探针" class="headerlink" title="光照探针"></a>光照探针</h4><p>光照探针存储的是漫射光信息。漫射光是粗糙的低频灯光，因此不需要形成像镜子那种清晰的反射画面，依此特性，为了减少内存提高性能，可以将其用球谐函数（SH）存储。<br>球谐函数类似于泰勒级数解三角函数，通过有限的多项式就可以近似出超越函数的效果，进而使得三角函数可被实际用于计算，非常实用。<br>同理球谐函数只需要存储几个多项式的参数，就可以实现输入方向输出该方向大概亮度的功能，内存占用少计算也高效。</p><pre><code class="language-hlsl">float3 irradiance = SampleSH(n);</code></pre><h4 id="光照贴图与光照探针的统一计算"><a href="#光照贴图与光照探针的统一计算" class="headerlink" title="光照贴图与光照探针的统一计算"></a>光照贴图与光照探针的统一计算</h4><p>在Unity URP中,光照探针的计算实际存储在<strong>inputData.bakedGI</strong>中，我们只需要在LitPass中寻找就能找到相关代码，以下贴出URP中的具体实现：</p><pre><code class="language-hlsl">inputData.bakedGI = SAMPLE_GI(i.staticLightmapUV, i.vertexSH, inputData.normalWS);</code></pre><div class="note warning flat"><p>注意：需要在Pass中添加关键字。</p></div><pre><code class="language-hlsl">#pragma multi_compile _ EVALUATE_SH_MIXED EVALUATE_SH_VERTEX</code></pre><p>定位<strong>SAMPLE_GI</strong>我们可以找到：</p><pre><code class="language-hlsl">// We either sample GI from baked lightmap or from probes.// If lightmap: sampleData.xy = lightmapUV// If probe: sampleData.xyz = L2 SH terms#if defined(LIGHTMAP_ON) &amp;&amp; defined(DYNAMICLIGHTMAP_ON)#define SAMPLE_GI(staticLmName, dynamicLmName, shName, normalWSName) SampleLightmap(staticLmName, dynamicLmName, normalWSName)#elif defined(DYNAMICLIGHTMAP_ON)#define SAMPLE_GI(staticLmName, dynamicLmName, shName, normalWSName) SampleLightmap(0, dynamicLmName, normalWSName)#elif defined(LIGHTMAP_ON)#define SAMPLE_GI(staticLmName, shName, normalWSName) SampleLightmap(staticLmName, 0, normalWSName)#else#define SAMPLE_GI(staticLmName, shName, normalWSName) SampleSHPixel(shName, normalWSName)#endif</code></pre><blockquote><p>可以看到，在有LightMap功能开启的前提下，都是采样LightMap，而没有的时候，则会采用SH函数。</p></blockquote><p>继续定位：</p><ul><li><strong>SampleLightmap</strong>：</li></ul><pre><code class="language-hlsl">// Sample baked and/or realtime lightmap. Non-Direction and Directional if available.half3 SampleLightmap(float2 staticLightmapUV, float2 dynamicLightmapUV, half3 normalWS)&#123;    #ifdef UNITY_LIGHTMAP_FULL_HDR        bool encodedLightmap = false;    #else        bool encodedLightmap = true;    #endif        half4 decodeInstructions = half4(LIGHTMAP_HDR_MULTIPLIER, LIGHTMAP_HDR_EXPONENT, 0.0h, 0.0h);        // The shader library sample lightmap functions transform the lightmap uv coords to apply bias and scale.        // However, universal pipeline already transformed those coords in vertex. We pass half4(1, 1, 0, 0) and        // the compiler will optimize the transform away.        half4 transformCoords = half4(1, 1, 0, 0);        float3 diffuseLighting = 0;    #if defined(LIGHTMAP_ON) &amp;&amp; defined(DIRLIGHTMAP_COMBINED)        diffuseLighting = SampleDirectionalLightmap(TEXTURE2D_LIGHTMAP_ARGS(LIGHTMAP_NAME, LIGHTMAP_SAMPLER_NAME),            TEXTURE2D_LIGHTMAP_ARGS(LIGHTMAP_INDIRECTION_NAME, LIGHTMAP_SAMPLER_NAME),            LIGHTMAP_SAMPLE_EXTRA_ARGS, transformCoords, normalWS, encodedLightmap, decodeInstructions);    #elif defined(LIGHTMAP_ON)        diffuseLighting = SampleSingleLightmap(TEXTURE2D_LIGHTMAP_ARGS(LIGHTMAP_NAME, LIGHTMAP_SAMPLER_NAME), LIGHTMAP_SAMPLE_EXTRA_ARGS, transformCoords, encodedLightmap, decodeInstructions);    #endif    #if defined(DYNAMICLIGHTMAP_ON) &amp;&amp; defined(DIRLIGHTMAP_COMBINED)        diffuseLighting += SampleDirectionalLightmap(TEXTURE2D_ARGS(unity_DynamicLightmap, samplerunity_DynamicLightmap),            TEXTURE2D_ARGS(unity_DynamicDirectionality, samplerunity_DynamicLightmap),            dynamicLightmapUV, transformCoords, normalWS, false, decodeInstructions);    #elif defined(DYNAMICLIGHTMAP_ON)        diffuseLighting += SampleSingleLightmap(TEXTURE2D_ARGS(unity_DynamicLightmap, samplerunity_DynamicLightmap),            dynamicLightmapUV, transformCoords, false, decodeInstructions);    #endif        return diffuseLighting;&#125;</code></pre><ul><li><strong>SampleSHPixel</strong>：</li></ul><pre><code class="language-hlsl">// SH Pixel Evaluation. Depending on target SH sampling might be done// mixed or fully in pixel. See SampleSHVertexhalf3 SampleSHPixel(half3 L2Term, half3 normalWS)&#123;    #if defined(EVALUATE_SH_VERTEX)        return L2Term;    #elif defined(EVALUATE_SH_MIXED)        half3 res = L2Term + SHEvalLinearL0L1(normalWS, unity_SHAr, unity_SHAg, unity_SHAb);    #ifdef UNITY_COLORSPACE_GAMMA        res = LinearToSRGB(res);    #endif        return max(half3(0, 0, 0), res);    #endif        // Default: Evaluate SH fully per-pixel        return SampleSH(normalWS);&#125;</code></pre><h4 id="反射探针"><a href="#反射探针" class="headerlink" title="反射探针"></a>反射探针</h4><p>镜射光是高频的需要形成清晰镜面的光照，使用环境贴图存储。<br>此外镜射光还与视角和材质粗糙度有关，其中视角是动态的因此无法烘焙，但粗糙度是固定的01范围，因此可以针对不同的粗糙度多烘焙几张。<br>由于越粗糙频率越低，贴图分辨率需求越小，因此这多张不同粗糙度烘焙的贴图可以利用mipmap存储，借此还能实现GPU自动插值纹理内容。<br>环境贴图本质就是一张具有mipmap的立方体纹理，因此只要根据粗糙度和微表面法向，就能取出对应的镜射光辐照度。</p><pre><code class="language-hlsl">float perceptualRoughness = 1 - smoothness; //直觉上的粗糙度（perceptualRoughness）float mipLevel = pr * (1.7 - 0.7 * pr) * UNITY_SPECCUBE_LOD_STEPS;float4 encodedIrradiance = unity_SpecCube0.SampleLevel(samplerunity_SpecCube0, reflect(-v, n), mipLevel);float3 irradiance = DecodeHDREnvironment(encodedIrradiance, unity_SpecCube0_HDR);//环境贴图一般是HDR贴图</code></pre><ul><li>UNITY_SPECCUBE_LOD_STEPS：unity中默认环境贴图的最大mipmap等级.</li><li>unity_SpecCube0：unity中的默认环境贴图。</li><li>samplerunity_SpecCube0：unity中默认环境贴图的采样器。</li><li>unity_SpecCube0_HDR：unity中默认环境贴图的编码信息。</li></ul><p>在URPShader函数库中，我们可以在<strong>GlobalIllumination.hlsl</strong>的<strong>GlossyEnvironmentReflection</strong>函数找到具体的代码，如下：</p><pre><code class="language-hlsl">half3 GlossyEnvironmentReflection(half3 reflectVector, float3 positionWS, half perceptualRoughness, half occlusion, float2 normalizedScreenSpaceUV)&#123;    #if !defined(_ENVIRONMENTREFLECTIONS_OFF)        half3 irradiance;    #if defined(_REFLECTION_PROBE_BLENDING) || USE_FORWARD_PLUS        irradiance = CalculateIrradianceFromReflectionProbes(reflectVector, positionWS, perceptualRoughness, normalizedScreenSpaceUV);    #else    #ifdef _REFLECTION_PROBE_BOX_PROJECTION        reflectVector = BoxProjectedCubemapDirection(reflectVector, positionWS, unity_SpecCube0_ProbePosition, unity_SpecCube0_BoxMin, unity_SpecCube0_BoxMax);    #endif // _REFLECTION_PROBE_BOX_PROJECTION        half mip = PerceptualRoughnessToMipmapLevel(perceptualRoughness);        half4 encodedIrradiance = half4(SAMPLE_TEXTURECUBE_LOD(unity_SpecCube0, samplerunity_SpecCube0, reflectVector, mip));        irradiance = DecodeHDREnvironment(encodedIrradiance, unity_SpecCube0_HDR);    #endif // _REFLECTION_PROBE_BLENDING        return irradiance * occlusion;    #else        return _GlossyEnvironmentColor.rgb * occlusion;    #endif // _ENVIRONMENTREFLECTIONS_OFF&#125;</code></pre><ul><li>reflectVector：反射方向</li><li>positionWS：世界空间位置</li><li>perceptualRoughness：1.0-smoothness</li><li>occlusion：遮蔽值，也就是我们的ao</li><li>normalizedScreenSpaceUV：屏幕空间uv</li></ul><h4 id="使用内置函数"><a href="#使用内置函数" class="headerlink" title="使用内置函数"></a>使用内置函数</h4><p>上述是比较详细的版本，实际上我们需要使用的就只是下面的就可以了，我们仍然可以在<strong>GlobalIllumination.hlsl</strong>的<strong>GlossyEnvironmentReflection</strong>函数中找到，只是参数不同而已。</p><pre><code class="language-hlsl">half3 GlossyEnvironmentReflection(half3 reflectVector, half perceptualRoughness, half occlusion)&#123;    #if !defined(_ENVIRONMENTREFLECTIONS_OFF)        half3 irradiance;        half mip = PerceptualRoughnessToMipmapLevel(perceptualRoughness);        half4 encodedIrradiance = half4(SAMPLE_TEXTURECUBE_LOD(unity_SpecCube0, samplerunity_SpecCube0, reflectVector, mip));        irradiance = DecodeHDREnvironment(encodedIrradiance, unity_SpecCube0_HDR);        return irradiance * occlusion;    #else        return _GlossyEnvironmentColor.rgb * occlusion;    #endif // _ENVIRONMENTREFLECTIONS_OFF&#125;</code></pre><p>实际上，上述的GI的计算，我们都可以使用一个函数进行：</p><pre><code class="language-hlsl">//顶点着色器可以获取bakedGIinputData.bakedGI = SAMPLE_GI(i.staticLightmapUV, i.vertexSH, inputData.normalWS);//片元着色器混合主光与bakedGIMixRealtimeAndBakedGI(mainLight,inputData.normalWS,inputData.bakedGI);//开始将bakedGI与反射探针混合计算half3 GIcolor = GlobalIllumination(brdfData, inputData.bakedGI, occlusion, normalWS, viewDirectionWS);</code></pre><p>我们可以查看一下的代码：</p><pre><code class="language-hlsl">half3 GlobalIllumination(BRDFData brdfData, BRDFData brdfDataClearCoat, float clearCoatMask,    half3 bakedGI, half occlusion, float3 positionWS,    half3 normalWS, half3 viewDirectionWS, float2 normalizedScreenSpaceUV)&#123;    half3 reflectVector = reflect(-viewDirectionWS, normalWS);    half NoV = saturate(dot(normalWS, viewDirectionWS));    half fresnelTerm = Pow4(1.0 - NoV);    half3 indirectDiffuse = bakedGI;    half3 indirectSpecular = GlossyEnvironmentReflection(reflectVector, positionWS, brdfData.perceptualRoughness, 1.0h, normalizedScreenSpaceUV);    half3 color = EnvironmentBRDF(brdfData, indirectDiffuse, indirectSpecular, fresnelTerm);    if (IsOnlyAOLightingFeatureEnabled())    &#123;        color = half3(1,1,1); // &quot;Base white&quot; for AO debug lighting mode    &#125;//清漆#if defined(_CLEARCOAT) || defined(_CLEARCOATMAP)    half3 coatIndirectSpecular = GlossyEnvironmentReflection(reflectVector, positionWS, brdfDataClearCoat.perceptualRoughness, 1.0h, normalizedScreenSpaceUV);    // TODO: &quot;grazing term&quot; causes problems on full roughness    half3 coatColor = EnvironmentBRDFClearCoat(brdfDataClearCoat, clearCoatMask, coatIndirectSpecular, fresnelTerm);    // Blend with base layer using khronos glTF recommended way using NoV    // Smooth surface &amp; &quot;ambiguous&quot; lighting    // NOTE: fresnelTerm (above) is pow4 instead of pow5, but should be ok as blend weight.    half coatFresnel = kDielectricSpec.x + kDielectricSpec.a * fresnelTerm;    return (color * (1.0 - coatFresnel * clearCoatMask) + coatColor) * occlusion;#else    return color * occlusion;#endif&#125;</code></pre><div class="note warning flat"><p>注意：需要在Pass中添加关键字。</p></div><pre><code class="language-hlsl">#pragma shader_feature_local_fragment _ENVIRONMENTREFLECTIONS_OFF#pragma multi_compile_fragment _ _REFLECTION_PROBE_BLENDING#pragma multi_compile_fragment _ _REFLECTION_PROBE_BOX_PROJECTION</code></pre><div class="note primary flat"><p>上述提及的关键字都可以根据项目所需功能进行增删或者修改，只要注意上下文不会报错就行。</p></div><h2 id="在URP中实现PBR"><a href="#在URP中实现PBR" class="headerlink" title="在URP中实现PBR"></a>在URP中实现PBR</h2><div class="note warning flat"><p>注意：本文使用的Unity版本号为：2022.3.63f2c1，不同版本所含有的URPShader库可能有所不同，请注意辨别。</p></div><h3 id="定义属性"><a href="#定义属性" class="headerlink" title="定义属性"></a>定义属性</h3><pre><code class="language-hlsl">Properties&#123;    [MainTexture]_BaseMap(&quot;Albedo&quot;,2D)=&quot;white&quot;&#123;&#125;    [MainColor]_BaseColor(&quot;Color&quot;,Color)=(1,1,1,1)    [Normal]_BumpMap(&quot;Normal&quot;,2D)=&quot;bump&quot;&#123;&#125;    _BumpScale(&quot;NormalScale&quot;,Float)=1.0    _MetallicMap(&quot;MetallicMap&quot;,2D)=&quot;black&quot;&#123;&#125;    _Metallic(&quot;Metallic&quot;,Range(0.0,1.0))=0.0    _SmoothnessMap(&quot;SmoothnessMap&quot;,2D)=&quot;gray&quot;&#123;&#125;    _Smoothness(&quot;Smoothness&quot;,Range(0.0,1.0))=0.5    _OcclusionMap(&quot;OcclusionMap&quot;,2D)=&quot;white&quot;&#123;&#125;    _OcclusionStrength(&quot;OcclusionStrength&quot;,Range(0.0,1.0))=1.0    _EmissionMap(&quot;Emission&quot;, 2D) = &quot;black&quot; &#123;&#125;    [HDR]_EmssionColor(&quot;EmssionColor&quot;,Color)=(0,0,0,0)    [Enum(UnityEngine.Rendering.CullMode)]_Cull(&quot;cullMode&quot;, Float) = 2.0    [Enum(UnityEngine.Rendering.BlendMode)] _SrcBlend(&quot;src&quot;, Float) = 1.0    [Enum(UnityEngine.Rendering.BlendMode)] _DstBlend(&quot;dst&quot;, Float) = 0.0    [Enum(UnityEngine.Rendering.BlendMode)] _SrcBlendAlpha(&quot;srcA&quot;, Float) = 1.0    [Enum(UnityEngine.Rendering.BlendMode)] _DstBlendAlpha(&quot;dstA&quot;, Float) = 0.0    [Enum(Off, 0, On, 1)] _ZWrite(&quot;zw&quot;, Float) = 1.0    [Enum(UnityEngine.Rendering.CompareFunction)] _ZTest(&quot;zt&quot;, Float) = 4.0&#125;</code></pre><h3 id="设置一些标签混合等"><a href="#设置一些标签混合等" class="headerlink" title="设置一些标签混合等"></a>设置一些标签混合等</h3><pre><code class="language-hlsl">Tags&#123;&quot;LightMode&quot;=&quot;UniversalForward&quot;&#125;Blend[_SrcBlend][_DstBlend], [_SrcBlendAlpha][_DstBlendAlpha]ZWrite[_ZWrite]Cull[_Cull]ZTest[_ZTest]</code></pre><h3 id="声明关键字"><a href="#声明关键字" class="headerlink" title="声明关键字"></a>声明关键字</h3><pre><code class="language-hlsl">#pragma vertex vert#pragma fragment frag#pragma prefer_hlslcc gles#pragma exclude_renderers d3d11_9x#pragma multi_compile_instancing#pragma target 2.0//这里多是声明贴图的关键字，需要配合自定义ShaderGUI开启与关闭#pragma shader_feature_local _NORMALMAP#pragma shader_feature_local _RECEIVE_SHADOWS_OFF#pragma shader_feature_local_fragment _ALPHATEST_ON#pragma shader_feature_local_fragment _EMISSION#pragma shader_feature_local_fragment _METALLICMAP#pragma shader_feature_local_fragment _SMOOTHNESSMAP#pragma shader_feature_local_fragment _OCCLUSIONMAP#pragma shader_feature_local_fragment _ENVIRONMENTREFLECTIONS_OFF// Universal Pipeline keywords#pragma multi_compile _ _MAIN_LIGHT_SHADOWS _MAIN_LIGHT_SHADOWS_CASCADE _MAIN_LIGHT_SHADOWS_SCREEN  //主光阴影关键字#pragma multi_compile _ _ADDITIONAL_LIGHTS_VERTEX _ADDITIONAL_LIGHTS            //额外光关键字#pragma multi_compile _ EVALUATE_SH_MIXED EVALUATE_SH_VERTEX                    //顶点球谐光照关键字#pragma multi_compile_fragment _ _ADDITIONAL_LIGHT_SHADOWS                      //额外光阴影关键字#pragma multi_compile_fragment _ _REFLECTION_PROBE_BLENDING                     //反射探针混合关键字#pragma multi_compile_fragment _ _REFLECTION_PROBE_BOX_PROJECTION               //反射探针盒投影关键字#pragma multi_compile_fragment _ _SHADOWS_SOFT _SHADOWS_SOFT_LOW _SHADOWS_SOFT_MEDIUM _SHADOWS_SOFT_HIGH    //阴影质量关键字#pragma multi_compile _ _LIGHT_LAYERS   //LightLayer关键字</code></pre><h3 id="引用头文件"><a href="#引用头文件" class="headerlink" title="引用头文件"></a>引用头文件</h3><pre><code class="language-hlsl">#include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/Core.hlsl&quot;#include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/Lighting.hlsl&quot;</code></pre><h3 id="声明变量与缓冲区"><a href="#声明变量与缓冲区" class="headerlink" title="声明变量与缓冲区"></a>声明变量与缓冲区</h3><pre><code class="language-hlsl">TEXTURE2D(_BaseMap);    SAMPLER(sampler_BaseMap);TEXTURE2D(_BumpMap);    SAMPLER(sampler_BumpMap);TEXTURE2D(_MetallicMap);    SAMPLER(sampler_MetallicMap);TEXTURE2D(_SmoothnessMap);    SAMPLER(sampler_SmoothnessMap);TEXTURE2D(_OcclusionMap);    SAMPLER(sampler_OcclusionMap);TEXTURE2D(_EmissionMap);    SAMPLER(sampler_EmissionMap);CBUFFER_START(UnityPerMaterial)    half4 _BaseColor;    half4 _EmssionColor;    half _Metallic;    half _Smoothness;    half _OcclusionStrength;    half _BumpScale;CBUFFER_END</code></pre><h3 id="声明顶点输入输出结构体"><a href="#声明顶点输入输出结构体" class="headerlink" title="声明顶点输入输出结构体"></a>声明顶点输入输出结构体</h3><pre><code class="language-hlsl">struct Attributes&#123;    float4 positionOS : POSITION;    float2 texcoord : TEXCOORD0;    float3 normalOS : NORMAL;    float4 tangentOS : TANGENT;    float2 staticLightMapUV : TEXCOORD1;    UNITY_VERTEX_INPUT_INSTANCE_ID&#125;;struct Varyings&#123;    float4 positionCS : SV_POSITION;    float2 uv : TEXCOORD0;    float3 positionWS : TEXCOORD1;    float3 normalWS : TEXCOORD2;    half4 tangentWS : TEXCOORD3;    #ifdef _ADDITIONAL_LIGHTS_VERTEX    half4 fogFactorAndVertexLight   : TEXCOORD5; // x: fogFactor, yzw: vertex light    #else    half  fogFactor                 : TEXCOORD5;    #endif    #if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)    float4 shadowCoord              : TEXCOORD6;    #endif    DECLARE_LIGHTMAP_OR_SH(staticLightmapUV, vertexSH, 7);    UNITY_VERTEX_INPUT_INSTANCE_ID    UNITY_VERTEX_OUTPUT_STEREO&#125;;</code></pre><p>可以查看一下<strong>DECLARE_LIGHTMAP_OR_SH</strong>的定义，在<strong>Lighting.hlsl</strong>文件中：</p><pre><code class="language-hlsl">#if defined(LIGHTMAP_ON)    #define DECLARE_LIGHTMAP_OR_SH(lmName, shName, index) float2 lmName : TEXCOORD##index    #define OUTPUT_LIGHTMAP_UV(lightmapUV, lightmapScaleOffset, OUT) OUT.xy = lightmapUV.xy * lightmapScaleOffset.xy + lightmapScaleOffset.zw;    #define OUTPUT_SH(normalWS, OUT)#else    #define DECLARE_LIGHTMAP_OR_SH(lmName, shName, index) half3 shName : TEXCOORD##index    #define OUTPUT_LIGHTMAP_UV(lightmapUV, lightmapScaleOffset, OUT)    #define OUTPUT_SH(normalWS, OUT) OUT.xyz = SampleSHVertex(normalWS)#endif</code></pre><h3 id="顶点着色器"><a href="#顶点着色器" class="headerlink" title="顶点着色器"></a>顶点着色器</h3><pre><code class="language-hlsl">Varyings vert (Attributes v)&#123;    Varyings o;    UNITY_SETUP_INSTANCE_ID(v);    UNITY_TRANSFER_INSTANCE_ID(v,o);    UNITY_INITIALIZE_VERTEX_OUTPUT_STEREO(o);    o.positionCS = TransformObjectToHClip(v.positionOS.xyz);    o.positionWS=TransformObjectToWorld(v.positionOS.xyz);    o.normalWS=TransformObjectToWorldNormal(v.normalOS);    o.tangentWS.xyz=TransformObjectToWorld(v.tangentOS.xyz);    o.tangentWS.z=v.tangentOS.w*GetOddNegativeScale();    o.uv = v.texcoord;    //额外光源    half3 vertexLightColor = VertexLighting(o.positionWS,o.normalWS);    //雾效因子    half fogFactor=0;    #if !defined(_FOG_FRAGMENT)        fogFactor=ComputeFogFactor(o.positionCS.z);    #endif    #ifdef _ADDITIONAL_LIGHTS_VERTEX        o.fogFactorAndVertexLight = half4(fogFactor, vertexLight);    #else        o.fogFactor = fogFactor;    #endif    //环境光uv    OUTPUT_LIGHTMAP_UV(v.staticLightmapUV, unity_LightmapST, v.staticLightmapUV);    OUTPUT_SH(o.normalWS, o.vertexSH);    //阴影uv    #if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)        #if defined(_MAIN_LIGHT_SHADOWS_SCREEN)            o.shadowCoord=ComputeScreenPos(o.positionCS);        #else            o.shadowCoord=TransformWorldToShadowCoord(o.positionWS);        #endif    #endif    return o;&#125;</code></pre><p>我们可以查看一下<strong>VertexLighting</strong>的定义，在<strong>Lighting.hlsl</strong>文件中：</p><pre><code class="language-hlsl">half3 VertexLighting(float3 positionWS, half3 normalWS)&#123;    half3 vertexLightColor = half3(0.0, 0.0, 0.0);#ifdef _ADDITIONAL_LIGHTS_VERTEX    uint lightsCount = GetAdditionalLightsCount();    uint meshRenderingLayers = GetMeshRenderingLayer();    LIGHT_LOOP_BEGIN(lightsCount)        Light light = GetAdditionalLight(lightIndex, positionWS);#ifdef _LIGHT_LAYERS    if (IsMatchingLightLayer(light.layerMask, meshRenderingLayers))#endif    &#123;        half3 lightColor = light.color * light.distanceAttenuation;        vertexLightColor += LightingLambert(lightColor, light.direction, normalWS);    &#125;    LIGHT_LOOP_END#endif    return vertexLightColor;&#125;</code></pre><p><strong>OUTPUT_LIGHTMAP_UV</strong>和<strong>OUTPUT_SH</strong>之前已经说过了，我们可以在看一下代码，仍然在<strong>Lighting.hlsl</strong>文件中：</p><pre><code class="language-hlsl">#if defined(LIGHTMAP_ON)    #define DECLARE_LIGHTMAP_OR_SH(lmName, shName, index) float2 lmName : TEXCOORD##index    #define OUTPUT_LIGHTMAP_UV(lightmapUV, lightmapScaleOffset, OUT) OUT.xy = lightmapUV.xy * lightmapScaleOffset.xy + lightmapScaleOffset.zw;    #define OUTPUT_SH(normalWS, OUT)#else    #define DECLARE_LIGHTMAP_OR_SH(lmName, shName, index) half3 shName : TEXCOORD##index    #define OUTPUT_LIGHTMAP_UV(lightmapUV, lightmapScaleOffset, OUT)    #define OUTPUT_SH(normalWS, OUT) OUT.xyz = SampleSHVertex(normalWS)#endif</code></pre><h3 id="片元着色器"><a href="#片元着色器" class="headerlink" title="片元着色器"></a>片元着色器</h3><pre><code class="language-hlsl">half4 frag (Varyings i) : SV_Target&#123;    UNITY_SETUP_INSTANCE_ID(i);    UNITY_SETUP_STEREO_EYE_INDEX_POST_VERTEX(i);    half4 finalColor=1.0;    SurfaceMapData surfaceData=(SurfaceMapData)0;    InitializeSurfaceData(i.uv,surfaceData);    MInputData inputData=(MInputData)0;    InitializeInputData(i,surfaceData.normalTS,inputData);        finalColor = PBRShading(inputData,surfaceData);    finalColor.rgb = MixFog(finalColor.rgb, inputData.fogCoord);    return finalColor;&#125;</code></pre><p>可以看到，我们是先计算了自定义的<strong>SurfaceMapData</strong>，然后再计算了自定义的<strong>MInputData</strong>，接在再计算PBR着色，最后进行雾效混合，输出颜色。</p><ul><li><strong>SurfaceMapData</strong>：自定义的表面贴图数据，是对声明的贴图进行采样，然后存放到对应的数据中，参考URP内置的<strong>SurfaceData</strong>。</li><li><strong>MInputData</strong>：自定义的输入数据，包含世界空间法线，世界空间位置，视角方向，环境光采样，阴影坐标等对模型数据，方向数据的存储，参考URP内置的<strong>InputData</strong>。</li><li><strong>PBRShading</strong>：自定义的PRB着色函数，计算直接光与间接光的全局光照。</li></ul><h4 id="SurfaceMapData"><a href="#SurfaceMapData" class="headerlink" title="SurfaceMapData"></a>SurfaceMapData</h4><pre><code class="language-hlsl">struct SurfaceMapData&#123;    half3 albedo;    half3 specular;    half metallic;    half smoothness;    half3 emission;    half occlusion;    half alpha;    half3 normalTS;&#125;;</code></pre><p>对比一下URP内置的<strong>SurfaceData</strong>:</p><pre><code class="language-hlsl">struct SurfaceData&#123;    half3 albedo;    half3 specular;    half  metallic;    half  smoothness;    half3 normalTS;    half3 emission;    half  occlusion;    half  alpha;    half  clearCoatMask;    half  clearCoatSmoothness;&#125;;</code></pre><p>我们去除了清漆的数据：<strong>clearCoatMask</strong>和<strong>clearCoatSmoothness</strong>。<br>初始化<strong>SurfaceMapData</strong>：</p><pre><code class="language-hlsl">inline void InitializeSurfaceData(float2 uv,out SurfaceMapData surfaceData)&#123;    half4 albedoAlpha=SAMPLE_TEXTURE2D(_BaseMap, sampler_BaseMap, uv);    //Albedo    surfaceData.albedo=albedoAlpha.rgb*_BaseColor.rgb;    //Alpha    surfaceData.alpha=albedoAlpha.a*_BaseColor.a;    //Specular    surfaceData.specular=0.0;    //Metallic    #ifdef _METALLICMAP        surfaceData.metallic=SAMPLE_TEXTURE2D(_MetallicMap, sampler_MetallicMap, uv).r*_Metallic;    #else        surfaceData.metallic=_Metallic;    #endif    //Smoothness    #ifdef _SOMMTHNESSMAP        surfaceData.smoothness=SAMPLE_TEXTURE2D(_SmoothnessMap, sampler_SmoothnessMap, uv).r*_Smoothness;    #else        surfaceData.smoothness=_Smoothness;    #endif        //Normal    #ifdef _NORMALMAP        half4 n = SAMPLE_TEXTURE2D(_BumpMap, sampler_BumpMap, uv);        #if BUMP_SCALE_NOT_SUPPORTED            surfaceData.normalTS=UnpackNormal(n);        #else            surfaceData.normalTS=UnpackNormalScale(n, _BumpScale);        #endif    #else        surfaceData.normalTS=half3(0.0, 0.0, 1.0);    #endif    //Occusion    #ifdef _OCCLUSIONMAP        half occ = SAMPLE_TEXTURE2D(_OcclusionMap, sampler_OcclusionMap,uv).r;        surfaceData.occlusion= LerpWhiteTo(occ, _OcclusionStrength);    #else        surfaceData.occlusion= _OcclusionStrength;    #endif    //Emssion    #ifndef _EMISSION        surfaceData.emission=_EmssionColor.rgb;    #else        surfaceData.emission= SAMPLE_TEXTURE2D(_EmissionMap, sampler_EmissionMap, uv).rgb * _EmssionColor.rgb;    #endif&#125;</code></pre><details><summary>   <font color="#007ACC">🐍 为什么surfaceData.specular的值被设置为0? </font></summary><blockquote><p>因为我们的这次使用的是金属工作流，在URP默认LitShader中还有一种叫做高光工作流，我们可以在<strong>LitInput.hlsl</strong>文件中的<strong>InitializeStandardLitSurfaceData</strong>函数找到：</p></blockquote><pre><code class="language-hlsl">half4 specGloss = SampleMetallicSpecGloss(uv, albedoAlpha.a);#if _SPECULAR_SETUP    outSurfaceData.metallic = half(1.0);    outSurfaceData.specular = specGloss.rgb;#else    outSurfaceData.metallic = specGloss.r;    outSurfaceData.specular = half3(0.0, 0.0, 0.0);#endif</code></pre><p>可以看到高光工作流中存在高光颜色，而在金属工作流中高光颜色就被设定为了黑色。</p><blockquote><p>跟踪函数，我们可以在<strong>LitInput.hlsl</strong>中找到<strong>SampleMetallicSpecGloss</strong>定义：</p></blockquote><pre><code class="language-hlsl">half4 SampleMetallicSpecGloss(float2 uv, half albedoAlpha)&#123;    half4 specGloss;#ifdef _METALLICSPECGLOSSMAP    specGloss = half4(SAMPLE_METALLICSPECULAR(uv));    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a *= _Smoothness;    #endif#else // _METALLICSPECGLOSSMAP    #if _SPECULAR_SETUP        specGloss.rgb = _SpecColor.rgb;    #else        specGloss.rgb = _Metallic.rrr;    #endif    #ifdef _SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A        specGloss.a = albedoAlpha * _Smoothness;    #else        specGloss.a = _Smoothness;    #endif#endif    return specGloss;&#125;</code></pre><p><strong>SAMPLE_METALLICSPECULAR</strong>定义如下：</p><pre><code class="language-hlsl">#ifdef _SPECULAR_SETUP    #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_SpecGlossMap, sampler_SpecGlossMap, uv)#else    #define SAMPLE_METALLICSPECULAR(uv) SAMPLE_TEXTURE2D(_MetallicGlossMap, sampler_MetallicGlossMap, uv)#endif</code></pre><p>所以Unity URP内置的金属工作流和高光工作流的执行逻辑是这样的：</p><ol><li>金属工作流或者高光工作流采用了贴图：<ol><li>先获取高光的颜色：<ul><li><code>specGloss = half4(SAMPLE_METALLICSPECULAR(uv));</code>其中rgb就是贴图颜色，也就是高光颜色。</li></ul></li><li>然后获取光滑度：<ul><li>如果光滑度的数值被存放在Albedo贴图的Alpha通道中，对应关键字：<strong>_SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A</strong>，则执行：<code>specGloss.a = albedoAlpha * _Smoothness;</code></li><li>如果光滑度的数值被存放在金属工作流或者高光工作流的贴图Alpha通道中，则执行：<code>specGloss.a *= _Smoothness;</code></li></ul></li></ol></li><li>金属工作流或者高光工作流未采用贴图:<ol><li>先获取高光的颜色：<ul><li>如果是高光工作流，对应关键字**_SPECULAR_SETUP**，则执行：<code>specGloss.rgb = _SpecColor.rgb;</code></li><li>如果是金属工作流，则执行：<code>specGloss.rgb = _Metallic.rrr;</code></li></ul></li><li>然后获取光滑度：<ul><li>如果光滑度的数值被存放在Albedo贴图的Alpha通道中，对应关键字：<strong>_SMOOTHNESS_TEXTURE_ALBEDO_CHANNEL_A</strong>，则执行：<code>specGloss.a = albedoAlpha * _Smoothness;</code></li><li>因为没有贴图了，所以直接是数值，则执行：<code>specGloss.a = _Smoothness;</code></li></ul></li></ol></li></ol></details><details><summary>   <font color="#007ACC">🐍 LerpWhiteTo是什么，为什么occlusion采样贴图的时候要执行? </font></summary><p>我们可以在<strong>CommonMaterial.hlsl</strong>文件中找到<strong>LerpWhiteTo</strong>的定义：</p><pre><code class="language-hlsl">real LerpWhiteTo(real b, real t)&#123;    real oneMinusT = 1.0 - t;    return oneMinusT + b * t;&#125;</code></pre><p>其实这样做只是让 <strong>_OcclusionStrength</strong> 起到调节的参数而已，我们可以观察，当 <strong>_OcclusionStrength</strong> 为1的时候， <strong>occlusion</strong> 完全取决与采样的贴图数值；当 <strong>_OcclusionStrength</strong> 为0的时候，则数值永远为1，这就是为什么函数叫做 <strong>LerpWhiteTo</strong> ，翻译过来就是从白色插值到。</p></details><h4 id="MInputData"><a href="#MInputData" class="headerlink" title="MInputData"></a>MInputData</h4><pre><code class="language-hlsl">struct MInputData&#123;    float3 positionWS;    float3 normalWS;    half3 viewWS;    float4 shadowCoord;    half fogCoord;    half3 vertexLighting;    half3 bakedGI;    half4 shadowMask;&#125;;</code></pre><p>对比一下URP内置的<strong>InputData</strong>:</p><pre><code class="language-hlsl">struct InputData&#123;    float3  positionWS;    float4  positionCS;    float3  normalWS;    half3   viewDirectionWS;    float4  shadowCoord;    half    fogCoord;    half3   vertexLighting;    half3   bakedGI;    float2  normalizedScreenSpaceUV;    half4   shadowMask;    half3x3 tangentToWorld;    #if defined(DEBUG_DISPLAY)    half2   dynamicLightmapUV;    half2   staticLightmapUV;    float3  vertexSH;    half3 brdfDiffuse;    half3 brdfSpecular;    float2 uv;    uint mipCount;    // texelSize :    // x = 1 / width    // y = 1 / height    // z = width    // w = height    float4 texelSize;    // mipInfo :    // x = quality settings minStreamingMipLevel    // y = original mip count for texture    // z = desired on screen mip level    // w = loaded mip level    float4 mipInfo;    #endif&#125;;</code></pre><p>初始化<strong>InputData</strong>:</p><pre><code class="language-hlsl">inline void InitializeInputData(Varyings i,half3 normalTS,out MInputData inputData)&#123;    inputData.positionWS=i.positionWS;    #ifdef _NORMALMAP        float3 normalWS = normalize(i.normalWS);        float3 tangentWS = normalize(i.tangentWS.xyz);        float3 binormalWS = cross(normalWS,tangentWS)*i.tangentWS.w;        half3x3 tangentToWorld = half3x3(tangentWS, binormalWS,normalWS);        inputData.normalWS=normalize(mul(normalTS,tangentToWorld));    #else        inputData.normalWS=normalize(i.normalWS);        inputData.normalWS.xy*=_BumpScale;    #endif    // Perspective    inputData.viewWS=normalize(_WorldSpaceCameraPos.xyz-i.positionWS);    /*    //Orthographic    float4x4 viewMat = GetWorldToViewMatrix();    inputData.viewWS = viewMat[2].xyz;    */    #if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)        inputData.shadowCoord = i.shadowCoord;    #else        inputData.shadowCoord = float4(0, 0, 0, 0);    #endif    #ifdef _ADDITIONAL_LIGHTS_VERTEX        inputData.fogCoord = InitializeInputDataFog(float4(i.positionWS, 1.0), i.fogFactorAndVertexLight.x);        inputData.vertexLighting = i.fogFactorAndVertexLight.yzw;    #else        inputData.vertexLighting=half3(0.0,0.0,0.0);        inputData.fogCoord = InitializeInputDataFog(float4(i.positionWS, 1.0), i.fogFactor);    #endif    inputData.bakedGI = SAMPLE_GI(i.staticLightmapUV, i.vertexSH, inputData.normalWS);    inputData.shadowMask = SAMPLE_SHADOWMASK(i.staticLightmapUV);&#125;</code></pre><h4 id="PBRShading"><a href="#PBRShading" class="headerlink" title="PBRShading"></a>PBRShading</h4><pre><code class="language-hlsl">inline half4 PBRShading(MInputData inputData,SurfaceMapData surfaceData)&#123;    BRDFData brdfData;    InitializeBRDF(surfaceData,brdfData);    //ShadowMask    #if defined(SHADOWS_SHADOWMASK) &amp;&amp; defined(LIGHTMAP_ON)        half4 shadowMask = inputData.shadowMask;    #elif !defined (LIGHTMAP_ON)        half4 shadowMask = unity_ProbesOcclusion;    #else        half4 shadowMask = half4(1, 1, 1, 1);    #endif    Light mainLight=GetMainLight(inputData.shadowCoord,inputData.positionWS,shadowMask);    MixRealtimeAndBakedGI(mainLight,inputData.normalWS,inputData.bakedGI);    uint meshRenderingLayers = GetMeshRenderingLayer();    //间接光GI        half3 giColor = GlobalIllumination(brdfData,inputData.bakedGI,surfaceData.occlusion,inputData.positionWS,inputData.normalWS,inputData.viewWS);    //直接光    half3 mainLightColor=0;    half3 additionalLightsColor=0;    #ifdef _LIGHT_LAYERS        if (IsMatchingLightLayer(mainLight.layerMask, meshRenderingLayers))    #endif        &#123;            mainLightColor = LightingPhysicallyBased(brdfData,mainLight,inputData.normalWS, inputData.viewWS);        &#125;    #if defined(_ADDITIONAL_LIGHTS)    uint pixelLightCount = GetAdditionalLightsCount();    LIGHT_LOOP_BEGIN(pixelLightCount)        Light light = GetAdditionalLight(lightIndex,inputData.positionWS,shadowMask);        #ifdef _LIGHT_LAYERS                if (IsMatchingLightLayer(light.layerMask, meshRenderingLayers))        #endif                &#123;                    additionalLightsColor += LightingPhysicallyBased(brdfData,light,inputData.normalWS, inputData.viewWS);                &#125;    LIGHT_LOOP_END    #endif    #if defined(_ADDITIONAL_LIGHTS_VERTEX)        half3 lightColor=mainLightColor+additionalLightsColor+inputData.vertexLighting * brdfData.diffuse+giColor;    #else        half3 lightColor=mainLightColor+additionalLightsColor+giColor;    #endif    lightColor.rgb+=surfaceData.emission;    #if REAL_IS_HALF        return min(half4(lightColor,surfaceData.alpha), HALF_MAX);    #else        return half4(lightColor,surfaceData.alpha);    #endif&#125;</code></pre><p>这其中最重要的涉及到PBR的就是<strong>InitializeBRDF</strong>与<strong>LightingPhysicallyBased</strong>两个函数。</p><h4 id="InitializeBRDF"><a href="#InitializeBRDF" class="headerlink" title="InitializeBRDF"></a>InitializeBRDF</h4><p>这个函数就是初始化BRDF所用的参数</p><pre><code class="language-hlsl">inline void InitializeBRDF(SurfaceMapData surfaceData,out BRDFData outBRDFData)&#123;    half oneMinusReflectivity = OneMinusReflectivityMetallic(surfaceData.metallic);    half reflectivity = half(1.0) - oneMinusReflectivity;    half3 brdfDiffuse = surfaceData.albedo * oneMinusReflectivity;    half3 brdfSpecular = lerp(kDieletricSpec.rgb, surfaceData.albedo, surfaceData.metallic);    outBRDFData = (BRDFData)0;    outBRDFData.albedo = surfaceData.albedo;    outBRDFData.diffuse = brdfDiffuse;    outBRDFData.specular = brdfSpecular;    outBRDFData.reflectivity = reflectivity;    outBRDFData.perceptualRoughness = PerceptualSmoothnessToPerceptualRoughness(surfaceData.smoothness);    outBRDFData.roughness           = max(PerceptualRoughnessToRoughness(outBRDFData.perceptualRoughness), HALF_MIN_SQRT);    outBRDFData.roughness2          = max(outBRDFData.roughness * outBRDFData.roughness, HALF_MIN);    outBRDFData.grazingTerm         = saturate(surfaceData.smoothness + reflectivity);    outBRDFData.normalizationTerm   = outBRDFData.roughness * half(4.0) + half(2.0);    outBRDFData.roughness2MinusOne  = outBRDFData.roughness2 - half(1.0);&#125;</code></pre><p>其中<code>outBRDFData.diffuse = brdfDiffuse;</code>就是$k_d$，<code>outBRDFData.specular = brdfSpecular;</code>就是$k_s$。<br>两者的分配权重由金属度决定：</p><center>$k_d = lerp(1,0,metallic)$</center><center>$k_s = lerp(0,1,metallic)$</center>但现实中的一般物体是不可能全漫射的，即使是黑色表面，遇到强光也会反射出镜射光，因此需要提供一定的保底全反射，该全反射的系数一般为 0.04（标准介电镜面反射）：<center>$k_d = lerp(0.96,0,metallic)$</center><center>$k_s = lerp(0.04,1,metallic)$</center>反射光的光不一定和入射光颜色一样，因为很多物体的表面也会吸收部分波段的光，因此计算时还需要考虑表面的基础反照率（表面颜色）：<center>$k_d = lerp(0.96*albedo,0,metallic)$</center><center>$k_s = lerp(0.04,1*albedo,metallic)$</center><details><summary>   <font color="#007ACC">🐍 kDielectricSpec </font></summary><pre><code class="language-hlsl">#define kDielectricSpec half4(0.04, 0.04, 0.04, 1.0 - 0.04) // standard dielectric reflectivity coef at incident angle (= 4%)</code></pre></details><details><summary>   <font color="#007ACC">🐍 OneMinusReflectivityMetallic </font></summary><pre><code class="language-hlsl">half OneMinusReflectivityMetallic(half metallic)&#123;    // We&#39;ll need oneMinusReflectivity, so    //   1-reflectivity = 1-lerp(dielectricSpec, 1, metallic) = lerp(1-dielectricSpec, 0, metallic)    // store (1-dielectricSpec) in kDielectricSpec.a, then    //   1-reflectivity = lerp(alpha, 0, metallic) = alpha + metallic*(0 - alpha) =    //                  = alpha - metallic * alpha    half oneMinusDielectricSpec = kDielectricSpec.a;    return oneMinusDielectricSpec - metallic * oneMinusDielectricSpec;&#125;</code></pre></details><details><summary>   <font color="#007ACC">🐍 PerceptualSmoothnessToPerceptualRoughness </font></summary><pre><code class="language-hlsl">real PerceptualSmoothnessToPerceptualRoughness(real perceptualSmoothness)&#123;    return (1.0 - perceptualSmoothness);&#125;</code></pre></details><details><summary>   <font color="#007ACC">🐍 PerceptualRoughnessToRoughness </font></summary><pre><code class="language-hlsl">real PerceptualRoughnessToRoughness(real perceptualRoughness)&#123;    return perceptualRoughness * perceptualRoughness;&#125;</code></pre></details><h4 id="LightingPhysicallyBased"><a href="#LightingPhysicallyBased" class="headerlink" title="LightingPhysicallyBased"></a>LightingPhysicallyBased</h4><p>光照物理计算。<br>如下是基础函数，也是我们使用的函数，在<strong>Lighting.hlsl</strong>中有许多封装好的函数在里面，可以自己去查看：</p><pre><code class="language-hlsl">half3 LightingPhysicallyBased(BRDFData brdfData, Light light, half3 normalWS, half3 viewDirectionWS)&#123;    #ifdef _SPECULARHIGHLIGHTS_OFF    bool specularHighlightsOff = true;#else    bool specularHighlightsOff = false;#endif    const BRDFData noClearCoat = (BRDFData)0;    return LightingPhysicallyBased(brdfData, noClearCoat, light, normalWS, viewDirectionWS, 0.0, specularHighlightsOff);&#125;</code></pre><p>根本函数：</p><pre><code class="language-hlsl">half3 LightingPhysicallyBased(BRDFData brdfData, BRDFData brdfDataClearCoat,    half3 lightColor, half3 lightDirectionWS, half lightAttenuation,    half3 normalWS, half3 viewDirectionWS,    half clearCoatMask, bool specularHighlightsOff)&#123;    half NdotL = saturate(dot(normalWS, lightDirectionWS));    half3 radiance = lightColor * (lightAttenuation * NdotL);    half3 brdf = brdfData.diffuse;#ifndef _SPECULARHIGHLIGHTS_OFF    [branch] if (!specularHighlightsOff)    &#123;        brdf += brdfData.specular * DirectBRDFSpecular(brdfData, normalWS, lightDirectionWS, viewDirectionWS);#if defined(_CLEARCOAT) || defined(_CLEARCOATMAP)        // Clear coat evaluates the specular a second timw and has some common terms with the base specular.        // We rely on the compiler to merge these and compute them only once.        half brdfCoat = kDielectricSpec.r * DirectBRDFSpecular(brdfDataClearCoat, normalWS, lightDirectionWS, viewDirectionWS);            // Mix clear coat and base layer using khronos glTF recommended formula            // https://github.com/KhronosGroup/glTF/blob/master/extensions/2.0/Khronos/KHR_materials_clearcoat/README.md            // Use NoV for direct too instead of LoH as an optimization (NoV is light invariant).            half NoV = saturate(dot(normalWS, viewDirectionWS));            // Use slightly simpler fresnelTerm (Pow4 vs Pow5) as a small optimization.            // It is matching fresnel used in the GI/Env, so should produce a consistent clear coat blend (env vs. direct)            half coatFresnel = kDielectricSpec.x + kDielectricSpec.a * Pow4(1.0 - NoV);        brdf = brdf * (1.0 - clearCoatMask * coatFresnel) + brdfCoat * clearCoatMask;#endif // _CLEARCOAT    &#125;#endif // _SPECULARHIGHLIGHTS_OFF    return brdf * radiance;&#125;</code></pre><p>清漆的我们可以暂时不看，那代码就可以简化成：</p><pre><code class="language-hlsl">half3 LightingPhysicallyBased(BRDFData brdfData, BRDFData brdfDataClearCoat,    half3 lightColor, half3 lightDirectionWS, half lightAttenuation,    half3 normalWS, half3 viewDirectionWS,    half clearCoatMask, bool specularHighlightsOff)&#123;    half NdotL = saturate(dot(normalWS, lightDirectionWS));    half3 radiance = lightColor * (lightAttenuation * NdotL);    half3 brdf = brdfData.diffuse;#ifndef _SPECULARHIGHLIGHTS_OFF    [branch] if (!specularHighlightsOff)    &#123;        brdf += brdfData.specular * DirectBRDFSpecular(brdfData, normalWS, lightDirectionWS, viewDirectionWS);    &#125;#endif // _SPECULARHIGHLIGHTS_OFF    return brdf * radiance;&#125;</code></pre><p>我们详细来分析这里的计算。<br>首先是进行了一个兰伯特计算，然后求取当前光的辐射亮度<strong>radiance</strong>为：光照颜色<em>兰伯特</em>光照衰减。</p><details><summary>   <font color="#007ACC">🐍 为什么这样就能求出光的辐射亮度呢？ </font></summary><p>之前我们有提到过，辐照度也就是我们的albedo，光的albedo也就是光照颜色，因为点光和一些其余光源会有光照衰弱的特性（随距离增加能量减少），那么我们就获得了辐照度：<br> <center> <code>float3 irradiance = light.color * light.distanceAttenuation * light.shadowAttenuation;</code> </center><br>我们说过，光的辐射亮度与辐照的之间是正比关系，这个数值就是物体表面法线与光线的余角，那么我们自然而然就获取了光照的辐照亮度了。</p></details><p>然后我们计算镜面反射高光<strong>DirectBRDFSpecular</strong>：</p><pre><code class="language-hlsl">// Computes the scalar specular term for Minimalist CookTorrance BRDF// NOTE: needs to be multiplied with reflectance f0, i.e. specular color to completehalf DirectBRDFSpecular(BRDFData brdfData, half3 normalWS, half3 lightDirectionWS, half3 viewDirectionWS)&#123;    float3 lightDirectionWSFloat3 = float3(lightDirectionWS);    float3 halfDir = SafeNormalize(lightDirectionWSFloat3 + float3(viewDirectionWS));    float NoH = saturate(dot(float3(normalWS), halfDir));    half LoH = half(saturate(dot(lightDirectionWSFloat3, halfDir)));    // GGX Distribution multiplied by combined approximation of Visibility and Fresnel    // BRDFspec = (D * V * F) / 4.0    // D = roughness^2 / ( NoH^2 * (roughness^2 - 1) + 1 )^2    // V * F = 1.0 / ( LoH^2 * (roughness + 0.5) )    // See &quot;Optimizing PBR for Mobile&quot; from Siggraph 2015 moving mobile graphics course    // https://community.arm.com/events/1155    // Final BRDFspec = roughness^2 / ( NoH^2 * (roughness^2 - 1) + 1 )^2 * (LoH^2 * (roughness + 0.5) * 4.0)    // We further optimize a few light invariant terms    // brdfData.normalizationTerm = (roughness + 0.5) * 4.0 rewritten as roughness * 4.0 + 2.0 to a fit a MAD.    float d = NoH * NoH * brdfData.roughness2MinusOne + 1.00001f;    half LoH2 = LoH * LoH;    half specularTerm = brdfData.roughness2 / ((d * d) * max(0.1h, LoH2) * brdfData.normalizationTerm);    // On platforms where half actually means something, the denominator has a risk of overflow    // clamp below was added specifically to &quot;fix&quot; that, but dx compiler (we convert bytecode to metal/gles)    // sees that specularTerm have only non-negative terms, so it skips max(0,..) in clamp (leaving only min(100,...))#if REAL_IS_HALF    specularTerm = specularTerm - HALF_MIN;    // Update: Conservative bump from 100.0 to 1000.0 to better match the full float specular look.    // Roughly 65504.0 / 32*2 == 1023.5,    // or HALF_MAX / ((mobile) MAX_VISIBLE_LIGHTS * 2),    // to reserve half of the per light range for specular and half for diffuse + indirect + emissive.    specularTerm = clamp(specularTerm, 0.0, 1000.0); // Prevent FP16 overflow on mobiles#endif    return specularTerm;&#125;</code></pre><blockquote><p>我们首先可以注意到注释中的<strong>D</strong>项目，它就是<strong>GGX</strong>中的<strong>D</strong>项计算，只是没有除以$\pi$，这是Unity故意去掉的，让整体画面看起来亮一些。</p><p><strong>V*F</strong>也就是<strong>G*F</strong>，这是拟合的近似公式:$\frac{1}{max((l·h)^2,0.1)(roughness+0.5)}$.</p><p>在官方介绍的文档中$V &#x3D; \frac{G(n·v,n·l,roughness)}{(n·v)(n·l)}$。<strong>G</strong>用的就是$G_{sksm} &#x3D; \frac{(n·l)(n·v)}{lerp(roughness^2,1,(l·h)^2)}$，因此$V &#x3D; \frac{1}{lerp(roughness,1,(l·h)^2)}$.</p><p>$F &#x3D; (1-(l·h))^5$.<br>那么$V·F &#x3D; \frac{(1-(l·h)^5)}{(l·h)^2(1-roughness^2)+roughness^2}$.</p><p>当(l·h)接近1的时候，曲线会呈现平坦状态，当l与h角度接近90度也就是为0时候，曲线倾斜度非常剧烈，光线掠过表面并几乎与相机方向相反照射，斜率会迅速增加。<br>本来这样的优化就是对移动端进行操作的，Unity又进行了魔改成最后的曲线：$(V·F) &#x3D; \frac{1}{(l·h)^2 * (roughness + 0.5)}$，这样的曲线变化率变得更高了，也导致看起来的高光很亮，但在移动端看起来还是可以的，这样的优化极大增加了渲染的效率，所以Unity内置的并不算是完全物理的，有很多近似的地方在里面。</p></blockquote><details><summary>   <font color="#007ACC">🐍 为什么会推导出G_sksm这样的函数出来呢？ </font></summary><p>参考连接 <a href="https://community.arm.com/cfs-file/__key/communityserver-blogs-components-weblogfiles/00-00-00-20-66/siggraph2015_2D00_mmg_2D00_renaldas_2D00_notes.pdf">https://community.arm.com/cfs-file/__key/communityserver-blogs-components-weblogfiles/00-00-00-20-66/siggraph2015_2D00_mmg_2D00_renaldas_2D00_notes.pdf</a><br>原因是：&amp;GGX&amp;本身就是Smith模型：&amp;G_{Smith} &#x3D; \frac{(n·l)(n·v)}{lerp(k,1,(n·l))lerp(k,1,(n·v))}&amp;，因为&amp;V&amp;和&amp;G&amp;的关系是提取了&amp;f_{cook-torrance}&amp;的分母并进行了约分，公式如下：<br>$$<br>\begin{align}<br>V_{Smith} &amp;&#x3D; \frac{G_{Smith}}{(n·l)(n·v)}\\<br>&amp; &#x3D; \frac{(n·l)(n·v)}{lerp(k,1,(n·l))lerp(k,1,(n·v))(n·l)(n·v)}\\<br>&amp; &#x3D; \frac{1}{lerp(k,1,(n·l))lerp(k,1,(n·v))}<br>\end{align}<br>$$<br>而有一种廉价的近似方法KSK，它原本的公式应该为：$G_{ksk原版}\frac{2(n·h)^2}{(l·h)(v·h)}$，但Unity对移动端进行了优化，$G_{ksk优化}&#x3D;\frac{(n·l)(n·v)}{(l·h)^2}$，保留原本的ksk模型可以在PC上更加高效的物理计算，我们这里并没有处理。因此$V_{ksk} &#x3D; \frac{1}{(l·h)^2}$。<br>但是这样的模型缺少了粗糙度控制，后面<strong>J.Hable</strong>对其进行了改造，则变成了$V_{sksm} &#x3D; \frac{1}{(l·h)^2(1-roughness^2)+roughness^2}$ ，所以这也是$G_{sksm}$的由来。</p><div class="note warning flat"><p>注意：公式推导的roughness与代码中的roughnes不一样，代码中的roughness是Untiy做了预处理，是公式推导的roughness的平法。</p></div></details><p>计算完高光项，$k_s$乘以镜面反射系数+$k_d$，最后再乘以灯光辐射亮度，就是最终输出的颜色。</p><h2 id="全代码"><a href="#全代码" class="headerlink" title="全代码"></a>全代码</h2><pre><code class="language-hlsl">Properties&#123;[MainTexture]_BaseMap(&quot;Albedo&quot;,2D)=&quot;white&quot;&#123;&#125;[MainColor]_BaseColor(&quot;Color&quot;,Color)=(1,1,1,1)[Normal]_BumpMap(&quot;Normal&quot;,2D)=&quot;bump&quot;&#123;&#125;_BumpScale(&quot;NormalScale&quot;,Float)=1.0_MetallicMap(&quot;MetallicMap&quot;,2D)=&quot;black&quot;&#123;&#125;_Metallic(&quot;Metallic&quot;,Range(0.0,1.0))=0.0_SmoothnessMap(&quot;SmoothnessMap&quot;,2D)=&quot;gray&quot;&#123;&#125;_Smoothness(&quot;Smoothness&quot;,Range(0.0,1.0))=0.5_OcclusionMap(&quot;OcclusionMap&quot;,2D)=&quot;white&quot;&#123;&#125;_OcclusionStrength(&quot;OcclusionStrength&quot;,Range(0.0,1.0))=1.0_EmissionMap(&quot;Emission&quot;, 2D) = &quot;black&quot; &#123;&#125;[HDR]_EmssionColor(&quot;EmssionColor&quot;,Color)=(0,0,0,0)[Enum(UnityEngine.Rendering.CullMode)]_Cull(&quot;cullMode&quot;, Float) = 2.0[Enum(UnityEngine.Rendering.BlendMode)] _SrcBlend(&quot;src&quot;, Float) = 1.0[Enum(UnityEngine.Rendering.BlendMode)] _DstBlend(&quot;dst&quot;, Float) = 0.0[Enum(UnityEngine.Rendering.BlendMode)] _SrcBlendAlpha(&quot;srcA&quot;, Float) = 1.0[Enum(UnityEngine.Rendering.BlendMode)] _DstBlendAlpha(&quot;dstA&quot;, Float) = 0.0[Enum(Off, 0, On, 1)] _ZWrite(&quot;zw&quot;, Float) = 1.0[Enum(UnityEngine.Rendering.CompareFunction)] _ZTest(&quot;zt&quot;, Float) = 4.0&#125;SubShader&#123;Tags &#123; &quot;RenderType&quot;=&quot;Opaque&quot; &quot;RenderPipeline&quot;=&quot;UniversalPipeline&quot;&#125;LOD 100Pass&#123;    Tags&#123;&quot;LightMode&quot;=&quot;UniversalForward&quot;&#125;    Blend[_SrcBlend][_DstBlend], [_SrcBlendAlpha][_DstBlendAlpha]    ZWrite[_ZWrite]    Cull[_Cull]    ZTest[_ZTest]    HLSLPROGRAM    #pragma vertex vert    #pragma fragment frag    #pragma prefer_hlslcc gles    #pragma exclude_renderers d3d11_9x    #pragma multi_compile_instancing    #pragma target 2.0    #pragma shader_feature_local _NORMALMAP    #pragma shader_feature_local _RECEIVE_SHADOWS_OFF    #pragma shader_feature_local_fragment _ALPHATEST_ON    #pragma shader_feature_local_fragment _EMISSION    #pragma shader_feature_local_fragment _METALLICMAP    #pragma shader_feature_local_fragment _SMOOTHNESSMAP    #pragma shader_feature_local_fragment _OCCLUSIONMAP    #pragma shader_feature_local_fragment _ENVIRONMENTREFLECTIONS_OFF    // Universal Pipeline keywords    #pragma multi_compile _ _MAIN_LIGHT_SHADOWS _MAIN_LIGHT_SHADOWS_CASCADE _MAIN_LIGHT_SHADOWS_SCREEN    #pragma multi_compile _ _ADDITIONAL_LIGHTS_VERTEX _ADDITIONAL_LIGHTS    #pragma multi_compile _ EVALUATE_SH_MIXED EVALUATE_SH_VERTEX    #pragma multi_compile_fragment _ _ADDITIONAL_LIGHT_SHADOWS    #pragma multi_compile_fragment _ _REFLECTION_PROBE_BLENDING    #pragma multi_compile_fragment _ _REFLECTION_PROBE_BOX_PROJECTION    #pragma multi_compile_fragment _ _SHADOWS_SOFT _SHADOWS_SOFT_LOW _SHADOWS_SOFT_MEDIUM _SHADOWS_SOFT_HIGH    #pragma multi_compile _ _LIGHT_LAYERS    #include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/Core.hlsl&quot;    #include &quot;Packages/com.unity.render-pipelines.universal/ShaderLibrary/Lighting.hlsl&quot;    TEXTURE2D(_BaseMap);    SAMPLER(sampler_BaseMap);    TEXTURE2D(_BumpMap);    SAMPLER(sampler_BumpMap);    TEXTURE2D(_MetallicMap);    SAMPLER(sampler_MetallicMap);    TEXTURE2D(_SmoothnessMap);    SAMPLER(sampler_SmoothnessMap);    TEXTURE2D(_OcclusionMap);    SAMPLER(sampler_OcclusionMap);    TEXTURE2D(_EmissionMap);    SAMPLER(sampler_EmissionMap);    CBUFFER_START(UnityPerMaterial)        half4 _BaseColor;        half4 _EmssionColor;        half _Metallic;        half _Smoothness;        half _OcclusionStrength;        half _BumpScale;    CBUFFER_END    struct Attributes    &#123;        float4 positionOS : POSITION;        float2 texcoord : TEXCOORD0;        float3 normalOS : NORMAL;        float4 tangentOS : TANGENT;        float2 staticLightMapUV : TEXCOORD1;        UNITY_VERTEX_INPUT_INSTANCE_ID    &#125;;    struct Varyings    &#123;        float4 positionCS : SV_POSITION;        float2 uv : TEXCOORD0;        float3 positionWS : TEXCOORD1;        float3 normalWS : TEXCOORD2;        half4 tangentWS : TEXCOORD3;        #ifdef _ADDITIONAL_LIGHTS_VERTEX        half4 fogFactorAndVertexLight   : TEXCOORD5; // x: fogFactor, yzw: vertex light        #else        half  fogFactor                 : TEXCOORD5;        #endif        #if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)        float4 shadowCoord              : TEXCOORD6;        #endif        DECLARE_LIGHTMAP_OR_SH(staticLightmapUV, vertexSH, 7);        UNITY_VERTEX_INPUT_INSTANCE_ID        UNITY_VERTEX_OUTPUT_STEREO    &#125;;    struct SurfaceMapData    &#123;        half3 albedo;        half3 specular;        half metallic;        half smoothness;        half3 emission;        half occlusion;        half alpha;        half3 normalTS;    &#125;;    struct MInputData    &#123;        float3 positionWS;        float3 normalWS;        half3 viewWS;        float4 shadowCoord;        half fogCoord;        half3 vertexLighting;        half3 bakedGI;        half4 shadowMask;    &#125;;    inline void InitializeSurfaceData(float2 uv,out SurfaceMapData surfaceData)    &#123;        half4 albedoAlpha=SAMPLE_TEXTURE2D(_BaseMap, sampler_BaseMap, uv);        //Albedo        surfaceData.albedo=albedoAlpha.rgb*_BaseColor.rgb;        //Alpha        surfaceData.alpha=albedoAlpha.a*_BaseColor.a;        //Specular        surfaceData.specular=0.0;        //Metallic        #ifdef _METALLICMAP            surfaceData.metallic=SAMPLE_TEXTURE2D(_MetallicMap, sampler_MetallicMap, uv).r*_Metallic;        #else            surfaceData.metallic=_Metallic;        #endif        //Smoothness        #ifdef _SOMMTHNESSMAP            surfaceData.smoothness=SAMPLE_TEXTURE2D(_SmoothnessMap, sampler_SmoothnessMap, uv).r*_Smoothness;        #else            surfaceData.smoothness=_Smoothness;        #endif                //Normal        #ifdef _NORMALMAP            half4 n = SAMPLE_TEXTURE2D(_BumpMap, sampler_BumpMap, uv);            #if BUMP_SCALE_NOT_SUPPORTED                surfaceData.normalTS=UnpackNormal(n);            #else                surfaceData.normalTS=UnpackNormalScale(n, _BumpScale);            #endif        #else            surfaceData.normalTS=half3(0.0, 0.0, 1.0);        #endif        //Occusion        #ifdef _OCCLUSIONMAP            half occ = SAMPLE_TEXTURE2D(_OcclusionMap, sampler_OcclusionMap,uv).r;            surfaceData.occlusion= LerpWhiteTo(occ, _OcclusionStrength);        #else            surfaceData.occlusion= _OcclusionStrength;        #endif        //Emssion        #ifndef _EMISSION            surfaceData.emission=_EmssionColor.rgb;        #else            surfaceData.emission= SAMPLE_TEXTURE2D(_EmissionMap, sampler_EmissionMap, uv).rgb * _EmssionColor.rgb;        #endif    &#125;    inline void InitializeInputData(Varyings i,half3 normalTS,out MInputData inputData)    &#123;        inputData.positionWS=i.positionWS;        #ifdef _NORMALMAP            float3 normalWS = normalize(i.normalWS);            float3 tangentWS = normalize(i.tangentWS.xyz);            float3 binormalWS = cross(normalWS,tangentWS)*i.tangentWS.w;            half3x3 tangentToWorld = half3x3(tangentWS, binormalWS,normalWS);            inputData.normalWS=normalize(mul(normalTS,tangentToWorld));        #else            inputData.normalWS=normalize(i.normalWS);            inputData.normalWS.xy*=_BumpScale;        #endif        // Perspective        inputData.viewWS=normalize(_WorldSpaceCameraPos.xyz-i.positionWS);        /*        //Orthographic        float4x4 viewMat = GetWorldToViewMatrix();        inputData.viewWS = viewMat[2].xyz;        */        #if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)            inputData.shadowCoord = i.shadowCoord;        #else            inputData.shadowCoord = float4(0, 0, 0, 0);        #endif        #ifdef _ADDITIONAL_LIGHTS_VERTEX            inputData.fogCoord = InitializeInputDataFog(float4(i.positionWS, 1.0), i.fogFactorAndVertexLight.x);            inputData.vertexLighting = i.fogFactorAndVertexLight.yzw;        #else            inputData.vertexLighting=half3(0.0,0.0,0.0);            inputData.fogCoord = InitializeInputDataFog(float4(i.positionWS, 1.0), i.fogFactor);        #endif        inputData.bakedGI = SAMPLE_GI(i.staticLightmapUV, i.vertexSH, inputData.normalWS);        inputData.shadowMask = SAMPLE_SHADOWMASK(i.staticLightmapUV);    &#125;    inline void InitializeBRDF(SurfaceMapData surfaceData,out BRDFData outBRDFData)    &#123;        #ifdef _SPECULAR_SETUP            half reflectivity = ReflectivitySpecular(surfaceData.specular);            half oneMinusReflectivity = half(1.0) - reflectivity;            half3 brdfDiffuse = surfaceData.albedo * oneMinusReflectivity;            half3 brdfSpecular = surfaceData.specular;        #else            half oneMinusReflectivity = OneMinusReflectivityMetallic(surfaceData.metallic);            half reflectivity = half(1.0) - oneMinusReflectivity;            half3 brdfDiffuse = surfaceData.albedo * oneMinusReflectivity;            half3 brdfSpecular = lerp(kDieletricSpec.rgb, surfaceData.albedo, surfaceData.metallic);        #endif        outBRDFData = (BRDFData)0;        outBRDFData.albedo = surfaceData.albedo;        outBRDFData.diffuse = brdfDiffuse;        outBRDFData.specular = brdfSpecular;        outBRDFData.reflectivity = reflectivity;        outBRDFData.perceptualRoughness = PerceptualSmoothnessToPerceptualRoughness(surfaceData.smoothness);        outBRDFData.roughness           = max(PerceptualRoughnessToRoughness(outBRDFData.perceptualRoughness), HALF_MIN_SQRT);        outBRDFData.roughness2          = max(outBRDFData.roughness * outBRDFData.roughness, HALF_MIN);        outBRDFData.grazingTerm         = saturate(surfaceData.smoothness + reflectivity);        outBRDFData.normalizationTerm   = outBRDFData.roughness * half(4.0) + half(2.0);        outBRDFData.roughness2MinusOne  = outBRDFData.roughness2 - half(1.0);    &#125;    inline half4 PBRShading(MInputData inputData,SurfaceMapData surfaceData)    &#123;        BRDFData brdfData;        InitializeBRDF(surfaceData,brdfData);        //ShadowMask        #if defined(SHADOWS_SHADOWMASK) &amp;&amp; defined(LIGHTMAP_ON)            half4 shadowMask = inputData.shadowMask;        #elif !defined (LIGHTMAP_ON)            half4 shadowMask = unity_ProbesOcclusion;        #else            half4 shadowMask = half4(1, 1, 1, 1);        #endif        Light mainLight=GetMainLight(inputData.shadowCoord,inputData.positionWS,shadowMask);        MixRealtimeAndBakedGI(mainLight,inputData.normalWS,inputData.bakedGI);        uint meshRenderingLayers = GetMeshRenderingLayer();        //间接光GI            half3 giColor = GlobalIllumination(brdfData,inputData.bakedGI,surfaceData.occlusion,inputData.positionWS,inputData.normalWS,inputData.viewWS);        //直接光        half3 mainLightColor=0;        half3 additionalLightsColor=0;        #ifdef _LIGHT_LAYERS            if (IsMatchingLightLayer(mainLight.layerMask, meshRenderingLayers))        #endif            &#123;                mainLightColor = LightingPhysicallyBased(brdfData,mainLight,inputData.normalWS, inputData.viewWS);            &#125;        #if defined(_ADDITIONAL_LIGHTS)        uint pixelLightCount = GetAdditionalLightsCount();        LIGHT_LOOP_BEGIN(pixelLightCount)            Light light = GetAdditionalLight(lightIndex,inputData.positionWS,shadowMask);            #ifdef _LIGHT_LAYERS                    if (IsMatchingLightLayer(light.layerMask, meshRenderingLayers))            #endif                    &#123;                        additionalLightsColor += LightingPhysicallyBased(brdfData,light,inputData.normalWS, inputData.viewWS);                    &#125;        LIGHT_LOOP_END        #endif        #if defined(_ADDITIONAL_LIGHTS_VERTEX)            half3 lightColor=mainLightColor+additionalLightsColor+inputData.vertexLighting * brdfData.diffuse+giColor;        #else            half3 lightColor=mainLightColor+additionalLightsColor+giColor;        #endif        lightColor.rgb+=surfaceData.emission;        #if REAL_IS_HALF            return min(half4(lightColor,surfaceData.alpha), HALF_MAX);        #else            return half4(lightColor,surfaceData.alpha);        #endif    &#125;        Varyings vert (Attributes v)    &#123;        Varyings o;        UNITY_SETUP_INSTANCE_ID(v);        UNITY_TRANSFER_INSTANCE_ID(v,o);        UNITY_INITIALIZE_VERTEX_OUTPUT_STEREO(o);        o.positionCS = TransformObjectToHClip(v.positionOS.xyz);        o.positionWS=TransformObjectToWorld(v.positionOS.xyz);        o.normalWS=TransformObjectToWorldNormal(v.normalOS);        o.tangentWS.xyz=TransformObjectToWorld(v.tangentOS.xyz);        o.tangentWS.z=v.tangentOS.w*GetOddNegativeScale();        o.uv = v.texcoord;        //额外光源        half3 vertexLightColor = VertexLighting(o.positionWS,o.normalWS);        //雾效因子        half fogFactor=0;        #if !defined(_FOG_FRAGMENT)            fogFactor=ComputeFogFactor(o.positionCS.z);        #endif        #ifdef _ADDITIONAL_LIGHTS_VERTEX            o.fogFactorAndVertexLight = half4(fogFactor, vertexLight);        #else            o.fogFactor = fogFactor;        #endif        //环境光uv        OUTPUT_LIGHTMAP_UV(v.staticLightmapUV, unity_LightmapST, v.staticLightmapUV);        OUTPUT_SH(o.normalWS, o.vertexSH);        //阴影uv        #if defined(REQUIRES_VERTEX_SHADOW_COORD_INTERPOLATOR)            #if defined(_MAIN_LIGHT_SHADOWS_SCREEN)                o.shadowCoord=ComputeScreenPos(o.positionCS);            #else                o.shadowCoord=TransformWorldToShadowCoord(o.positionWS);            #endif        #endif        return o;    &#125;    half4 frag (Varyings i) : SV_Target    &#123;        UNITY_SETUP_INSTANCE_ID(i);        UNITY_SETUP_STEREO_EYE_INDEX_POST_VERTEX(i);        half4 finalColor=1.0;        SurfaceMapData surfaceData=(SurfaceMapData)0;        InitializeSurfaceData(i.uv,surfaceData);        MInputData inputData=(MInputData)0;        InitializeInputData(i,surfaceData.normalTS,inputData);                finalColor = PBRShading(inputData,surfaceData);        finalColor.rgb = MixFog(finalColor.rgb, inputData.fogCoord);        return finalColor;    &#125;    ENDHLSL&#125;CustomEditor &quot;PBRShaderGUI&quot;</code></pre><p>脚本代码:</p><pre><code class="language-C#">using System.Collections;using System.Collections.Generic;using UnityEditor;using UnityEngine;public class PBRShaderGUI : ShaderGUI&#123;    private MaterialProperty baseMap;    private MaterialProperty baseColor;    private MaterialProperty normal;    private MaterialProperty normalScale;    private MaterialProperty metallicMap;    private MaterialProperty metallic;    private MaterialProperty smoothnessMap;    private MaterialProperty smoothness;    private MaterialProperty occlusionMap;    private MaterialProperty occlusion;    private MaterialProperty emissionMap;    private MaterialProperty emissionColor;    private MaterialProperty cull;    private MaterialProperty srcBlend;    private MaterialProperty dstBlend;    private MaterialProperty srcAlpha;    private MaterialProperty dstAlpha;    private MaterialProperty zWrtie;    private MaterialProperty zTest;    static bool Foldout(bool display, string title)    &#123;        var style = new GUIStyle(&quot;ShurikenModuleTitle&quot;);        style.font = new GUIStyle(EditorStyles.boldLabel).font;        style.border = new RectOffset(15, 7, 4, 4);        style.fixedHeight = 22;        style.contentOffset = new Vector2(20f, -2f);        style.fontSize = 11;        style.normal.textColor = new Color(0.7f, 0.8f, 0.9f);        var rect = GUILayoutUtility.GetRect(16f, 25f, style);        GUI.Box(rect, title, style);        var e = Event.current;        var toggleRect = new Rect(rect.x + 4f, rect.y + 2f, 13f, 13f);        if (e.type == EventType.Repaint)        &#123;            EditorStyles.foldout.Draw(toggleRect, false, false, display, false);        &#125;        if (e.type == EventType.MouseDown &amp;&amp; rect.Contains(e.mousePosition))        &#123;            display = !display;            e.Use();        &#125;        return display;    &#125;    private void FindProperties(MaterialProperty[] props)    &#123;        baseMap=FindProperty(&quot;_BaseMap&quot;,props);        baseColor=FindProperty(&quot;_BaseColor&quot;,props);        normal=FindProperty(&quot;_BumpMap&quot;,props);        normalScale=FindProperty(&quot;_BumpScale&quot;,props);        metallicMap=FindProperty(&quot;_MetallicMap&quot;,props);        metallic=FindProperty(&quot;_Metallic&quot;,props);        smoothnessMap=FindProperty(&quot;_SmoothnessMap&quot;,props);        smoothness=FindProperty(&quot;_Smoothness&quot;,props);        occlusionMap=FindProperty(&quot;_OcclusionMap&quot;,props);        occlusion=FindProperty(&quot;_OcclusionStrength&quot;,props);        emissionColor=FindProperty(&quot;_EmssionColor&quot;,props);        emissionMap=FindProperty(&quot;_EmissionMap&quot;,props);        cull=FindProperty(&quot;_Cull&quot;,props);        srcBlend=FindProperty(&quot;_SrcBlend&quot;,props);        dstBlend=FindProperty(&quot;_DstBlend&quot;,props);        srcAlpha=FindProperty(&quot;_SrcBlendAlpha&quot;,props);        dstAlpha=FindProperty(&quot;_DstBlendAlpha&quot;,props);        zWrtie=FindProperty(&quot;_ZWrite&quot;,props);        zTest=FindProperty(&quot;_ZTest&quot;,props);    &#125;        private bool albedoFoldout;    private bool normalFoldout;    private bool metallicFoldout;    private bool smoothnessFoldout;    private bool emissionFoldout;    private bool aoFoldout;    private bool zFoldout;    private bool cullFoldout;    private bool alphaBlendFoldout;    private Material material;    public override void OnGUI(MaterialEditor materialEditor, MaterialProperty[] properties)    &#123;        FindProperties(properties);        material=materialEditor.target as Material;        albedoFoldout=Foldout(albedoFoldout,&quot;Albedo&quot;);        if(albedoFoldout)        &#123;            EditorGUI.indentLevel++;            materialEditor.ShaderProperty(baseMap,baseMap.displayName);            materialEditor.ShaderProperty(baseColor,baseColor.displayName);            EditorGUI.indentLevel--;        &#125;        normalFoldout=Foldout(normalFoldout,&quot;Normal&quot;);        if(normalFoldout)        &#123;            EditorGUI.indentLevel++;            EditorGUI.BeginChangeCheck();            materialEditor.ShaderProperty(normal,normal.displayName);            if(EditorGUI.EndChangeCheck())            &#123;                if(material.GetTexture(normal.name)!=null)                    material.EnableKeyword(&quot;_NORMALMAP&quot;);                else                    material.DisableKeyword(&quot;_NORMALMAP&quot;);            &#125;            materialEditor.ShaderProperty(normalScale,normalScale.displayName);            EditorGUI.indentLevel--;        &#125;        metallicFoldout=Foldout(metallicFoldout,&quot;Metallic&quot;);        if(metallicFoldout)        &#123;            EditorGUI.indentLevel++;            EditorGUI.BeginChangeCheck();            materialEditor.ShaderProperty(metallicMap,metallicMap.displayName);            if(EditorGUI.EndChangeCheck())            &#123;                if(material.GetTexture(metallicMap.name)!=null)                    material.EnableKeyword(&quot;_METALLICMAP&quot;);                else                    material.DisableKeyword(&quot;_METALLICMAP&quot;);            &#125;            materialEditor.ShaderProperty(metallic,metallic.displayName);            EditorGUI.indentLevel--;        &#125;        smoothnessFoldout=Foldout(smoothnessFoldout,&quot;Smoothness&quot;);        if(smoothnessFoldout)        &#123;            EditorGUI.indentLevel++;            EditorGUI.BeginChangeCheck();            materialEditor.ShaderProperty(smoothnessMap,smoothnessMap.displayName);            if(EditorGUI.EndChangeCheck())            &#123;                if(material.GetTexture(smoothnessMap.name)!=null)                    material.EnableKeyword(&quot;_SMOOTHNESSMAP&quot;);                else                    material.DisableKeyword(&quot;_SMOOTHNESSMAP&quot;);            &#125;            materialEditor.ShaderProperty(smoothness,smoothness.displayName);            EditorGUI.indentLevel--;        &#125;        aoFoldout=Foldout(aoFoldout,&quot;AO&quot;);        if(aoFoldout)        &#123;            EditorGUI.indentLevel++;            EditorGUI.BeginChangeCheck();            materialEditor.ShaderProperty(occlusionMap,occlusionMap.displayName);            if(EditorGUI.EndChangeCheck())            &#123;                if(material.GetTexture(occlusionMap.name)!=null)                    material.EnableKeyword(&quot;_OCCLUSIONMAP&quot;);                else                    material.DisableKeyword(&quot;_OCCLUSIONMAP&quot;);            &#125;            materialEditor.ShaderProperty(occlusion,occlusion.displayName);            EditorGUI.indentLevel--;        &#125;        emissionFoldout=Foldout(emissionFoldout,&quot;Emission&quot;);        if(emissionFoldout)        &#123;            EditorGUI.indentLevel++;            EditorGUI.BeginChangeCheck();            materialEditor.ShaderProperty(emissionMap,emissionMap.displayName);            if(EditorGUI.EndChangeCheck())            &#123;                if(material.GetTexture(emissionMap.name)!=null)                    material.EnableKeyword(&quot;_EMISSION&quot;);                else                    material.DisableKeyword(&quot;_EMISSION&quot;);            &#125;            materialEditor.ShaderProperty(emissionColor,emissionColor.displayName);            EditorGUI.indentLevel--;        &#125;        zFoldout=Foldout(zFoldout,&quot;Z&quot;);        if(zFoldout)        &#123;            EditorGUI.indentLevel++;            materialEditor.ShaderProperty(zWrtie,&quot;ZWrite&quot;);            materialEditor.ShaderProperty(zTest,&quot;ZTest&quot;);            EditorGUI.indentLevel--;        &#125;        cullFoldout=Foldout(cullFoldout,&quot;Cull&quot;);        if(cullFoldout)        &#123;            EditorGUI.indentLevel++;            materialEditor.ShaderProperty(cull,&quot;CullMode&quot;);            EditorGUI.indentLevel--;        &#125;    &#125;&#125;</code></pre>]]></content>
      
      
      <categories>
          
          <category> Shader </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Shader </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
